# Current state

**Status:** IN PROGRESS (last verified 2026-09-12 — loader records for every
overlay family read off the disc, seeded and compiled; runtime proof pending)

> **2026-09-11 — script translation work moved to its own repo.** Content and
> content tooling now live in the sibling checkout
> `BreathOfFire3Translations` (private; `docs/DECISIONS.md` there records why).
> The interface back here is unchanged and one-directional: it writes
> `generated/bof3_xlate_<code>.c`, which `CMakeLists.txt` already globs, so a
> language is a file drop and nothing in this repo changed. What stays here is
> the apply path — `src/bof3_localize.c`, `src/bof3_xlate_table.h`, the CMake
> glob, and `[localization].languages` in `game.toml`. That repo extracts and
> aligns all four official scripts (Japan, USA, France, Germany) off the discs
> in `isos/`; alignment is 14075 of 14076 Japanese messages for the US and
> German, 14067 for the French. **The glossary moved with it**: point
> `BOF3_GLOSSARY` at `termbase/wiki_terms.tsv` there, or keep using the copy on
> the D drive. `tools/wiki_terms.py` here regenerates it from the wiki and
> fixes 41 rows whose columns were shifted by `colspan` cells in the source
> page (see `docs/TEXT_TABLES.md`).

> **2026-09-11 — the BIOS exception handler runs native; kernel interpreted
> work is down 87.7% (OpenBIOS) / 97.8% (retail).** The 44.6% kernel sink from
> [`kernel-patch-sites.md`](kernel-patch-sites.md) is fixed in `psxrecomp`
> branch `feat/kernel-install-slot-ranges` (`c12f0371`, off upstream master
> `ed55299b`, rebased there so the PR is only this change): install slots
> became RANGES with an explicit resume shape, the
> bless verifier skips them, and the dirty-RAM interpreter hands back at a
> range end. `kernel_bless.mismatch` 21 → 0 on OpenBIOS and 74 → 0 on retail;
> total interpreted instructions per frame -42.7% / -93.9%, both measured with
> `PSX_BIOS_HLE=0` so the kernel-call HLE tier cannot move the retail figure. Residual kernel
> work is exactly declared-words x entries, so only the guest's own patched
> instructions interpret (Rule 18). Card read traces are byte-identical across
> the A/B on both images, all 32 entries. MMX6 also caught a harness flaw: the
> A0/B0/C0 vectors are not constant across the A/B on every title (its B0
> vector goes to zero), so they are no longer discounted from the headline. Measure it yourself with
> `python psxrecomp/tools/kernel_patch_ab.py [--bios psxrecomp/bios/SCPH1001.BIN]`, which
> A/Bs one binary via `PSX_KERNEL_PATCH_RANGES=0`. **Open:** push the branch,
> open the PR, re-measure on a second title, then bump the pin. The A0/B0/C0
> vector trampolines are untouched by design and are now 87% of what OpenBIOS
> kernel RAM still interprets. **Open as
> [psxrecomp#346](https://github.com/RetroPortingToolKit/psxrecomp/pull/346)**
> (branch `feat/kernel-install-slot-ranges`, `baca0a8a`); the pin here already
> points at it. Both tools now live in `psxrecomp/tools/`.

> **2026-09-05 evening — "compiled band, still interpreted" root-caused and
> fixed on the compile side.** The enrichment's first lead (title screen:
> resident SCENA16, every spanning piece from SCENA19/04/06) was not an
> extract-side attribution gap. `compile_overlays.py --static`'s
> isolated-fragment pass keyed demands by bare address, so one occupant per
> band got all the fragments and its 19 siblings none (23,728 unserved
> (entry, image) demands across the multi-section bands). Fixed on psxrecomp
> fork branch `fix/static-fragments-per-variant` `2fa3472a` (per-variant
> demands, pooled fragment compiles, memoized data-as-code rejections, one
> unit per image); enrichment branch committed as `6e760748`; both pushed.
> Verified headless at the title screen: interpreted SCENARIO-band entries
> 12 → 2 (both unharvested PCs). Cost: `generated/` 358 MB → 1.6 GB, build-dbg
> exe 699 MB. `harvest_interp_pcs.py` / `area_poller.py watch` now print and
> record the gap split (seedable / attribution / outside compiled code).
> Details: HANDOFF → Enrichment.

> **2026-09-04 — mixed sections are extracted by default.** `extract_overlays.py`
> now takes `code` + `mixed` (405 sections, 184 of 200 AREA files) instead of
> `code` alone (338, 126 files); `--include-mixed` is accepted and ignored,
> `--no-mixed` is the A/B escape hatch, `axis_b_loop.sh` follows. Settles the
> open question from 2026-09-02 with the evidence it asked for: **58 of 200 AREA
> files ship no `code` section at all**, so their `mixed` section is their only
> compilable code (AREA000 MacNeil Village, AREA001/002 Dauna Mines) — excluded,
> those areas ran fully interpreted. All 67 mixed sections are section 13 of an
> AREA file loading to `0x801F2C00`, the WORLD band that is already a known
> interpreted sink. `mixed` turns out to be a classifier artifact: the sections
> are leaf-heavy and small, failing `emi_survey`'s absolute `prologues>=4` gate
> while passing density comfortably (WORLD04 AREA176-180: 34 `jr ra`, 3
> prologues). Cost of the old default was performance, not correctness — the
> interpreter ran them. Details: HANDOFF "Mixed sections are extracted by
> default".
>
> **2026-09-04 — the Axis B stop condition is now an estimate, not "0 new PCs".**
> [`tools/pc_coverage.py`](../tools/pc_coverage.py) reports estimated harvest
> coverage (Chao2 over per-session incidence), stratified `--by band` (default,
> with a static `jr ra` function-start ceiling per band) or `--by area` (named
> from `names/areas.toml`), so the report says *which* parts of the game are
> under-sampled instead of only whether the last session found anything.
> `harvest_interp_pcs.py` now writes the incidence it needs: a per-row
> `sessions` list (`--session`, default a timestamp) plus `areas` stamped on PCs
> newly seen in a pass (`--area`); `area_poller.py watch` passes its own session
> id so 15-minute re-harvests fold into one sampling unit, and stamps the
> resident area. It prints a coverage line after each harvest.
> **Not yet meaningful:** the accumulated 1 689 PCs (1 574 entered) are all
> legacy rows with no session id — Chao2 needs **two** sessions carrying ids, so
> the report says "not estimable yet" until two more play sessions land. Static
> ceiling for context: 9 705 distinct function starts across all `.EMI` sections
> + boot EXE. Rationale and how to read the output: HANDOFF "The stop
> condition". Host tooling only — no runtime, build, or submodule change.
>
> **2026-09-02 — static overlay compile is fast now (framework PR pending).**
> `axis_b_loop.sh` on build-dbg: ~16 min → **90 s** (phase 5a 12 min → 20 s via
> a process pool + a linear CPS resume-wrapper pass — cProfile showed 87 % of
> 5a was `add_cps_resume_case` re-scanning a 19 MB string 7,386 times; phase 5b
> 4 min → 69 s via one translation unit per overlay, 358 units, globbed by
> `runtime.cmake`; **build-relprof 1016 s → 162 s** for the same rebuild). Outputs
> byte-identical to the old path (diff -r), same
> shard summary, headless boot 99.92 % static hits. Lives in `psxrecomp` fork
> branch `perf/static-overlay-parallel` (`7ab698ca`, = upstream `04d9184b` +
> 1); PR mstan/psxrecomp#296 held as a **draft until play-tested more**; pin back to
> `mstan/master` when it merges. See HANDOFF "Shipping
> state". The harvest log is now kept (`analysis/harvest_last.log`,
> `harvest_sessions.log`).

> **2026-09-01 late — readability track opened: name sidecar + subsystem map.**
> Human names now live outside the regenerated C: `names/overlays.toml` (alias /
> role / status / evidence per overlay, keyed by section **md5**, not PC — bands
> overlap) and `names/functions.toml` ((md5, pc) per overlay function; boot-EXE
> names stay in `symbols.toml`). [`tools/name_map.py`](../tools/name_map.py)
> seeds/merges/validates them; [`tools/subsystem_map.py`](../tools/subsystem_map.py)
> renders [`docs/subsystem_map.html`](subsystem_map.html) — bands → overlays →
> 29 036 overlay functions (roots, spans, in-overlay jal edges, honest heat) +
> the 1 026 boot-EXE functions, searchable by name/alias/PC/md5, no bytes
> embedded. Coverage: 2 overlays `evidence`, 204 `hypothesis` (filename-derived),
> 133 `unnamed` (all AREA/COMMU), 0 overlay functions. Routes to earn names
> (resident script block + screenshot → areas, Psy-Q signatures → boot EXE,
> differential traces → Attack/Battle_Init) are in [`NAME_MAP.md`](NAME_MAP.md).
> Evidence gathering: [`tools/area_poller.py`](../tools/area_poller.py) —
> `watch` during play (identifies the resident AREA with certainty by hashing
> the `0x80010000` script block against `emi_sections.json`, screenshots each
> change, drains the overlay native ring), `harvest` once at end of session
> (now `axis_b_loop.sh` phase 2a, incl. `--harvest-only`), `summarize --apply`
> to write sightings as `evidence`. `axis_b_loop.sh` phase 6 refreshes `names/`
> + the map on every rebuild path. First live run done 2026-09-01: 6 WORLD00 areas identified
> (AREA001/002/006/009/024/031); transition shots were black → fixed with a
> settled-shot delay; native-ring parse fixed (nested `ring`) — it is a per-call
> native trace, which unblocks the differential tracer.

> **2026-09-01 — Capcom-logo lag FIXED (root-caused + compiled).** The opening/
> Capcom logo sequence runs from **`LOGO/LOGO.EXE`** — a standalone 120 KB PS-EXE
> loaded to **`0x801CE000`** (text 0x1D800, entry 0x801CE724), NOT an `.EMI`
> overlay. The `.EMI` extraction pipeline never captured it, so it ran **100%
> interpreted** (~19 present-fps, native dispatch ≈0, one PC `0x801CEEDC` = 84.5%
> of all interp work / 91 M insns). **This corrects the earlier "Capcom slow =
> dispatcher overhead" theory** (below): it was never dispatch *cost* — it was a
> whole uncompiled executable. The RAM overlap with the PLCHAR/battle bands
> (which reuse `0x801CE400+` later) only *camouflaged* it as a CRC-missing PLCHAR
> band. **Fix:** new tool [`tools/extract_logo_overlay.py`](../tools/extract_logo_overlay.py)
> synthesizes a `static-emi-v1` capture from the disc EXE (same jal+prologue
> discovery as `extract_overlays.py`, no external analyzer); `compile_overlays.py`
> compiles it natively like any overlay (1609→1656 funcs, 0 new audit fails); the
> CRC gate handles the RAM overlap. **Result:** `0x801CEEDC` fully native (gone
> from the profile), Capcom interp 5.0M/s→0.25M/s (20×), cumulative interp
> 107.8M→16.3M, present fps **19→steady ~30**, wall-time ~22s→~14s, no visual/boot
> regression. The residual ~30fps is now **pacing-limited** (CPU idle during the
> intro — FMV/CD streaming of `CAPCOM30.STR`), not CPU-limited. Durability: LOGO
> merged into `analysis/overlay_captures_all.json` and re-merged every run by
> `axis_b_loop.sh` phase 3a (extract_overlays rebuilds that file from `.EMI` only
> and would otherwise drop it). Pass-2 (harvest LOGO-resident interiors →
> `logo_observed.json`) landed the 10 jump-table interior entries.
>
> **The residual interior "whack-a-mole" is root-caused and generalized.** LOGO
> dispatches per-frame effect handlers through **function-pointer tables** that
> are zero in the image and populated at runtime (`lw v0,0(sN); jalr ra,v0`) — the
> static walk can't see the targets, so each pass makes the last handler native
> and the interpreter entry just shifts to the next unregistered `jalr` target.
> Traced via the caller ring → dispatcher (`0x801CF980`, walks a 7-slot table at
> `0x801D9CA4`). **Key rule:** a compiled static *root* is NOT automatically
> reachable by `jalr` — a function-pointer call needs the address registered as a
> *dispatch* entry too (`0x801D22EC` was a compiled root yet still interpreted).
> New [`tools/harvest_logo_handlers.py`](../tools/harvest_logo_handlers.py)
> generalizes the fix: statically locate the fn-ptr dispatch tables (forward
> const-prop), read them from a live LOGO-resident session, callable-boundary
> filter, emit every handler at once (found 12 tables → 18 real handlers, 26
> data-array false positives dropped). Rebuilt with all 18 registered. This
> pattern recurs in any title with effect/handler tables.

> **2026-09-01 evening session.** The **Axis B loop is now a one-command script**
> ([`tools/axis_b_loop.sh`](../tools/axis_b_loop.sh): harvest → extract →
> catalog → codegen-hash → compile → build) and is **proven end-to-end**. A
> second play session **banked 239 new PCs** (world map, shops, save/memcard
> screens) → observed set **1195 distinct PCs (1080 entered)**, up from 956/850;
> the new interpreted sinks are SCENARIO band `0x801F6C00` (67 M, doubled) and
> mixed BATTLE band `0x801D0C00` (124 M). Added the **overlay catalog sidecar**
> ([`tools/overlay_catalog.py`](../tools/overlay_catalog.py) →
> `analysis/overlay_catalog.json`): family/subsystem, band membership +
> co-residency, root provenance, honestly-attributed heat. Rebuilt and
> **verified healthy** (headless: 99.29% native dispatch, crc_misses Δ0, 152.9
> emu fps). Benign audit-failure count drifted 4 → 6 (expected). **Pending:** the
> per-PC re-measure confirming those 239 went native needs a play session
> re-exercising that content. Perf: world map + memcard ~50 fps, Capcom still
> ~10-20 fps (savestate anchors slot 08/09/10 — see [`HANDOFF.md`](HANDOFF.md)).

> **2026-09-01 session.** Synced the framework: merged upstream `mstan/master`
> into our fork branch `fix/static-overlay-residency-signal` (`psxrecomp` now
> `ecc0de16` = our 3 commits + 77 upstream), and bumped `recomp-ui` `8c30e004`
> → `4eda654` (**required** — the merged psxrecomp uses the multi-disc launcher
> API that lives in recomp-ui). **Upstream PR held as a draft by decision** — the
> fork is a living integration branch we keep pulling master into; the
> proof-of-process is a full BoF3 decompile with subsystems intact. Full rebuild
> **verified booting**: overlays dispatch native at ~99.6% steady-state hit rate,
> residency signal intact — the CD-ROM/DMA merge did not regress it. Two sync
> gotchas + the recipe are in [`HANDOFF.md`](HANDOFF.md) → "Shipping state".
> Non-blocking follow-up: pre-merge savestates load with `last_ok: 0` (merge
> reworked `savestate.c`).

Living status doc — the one place a new session learns where the project
actually stands. Update this rather than `CLAUDE.md`. Durable findings graduate
into their own `docs/` file (see [`README.md`](README.md)); this stays a short
"where we are, what's next". History lives in the [Log](#log) at the bottom —
do not stack dated banners above this paragraph; add a Log row and update the
sections instead.

## Where we are

**The game plays at full speed.** It boots, renders, has audio, takes input,
writes memory cards, and has been played past the prologue and intro boss into
the world map, shops and save screens. On the optimised tree (`build-relprof`)
the Capcom logo, world map and memory-card screens all hold a user-verified
60 fps with clean audio (2026-09-01). It has not been played end to end.

**The game's data is readable off the disc (2026-09-05).** Items, abilities,
places and the roster are id→name sidecars in `names/` generated by
`tools/text_tables.py`; the save verifier proves them against the card saves
(ability list types, weapon ATK, armour DEF all match). The world-map place
names are painted plates in the map textures, decoded by `tools/plates.py`
and transcribed in `names/plates.toml`. [`TEXT_TABLES.md`](TEXT_TABLES.md).

**Framework pins bumped 2026-09-10: `psxrecomp` `2fa3472a` → upstream `master` `ed55299b`, `recomp-ui` `db12620` → `26756c2`.** Both upstreams moved to the `RetroPortingToolKit` org on 2026-09-09 (`mstan/*` URLs still redirect); `.gitmodules` follows. The blocker that held the pin on a fork branch since 2026-09-06 is gone: psxrecomp master read six `RecompLauncherCNetplayLaunch` fields that lived only on mstan's unmerged `merge/frameblend-localization`, and that branch is now merged into recomp-ui master (`13d7d69`). Upstream psxrecomp builds against a published recomp-ui for the first time, so the pin is plain `master` again and nothing fork-only is left behind. `recomp-ui` stays on the fork branch `feat/additional-ui-functionality` ([#48](https://github.com/RetroPortingToolKit/recomp-ui/pull/48), open), refreshed by merging upstream master in — 53 commits, three conflicts, `ctest` identical to pristine master. **The bump forces a regenerate:** the new `dirty_ram_interp.c` calls `psx_overlay_static_can_dispatch`, emitted by `compile_overlays.py` only after the old pin, so a stale `generated/` fails at link. Base-EXE codegen came out byte-identical (0 of 35 shards updated); only the overlay layer changes. Done and verified: `build-relprof` links a 1.11 GB exe that boots to an OpenGL 3.3 context at 60 Hz with all three localization tables registered. **`PSX_STARVATION_TIMEOUT_US=0` can now be cleared** (`setx PSX_STARVATION_TIMEOUT_US ""`, new shell) — #321 is in this pin. See [`HANDOFF.md`](HANDOFF.md) → *Pins and branches*.

**`build-relprof` is current again, and much faster than the last recorded
figure.** It had not been relinked since the fragment fix; doing so needed the
stale pre-#325 overlay objects cleared first (see HANDOFF → *Building against
the pin*). Two runs of `fmv_bench.py` on the rebuilt tree: **213.8 and 226.8
present/s** during the Capcom FMV (876 MB exe), against the 99.6 recorded on
2026-09-05 — ~3.6× realtime, enormous headroom over the 59.9 it needs. Interp
during the window rose with it (38.6 k → ~450 k insns/s in ~66 k blocks/s,
≈6.8 insns per block), which is consistent with many more, much smaller native
fragments handing control back at their boundaries — worth confirming rather
than assuming.

**Most of the game is overlays, and they are all compiled.** 81.6% of the boot
EXE's text segment is zero-fill that `.EMI` sections load into at runtime
([`OVERLAYS.md`](OVERLAYS.md)). All 880 containers are enumerated, the code
lands in exactly ten RAM bands (405 unique sections, 3.61 MB), and **all ten
bands plus the standalone `LOGO/LOGO.EXE`** are extracted from the disc and
compiled in as CRC-guarded static overlays — bytes from the disc, entry points
from play ([`OVERLAY_EXTRACTION.md`](OVERLAY_EXTRACTION.md)). Steady-state
dispatch is ~99% native with `aborts: 0`. The runtime capture path
(`[runtime] overlay_cache`, the DLL loader) stays off, so OV-1 never arms.

**Coverage inside the bands is the remaining performance work (Axis B).** A
compiled band is not a fully native band: interior entry points reached only
through jump tables and function pointers are invisible to the static call-edge
walk and fall to the interpreter until a live session observes them. The loop
is one command ([`tools/axis_b_loop.sh`](../tools/axis_b_loop.sh)) and is
converging (325→56→20→6 new PCs per replayed session; new content still adds —
the last new-content session banked 239). Observed set: **1195 distinct PCs
(1080 entered)**. Detail and the traps in [`HANDOFF.md`](HANDOFF.md).

**The text engine is identified and confirmed live.** Renderer, stepper,
immediate draw, font-atlas mapper and the per-block message-table formula are
all pinned ([`TEXT_ENGINE.md`](TEXT_ENGINE.md)); cross-language disc diffing
independently isolates the script to the `0x80010000` `.EMI` section, and the
whole translation surface is enumerated ([`regional-builds.md`](regional-builds.md)).
No Western release is an address-compatible donor, and none has runtime
language support. Translation is not bound by the JP byte budget.

**Headlines that still hold:**

- Recompilation is clean under `strict = true` — 2.5M lines, 35 shards, 0
  skipped, 0 unsupported, 1467 dispatch entries.
- Seeding is a dead end, proven three ways ([`OVERLAYS.md`](OVERLAYS.md) §3).
- The all-bands overlay compile exits 2 with a handful of `[audit]`
  `0 unknown_bad, N unsupported` shard failures (data walked as code). That
  set drifts upward as the observed set grows — **7** on the 2026-09-01 rebuild
  (`0x800C1800` ×3, `0x801F2C00` ×3, `0x801EEC00` ×1). Not a regression.

## Build trees

| Tree | Config | Use |
|---|---|---|
| `build-relprof` | RelWithDebInfo, `PSX_DEBUG_TOOLS=ON`, `PSX_STATIC_RUNTIME=ON` | **Play and measure.** Holds 60 fps; has the debug server. Since 2026-09-11 the DWARF lives in `BreathOfFire3_Recompiled.exe.debug` next to the exe (`PSX_SPLIT_DEBUG`) — a single-file RelWithDebInfo exe no longer loads on Windows |
| ~~`build-dbg`~~ | Debug (-O0) | **Retired 2026-09-06.** Every `PSX_*` option was identical to `build-relprof` (15 keys, zero differences) and the runtime has **no `assert()` calls**, so `-O0` bought nothing but slowness — it ran *below real time* on the intro and card screens, and it silently lacked the dirty-PC enrichment. Reconfigure on demand (ccache makes it cheap) only to step through framework C in a debugger |
| `build-release` | Release, `PSX_DEBUG_TOOLS=OFF` | Shipping config. Cannot be inspected at all |
| `build-recompiler` | Release | Emitters (`psxrecomp-game`, `psxrecomp-bios`) |

`PSX_STATIC_RUNTIME` defaults OFF outside Release/MinSizeRel; a dynamic exe
picks up a stale `libstdc++-6.dll` from PATH and dies at startup, so force it
ON. Both debug-tool trees were regenerated and rebuilt against the `1bf70960`
pin on 2026-09-01 (see Log).

## In flight

- **[psxrecomp#367](https://github.com/RetroPortingToolKit/psxrecomp/pull/367)
  — `compile_overlays --static` crashes at the end of every run on master**,
  awaiting review. Upstream `3a174fab` reads an unbound `cache_dir` in a
  DLL-only reconciliation; the output is written first, but the exit 1 aborts
  `axis_b_loop.sh` before the rebuild. The `psxrecomp` pin is master `193a60b8`
  + this one commit (`4a792379`); bump to its merge commit when it lands.
  HANDOFF → *Pins and branches*.
- ~~psxrecomp#346 — kernel install-slot ranges~~ **merged 2026-09-12** as
  `c5390e42`; in the current pin.
- **recomp-ui #48** (launcher UI work; #42 was folded into it) awaiting upstream
  merge. Refreshed against master 2026-09-15 (`26756c2` → `20e0540`, merge not
  rebase, one conflict resolved upstream-first). The `recomp-ui` pin moves back
  to upstream master when it lands.
- ~~**The Axis B harvest is much thinner than its session count suggests**~~
  **Retracted 2026-09-16.** The "11 duplicate sessions" and the 3.3–18.8 %
  band coverage were an artifact of `pc_coverage.py`'s subset detector: it
  merged with union-find, so any session that banked zero new PCs (a subset
  of *every* session, since all share the same kernel / boot-EXE core) glued
  unrelated sessions together. The 2026-09-13 boss session it called a
  re-harvest holds 29 PCs no other session saw. Fixed (no transitivity; an id
  is absorbed only into a superset of itself), and the coverage percentages
  are retired as the headline — see *Next up 1*.
- **Savestate compatibility across the framework bump** is unchecked; the
  `savestate.c` rework (`47bda817`) made pre-merge `.pst` files load with
  `last_ok: 0` once already. In-game memory-card saves are the reliable path.

## Next up

0. ~~**Readings on inserted names — built, see it on screen.**~~ **DONE
   2026-09-11 (user, real play):** a real item pickup and a master's skill
   line (record 1) both draw with readings under *Japanese (Ruby)*.
   [`INSERT_RUBY.md`](INSERT_RUBY.md) is the build record.
0b. ~~**BIOS exception handler: take it upstream.**~~ **DONE 2026-09-11 —
   open as [psxrecomp#346](https://github.com/RetroPortingToolKit/psxrecomp/pull/346).**
   Was the largest single interpreted sink (44.6 % of all interpreted
   instructions, every scene). Shipped as one change rather than four PRs
   because the parts do not pay off separately, plus a fifth part the plan
   missed (the interpreter hand-back at a range end). Every kernel-bless
   mismatch is now zero on both BIOS images and on both titles measured.
   [`kernel-patch-sites.md`](kernel-patch-sites.md) →
   *The fix and what it bought*.
1. **Run the warp sweep on every fresh build; play only for BOSS / PLCHAR / SCENARIO proof.** *Re-scoped
   2026-09-12 evening:* `python tools/warp.py --boot --slot 5 --areas 0-199 --walk --attribute --harvest`
   then `tools/warp_gap.py` on its report; unseeded reads **0** on the 15:30
   build (14 before the pointer-root rule, 46 before the harvest self-heal). *Earlier the same day:* the interior entry points every family hides behind `jalr` are
   now seeded from the disc ([`LOADER_RECORDS.md`](LOADER_RECORDS.md)), so play
   is no longer how entries get found — it is how the seeds get **proven**
   and how the profile gets **weighted**. The first visit to an unplayed area,
   a boss and a party change on the 08:44 exe should show
   `harvest_interp_pcs.py` reporting few or no new interior entries; if it
   still reports many, the records are incomplete (the 18 data-classed areas,
   the world map, the PLP tail run are the known gaps).
   **Proven in play 2026-09-13/16:** a new boss (BOSS012/013) added 11
   BOSS-band PCs, each entered once for 1–26 instructions; two and a half
   hours of new WORLD01/02 areas added 19. The seeds hold.

   **The headline metric is now interpreted instructions per frame, lower is
   better** (`python tools/interp_rate.py`, also printed by every harvest).
   The coverage table that used to sit here measured the *size of the
   harvested set* against an estimated population, and once the bands were
   seeded from the disc that population became tiny by design — the estimate
   read 1.8–5 % and looked like a failing harvest when the truth was
   "nothing left to harvest". A PSX frame is ~560 000 guest instructions:

   | Session (build-relprof, OpenBIOS) | Frames | Interp insns / frame | Of guest |
   |---|---:|---:|---:|
   | 2026-09-12 evening | 212 324 | 301 | 0.05 % |
   | 2026-09-13 boss + WORLD00 | 155 625 | 336 | 0.06 % |
   | 2026-09-16 morning, WORLD01/02 | 536 607 | 392 | 0.07 % |
   | 2026-09-16 evening | 304 965 | 382 | 0.07 % |

   Short runs parked in one screen read high (4 900–5 800 on the two
   sub-25 000-frame rows) and the runtime's frame counter can restart inside
   a session while the interpreted-instruction counter does not, so compare
   long walks only. What those 300–400 instructions are: the A0/B0/C0 kernel
   trampolines, the OpenBIOS exception-handler prologue at `0x27B4` (declared
   words × entries, rule 18), and the boot-EXE stub run `0x80164D28..E90` that
   battle transitions dirty — none harvestable by play. Play still pays for
   proof (BOSS / PLCHAR / SCENARIO seeds) and for translation coverage; the
   loop after a harvest stays mechanical (`axis_b_loop.sh`).
1b. ~~**NEXT: the A0/B0/C0 vector trampolines.**~~ **FIXED 2026-09-17,
   measured headless** — they were never "untouched by design": the emitted
   native-stub guard already handles the vectors and only knew the retail
   stub shape, so OpenBIOS's `addiu $t0,$zero; jr $t0` stub failed closed to
   the interpreter on every one of the game's ~230 `ChangeTh` yields per
   frame. Two-shape guard on psxrecomp fork branch
   `feat/openbios-vector-stub-shape`, open as [#381](https://github.com/RetroPortingToolKit/psxrecomp/pull/381); vectors 459 → **0.0**
   per frame, all-interp 845 → 144 on OpenBIOS, retail unchanged.
   [`vector-stub-shapes.md`](vector-stub-shapes.md). **Left:** a real walk
   on `build-relprof` read with `tools/interp_rate.py` (expect the 290–390
   per frame to fall toward the `0x27B4` floor), bump the pin to the merged
   commit (checkout sits on `integration/vector-stub-plus-367` `88f4582a`
   meanwhile, which also carries item 1c). The headless wall-clock A/B is
   inconclusive (day-to-day drift), but **the user reports the save /
   memory-card screen visibly faster (2026-09-17 evening)** — the screen
   that hits both this fix (~1,300 `ChangeTh` yields per frame) and 1c (a
   decompressor run per card read) at once. That play report is the
   wall-clock evidence.
1c. ~~**The decompressor copy loop.**~~ **FIXED 2026-09-17, measured
   headless** — the non-kernel residual left after 1b was `func_80164CE4`'s
   unrolled byte copy entered mid-body by a computed jump (`jr t7`,
   `t7 = base + n*12`, Duff's device), which the recompiler could not
   resolve, so the trampoline had no key at the interior PC and the
   interpreter ran every copy on every load. New emitter resolver
   `resolve_computed_stride_jump` (psxrecomp [#382](https://github.com/RetroPortingToolKit/psxrecomp/pull/382),
   branch `feat/computed-stride-jump`): the loop's interpreted work over
   twenty headless area loads went 684,626 → **0**, all else unchanged;
   **the user's walk read 384 → 152 per frame on the same area.** The one
   boot-EXE row that walk still had, the decoder's unchecked-index state
   table at `0x80164AB0`, got a sibling resolver
   (`resolve_self_limited_jump_table`, second commit of #382).
   [`computed-stride-jump.md`](computed-stride-jump.md). **Left:** a real
   walk with battles read with `tools/interp_rate.py` (expect ~120 per
   frame, the `0x27B4` floor, instead of 384), the PR, the pin bump.
2. ~~**Ship the two psxrecomp fork branches**~~ **DONE** — `feat/dirty-pc-enrichment`
   and `fix/static-fragments-per-variant` both reached upstream master, which
   is why the pin sits on plain `ed55299b` and no fork branch carries anything
   the pin lacks (HANDOFF → *Pins and branches*). The follow-up that is still
   open and still worth doing: **demand fragments only for the occupant that
   actually ran a PC** (observed rows carry `areas` and `occ_crc`), which would
   cut `generated/` from 1.6 GB back toward 400 MB and buy back the ~9-10 %
   headless throughput the big binary costs in i-cache. Do it after the next
   Axis B round, since that round is what supplies the per-occupant evidence.
3. **Translation: first light is on screen (2026-09-09).** The delivery
   path is built and verified: a framework function-entry plugin at
   `MsgBox_Reset` repoints the box at English bytes in enhancement memory,
   the table comes from the US disc slot for slot, capitals only
   ([`LOCALIZATION_APPLY.md`](LOCALIZATION_APPLY.md)). Next, in order:
   play a real session through several areas and read the plugin's
   `miss` lines; decide the lowercase route (the glyph sheet is full: paint
   over menu-only cells, a second sheet with a mapper patch, or stay in
   caps); give the apostrophe a cell via `[[vram_patch]]`; key conflicting
   English by area. Menus/items/name entry stay a separate pool by decision.
   **Japanese (Ruby) rides the same plugin** (`jp_ruby`, built the same
   evening, verified on screen); its next step is the reading review pass.
4. ~~Name the text-engine functions in `symbols.toml`~~ **DONE 2026-09-05**
   (13 `confirmed` rows, `psx_symbols.h` regenerated).
5. ~~**Audit the 211 zero-fill dispatch addresses**~~ **DONE 2026-09-06 — not a
   live bug** ([`zero-fill-dispatch-audit.md`](zero-fill-dispatch-audit.md)).
   All three 2026-08-30 figures reproduce (211 entries, 18 seeds, 1,187,899
   zero bytes), but **182 of the 211 (86%) are now compiled as real overlay
   code** — they were overlay landing zones, not fabrications; `0x801A27A8` is
   `Script_ShowMessage`. Of the 29 left, 22 are in the `0x801F2C00` WORLD band
   already tracked by Axis B. The dispatch guard decides on live bytes, so a
   resident overlay always blocks the stale entry; the failure window needs a
   call into a never-loaded overlay, i.e. already-invalid execution. Optional
   cleanup: drop the 18 seeds at the next regeneration (no coverage lost, but
   it costs a full rebuild — not worth a cycle of its own).
6. ~~**Name the two game helpers Psy-Q did not cover**~~ **DONE 2026-09-17** —
   `0x8014E494` = `Packet_Commit` ([`GFX_PACKETS.md`](GFX_PACKETS.md)),
   `0x8015E908` = `SE_Play` ([`SOUND_CUES.md`](SOUND_CUES.md)); 18 boot rows
   added to `symbols.toml`, pushed into the Ghidra program. Left: hear a cue.
7. **First-screen headroom (user, 2026-09-05).** The save / memory-card / load
   screens and the Capcom intro are the first things a player sees and sit
   near the frame budget. Measured: the card screens are **99% BIOS-interpreted,
   ~0 overlays** — a BIOS/card-path problem, not the overlay work (which is
   confirmed neutral here). Two intake entries: [`IDEAS.md`](IDEAS.md) **I5**
   (faster save/card via the BIOS/card path — rank the hot B0 ordinals on
   `slot11`, then compile-the-BIOS-path vs enable call-HLE) and **I6** (why the
   intro caps ~45 fps on dbg — settle pace-bound vs CPU-bound on a windowed run;
   the intro does not execute headless).

8. **Audio, what is left after 2026-09-17** ([`SOUND_CUES.md`](SOUND_CUES.md)):
   the effects side is closed (cue words are slots, every sample named and
   homed, enemies named from the area tables). Open: **music** (sequences,
   the 16 sequencer voices, fades — untouched); the effect interpreter's
   sound opcode (only needed to change *when* a spell sounds); the enemy
   record's **resistance grid and drop slots** against the wiki, which would
   also adjudicate the two wiki/US-disc name-pair disagreements; and the
   one-record **ability-name shift** still carried by `text_tables.py` and
   `names/magic.toml`'s name columns (`audio_banks.py` compensates).

## Known issues, non-blocking

- **Starvation watchdog `exit(2)`**, reported as `reason: atexit` /
  `exit_origin: "unknown"`. Debug-tree safety net, not a game fault. The
  2026-09-02 15:01 trip (`build-dbg`, frame 5524, Glauss Mountain) fired with a
  **402 µs-old heartbeat**: `starvation_watchdog_check` reads the clock before
  the shared timestamp, and the debug-server IO thread stamps that timestamp
  from `send_all_blocking`, so a send landing between the two reads wraps the
  unsigned subtraction. Upstream patch drafted in
  [`starvation-watchdog-false-trip.md`](starvation-watchdog-false-trip.md)
  (read heartbeat first, atomic store/load, exit-origin label); **filed 2026-09-05
  as [mstan/psxrecomp#321](https://github.com/mstan/psxrecomp/pull/321)**.
  **Disabled on this machine for debug testing**: `PSX_STARVATION_TIMEOUT_US=0`
  is persisted as a user environment variable (`setx`), so launcher and shell
  runs of every build tree inherit it. Not a config-file key — the runtime reads
  only the env var. Re-enable with `setx PSX_STARVATION_TIMEOUT_US ""` if a
  genuine emu-thread stall needs the SIO ring dump.
- **Two ~87 MB freeze dumps at every boot** (frame ~328, `slow_frames` then
  `hard_freeze` false positive). They land in the **process working directory**,
  which for a launcher or shell run is the **repo root**, not the build tree —
  the 2026-09-06 cleanup found 37 of 45 sitting in the root (3.2 GB total), so
  the old advice to prune `build-*/` alone missed most of them. Prune both:

  ```bash
  find . -maxdepth 2 -name 'psx_freeze_dump_*.json'     -not -path './psxrecomp/*' -not -path './recomp-ui/*' -delete
  ```

  `axis_b_loop.sh` still handles `build-dbg` only. At ~174 MB per boot this
  accumulates fast — worth running after any session.
- ~~**Headless TCP `savestate load` wedges emulation on the current pin**~~
  **Does not reproduce on `build-relprof` (2026-09-06).** `tools/scene.py
  check` boots, loads and verifies every slot: **11 of 12 load and resume
  headless** at 300-460 emulated fps. The twelfth, slot 8, is *refused* at the
  header — not wedged — because it is a pre-v7 file (`version=5`,
  `codegen_hash=A4319B6F`) written 2026-09-01, and half of the original
  two-slot evidence. The wedge as reported was `build-enrich` (Debug) only,
  and that tree no longer exists, so it cannot be re-tested; if it returns,
  `scene.py`'s post-load `verify_running()` now names it as a wedge and dumps
  registers + `irq_state` while the guest is still frozen, instead of letting a
  `last_ok: 1` poison the next measurement. **Test beds no longer need to drive
  input from a cold boot** — see [`SAVESTATES.md`](SAVESTATES.md) "Driving a
  scene headlessly".
- **Savestate files can refuse** in overlay-heavy code: `savestate_poll` needs
  `psx_irq_resume_context_snapshot_safe()` (`g_cosim_dirty_pump_site == 0`), and
  an interrupt taken inside the dirty-RAM interpreter is never snapshot-safe.
  Much rarer now that the bands are compiled; in-game slot saving works,
  including in combat. Load with Enter/Start; the TCP `state load` path wedges
  the listener on the windowed build (works headless — that half is the
  starvation watchdog, `PSX_STARVATION_TIMEOUT_US=0`).
- **One unreproduced fail-fast** into kernel RAM `0x00002934` on a slot-7
  resume — [`crash-kernel-ram-2934.md`](crash-kernel-ram-2934.md). Fix only
  with a live repro.
- **Four upstream framework observations**, none affecting BoF3: F-1 / F-2 in
  [`BRINGUP.md`](BRINGUP.md), F-3 / F-4 in [`LOCALIZATION.md`](LOCALIZATION.md).

## Environment

MSYS2 MinGW-w64 at `C:\msys64` (GCC 16.2.0, CMake 4.4.2, Ninja 1.13.2, ccache,
SDL3). Not on PATH by default — every build shell needs
`export PATH=/c/msys64/mingw64/bin:$PATH`, or `cc1` crashes silently. `python`
is Anaconda 3.13; `python3` without the prepend is the Store stub. Run the exe
from PowerShell with `$env:` for env vars. `gh` 2.98 is installed and
authenticated as `kerokline`. Ghidra 12.1.3 + GhidraMCP project at
`D:\Utilities\GhidraProjects\BoF3` (1025 functions); the repo-root `.mcp.json`
points at the ghidra-mcp stdio bridge (`127.0.0.1:8089`), which needs the
Ghidra GUI running. Prior text-decode work at `D:\BoFIII`.

## Log

| Date | Entry |
|---|---|
| 2026-09-18 | **The enemy resistance grid is found, and elemental damage is read end to end.** `Battle_ElementAffinity` `0x8009FA78` (already decompiled in-tree, never read) takes a **9-bit affinity mask** (only `& 0x1F` reaches this function) and, per set element bit, sums `0x800B187C[class]` — a percent table `{300,200,100,75,50,25,0,-100,-1}`, so class 7 **heals** the target and a two-element hit on a neutral target is 200%. The class bytes are species record **`+0xC0..+0xC4`** (party: persistent record `+0x2B..+0x2F`); `Battle_EnemySpawn` `0x800A9148` copies nine bytes `+0xC0..+0xC8`, and **all nine are affinity classes** — fire / ice / lightning / earth / wind / holy / psionic / status / death, the community wiki's own nine categories in its own order (supplied by the player, which is what corrected this entry: an earlier revision read the last four as "type/size" classes, inheriting BATTLE_RAM's unverified label on enemy `+0x34`). They feed **three** tables: elements sum through `0x800B187C`, **holy** goes through `0x801EAF70` `{300,200,200,150,125,100,50,0}` inline in `Battle_ScaleDamage` under bit `0x20`, and psionic/status/death go through `0x800B188C` `{-1,150,100,75,50,25,0,0}` in the sibling function `0x8009FD08` (bits `0x40`/`0x80`/`0x100`, assigned not summed). The 2026-09-17 search missed it because it looked in `+0x00..0x47` and because the record's fields run `+0x48..+0xCD` against a `0x88` stride, so they fall outside the slice `enemy_table.py` cuts. **All 4,032 affinity bytes on the disc are in range `0..8`**, and the semantics hold up three ways: undead/plants take 300% on element 0, fire creatures absorb it, and in the `START.EMI` templates Garr takes 50% on it while パピー (the intro dragon) is immune — so **element 0 is fire**, with 1 = ice and 2 = lightning. **The bit order was then settled the same day from the weapon table** (user challenge: "confirm the ability mask bit is the correct element bit" — it was not). `Battle_CalcDamage` `0x801DC00C` takes the mask as an argument, parks it in **PSX scratchpad `0x1F800000`**, and on the sentinel `0xFFFF` loads it from **weapon record `+0x0B`** (`0x801C9F2F + id*0x14`). All 22 elemental weapons carry a single bit and every one is named for it: `0x01` **fire** (Flare Sword, Flame Spear, Flame Chrysm), `0x02` **ice** (Ice Halberd, Ice Chrysm), `0x04` **lightning** (Thunder Rod), `0x08` **earth** (Rockbreaker), `0x10` **wind** (WindCutter, Gale Javelin, FeatherSword) — the community wiki's one-based order, and consistent with flying species being immune on bit 3. `0x20` is **holy** — Holy Avenger, Ghostbuster, Silver Knife, Ascension, Rufadés Spear — and the undead are exactly the species weak to it (Zombie / Ghoul / Ghost / Phantom / Reaper at 300%, Dragon Zombie / Thanotos / Arwan at 200%, 396 of the 448 species rows neutral). Enemies pass `0`, so **enemy physical attacks are never elemental**. **Retracted the same day:** the earlier claim here that the ability record's `u16_4` low byte was the element mask — it gets fire/ice/lightning right and earth/wind backwards (Simoon `0x08`, Quake `0x10`), so it is not the mask. A spell's element lives in its **effect handler** (sixteen pass `0xFFFF`, one passes a literal `4`), reachable only through the ability → handler index mapping that is still open. **The structural proof is the neutral defaults:** the three tables have neutral at class 2, 5 and 2 respectively, and each column's modal value lands on its own table's neutral (1,460/2,240 element bytes at class 2; 396/448 holy at 5; psionic at 2; 110/448 immune to death). Wiki cross-check **attempted and not completed** by fetch (fandom 402, GameFAQs 403) — the categories came from the player; the wiki's per-cell *numbering* is still undecoded (PainWeed reads `0 0 0 0 0 5 4 4 2` on disc vs `7 7 7 / 7 7 5 / 4 4 2` on the wiki, last four literal). [`BATTLE_RAM.md`](BATTLE_RAM.md) *The resistance grid*, [`TEXT_TABLES.md`](TEXT_TABLES.md). |
| 2026-09-18 | **The item/ability `ref` u16 is the description id, and every one of the 538 resolves.** Prompted by the PC port naming the same field `descriptionId`, `ref` was run through `Msg_SystemPtr` `0x801503F8`: `base = 0x80014000 + u32[0x80014000 + ((ref >> 12) & 0xC)]`, then `base + u16[base + 2*(ref & 0x3FFF)]`. Every ref we hold has bits 14–15 = `01` and so selects header word 1, a **455-message table** (word 0 holds 176) in the 11,560-byte system pool — one blob shared byte-identically by 42 of the 44 `.EMI` files that carry one, with `FIRST.EMI`/`AFLDKWA.EMI` holding a second variant. **All 311 item refs and all 227 ability refs land in range and resolve to the right description**, and the first number in each weapon/armour description equals the `power` we had extracted separately in **147 of 149** records — the two exceptions have flavour text with no digits. Abilities drift off `0x40FB + id` at id 58, so the column is authored per record; only 12 of 227 ability descriptions contain a digit, so `b2`/`b3` (AP cost?) stay unread. Trap paid for: a raw `[0-9]` byte scan scores ~0 because `0x30`–`0x39` appear as the trailing byte of two-byte kanji — the walk has to be control-aware. [`TEXT_TABLES.md`](TEXT_TABLES.md), [`PC_PORT_CROSS_REFERENCE.md`](PC_PORT_CROSS_REFERENCE.md) §3. |
| 2026-09-18 | **A second reverse-engineering effort against a different binary of the same game corroborates our control codes — and cost us two corrections.** Surveyed `bof3ext` (a MinHook `ddraw.dll` for the 2001 Chinese PC port, local clone only, no `LICENSE`): the port kept the PSX data shapes (character record stride **164 = `0xA4`**, an area-number byte, a `MsgBox_Reset`-shaped box re-point) and its independently derived control-code table agrees with [`TEXT_ENGINE.md`](TEXT_ENGINE.md) on eleven codes. Two rows were wrong and are fixed **on our own evidence, not theirs**: `0x10`/`0x11` are **instant-print start / end** — both run `0x801490A0 ^= 0x10` and the per-glyph delay is only computed when that bit is clear, with 101 uses of each and the depth returning to zero in 6,696 of 6,696 messages; and `0x0C` is a **box/presentation selector**, not a head-only speaker id — 2,419 uses, **780 of them mid-message** (where `MsgBox_Step` shares `0x05`'s handler and ignores it), head arguments 83% `05`/`06` across 15 distinct values. `0x801490CA` is written by `MsgBox_Reset` and read by no boot-EXE code, so an overlay reads it; that watch would close the question. No pipeline impact — `build_script_xlate.py` already carried `0x0C` through. What does **not** transfer: the port's `.DAT` archives are repacked (TOC at `0x198`, no `MATH_TBL`, subfile 0 is a RIFF WAVE), and its Chinese script is a JP→ZH translation in a byte-swapped encoding behind a PC-only index split — not worth extracting against the aligned US script. [`PC_PORT_CROSS_REFERENCE.md`](PC_PORT_CROSS_REFERENCE.md). |
| 2026-09-17 | **The "unread 15 KB string table" in `GAME.EMI` §0 is the item/ability name table, now paired with the US disc's names.** The regional-builds census had left it as "nobody has read what it contains"; it is the five item tables plus the ability table `TEXT_TABLES.md` already reads off the JP disc, in the same record order with every `name[8]` widened to `name[12]` (US dest `0x80195800`, consumables `0x801C8964` … abilities `0x801CA718`). `tools/text_tables.py --us-cue` reads the US record at every JP index, compares every numeric field (538 records, 0 mismatches, refuses to write otherwise) and adds a `us` column to `names/items.toml` and `abilities.toml`. Result: 33 rows gain an English name the wiki glossary lacked (`Part A..H`, `Spearfish`, `Baselard`, `Tiger Claws`, `Coupons`, `Hypnotize`, …), three glossary rows differ from what Capcom shipped (`とっこう薬` = `Ginseng` where the wiki's *Wonder Drug* is the literal 特効薬, a localizer's choice not a wiki error; `シンカー` = `Sinker`; ids 121/122 = `Flame Breath` / `Frost Breath`), eleven wiki-named abilities ship as the misspelt placeholder `Noting`, and the US name alphabet is ASCII plus `0x8E` = apostrophe with `=` and `>` standing in for hyphen and full stop. Answer to the open question: yes, it needs translating, and the 8-byte JP field is the constraint. [`TEXT_TABLES.md`](TEXT_TABLES.md) "The US names". |
| 2026-09-17 | **Enemy names found and extracted: every area's species table, 448 rows, 167 of 168 glossed from the wiki.** The name cards come from the area, not the working record: each `AREAnnn.EMI` loads a 1,160-byte section at `0x800E4000`, eight `0x88`-byte species records indexed by the creature slot (record `+0x60`), with the 8-byte kana name at `+0x48` and the stat halfwords at `+0x54` (zenny, EXP, level, max HP, AP, ATK, DEF, AGI, Int — the wiki's Orc page matches やけっぱちオーク field for field). `tools/enemy_table.py` → `names/enemies.toml`; `names/enemy_gloss.toml` seeded through the built-in browser from the BoF wiki's enemy list (fetch and WebFetch are both blocked there); `se_watch` reads the species name live on every bank-6 cue and only asks for an English name the gloss lacks; the audio catalogue names bank-6 samples by species (`Lizard (ENEMY040) 1`). Enemy audio itself: one pre-baked 8-slot group per area (`ENEMYnnn` = `AREAnnn`), 58 distinct groups, not a party-style concatenation. [`SOUND_CUES.md`](SOUND_CUES.md), [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-17 | **Every sample on the disc has a name, by the player's convention: owner file + VAG number.** `tools/audio_banks.py names --apply` names all 833 samples (`Heal (MAGIC069) 1`, `Rei 3`, `System 10`; owner = first file by family precedence, lowest-numbered; ability / area alias / character as the stem; a stem suffix only when two owners' samples really differ) into `names/se_cues.toml` as `auto`, status `derived`, beside the player's `label`; `se_watch` offers the auto name as the default answer. The party voice files explained the slot behaviour: `BPLD034` = character ids 0,3,4 sorted, banks 3/4/5 in digit order — Momo moved from bank 5 to 4 when Nina (1) left and Peco (6) joined because the file became `BPLD056`. All ten player labels agree with their auto names. [`SOUND_CUES.md`](SOUND_CUES.md). |
| 2026-09-17 | **The sound catalogue is keyed by sample, and every sample has a disc home.** The player's diagnosis held: a cue word is a slot. `tools/se_resolve.py` follows the runtime's chain (cue entry → libsnd VAB registry `0x8018EB18`/`0x8018EB60` → VAG → SPU RAM bytes, hashed) live or from a `.pst`; `se_watch` labels the hash and records every `cue@context` a sample arrived through, asking after a quiet gap instead of mid-burst. Then the .EMI side: **the (6, 8, 7) triplet is a miniature VAB** — type 6 the header, type 7 the body, and **type 8 the cue-table entries the file installs at the head of its bank's table** (4 bytes per cue word; entries past the record are not cleared, which is why the live tables keep changing and a menu word can play a spell's sample). `MAGIC069` (Heal / the three-sound "Command") = 4 VAGs installed as `0x100..0x103`, and the catalogue hashes match the disc bytes byte-for-byte (Target = VAG 1, Whisk Away = VAG 2, Locked On = VAG 4). `tools/audio_banks.py` decodes all 901 triplets (833 distinct samples on the whole disc) into `names/audio_banks.toml` and `join --apply` writes each labelled sound's disc homes into `names/se_cues.toml`: 28 of 29 matched. Banks by file: 1 = `COMN_SE` (the menu set, duplicated in every battle file) + each spell's own samples, 2 = battle/area effects, 3..5 = `BPLCHAR` party voices dealt at party load, 6 = `BENEMY`/`BOSS` creatures. Corrections on the way: `SE_Play` stores bank before id (the decompiler's order was wrong), the field/battle context must not come from the resident overlay (`BATTLE.EMI` lingers, `SHOP.EMI#8` shares the spell band), the earlier type-3 = audio reading was wrong (art). [`SOUND_CUES.md`](SOUND_CUES.md), [`EMI_TYPES.md`](EMI_TYPES.md). |
| 2026-09-17 | **Battle cues are picked by slot, weapon class and creature type, not by spell.** Second `se_watch` session (the user relabelled the menu cues; two tool bugs fixed on the way: the compiled `SE_Play` stores bank before id, the reverse of the decompiler's order, and field/battle is now keyed by hashing the live bank 1+2 tables because `BATTLE.EMI` stays resident after a fight). Chasing the "Chlorine" cue `0x502`: `Battle_PlayActorCue` (`BATTLE.EMI 0x801DD820`) maps index 0..5 x actor kind 0..2 onto `0x03nn/0x04nn/0x05nn` through a 6 x 3 table at `0x801EAF80`, so banks 3/4/5 are the three party slots' personal sets and `0x502+0x504` is Nina's swing pair (`Battle_SwingCue_Step` `0x801DFA14`, index 3 on the crit roll vs `ctx+0xAA`), fired for 毒撃 because it is a physical skill. Hit sounds: `Battle_PlayHitSound` (engine `0x800A8764`) indexes `0x800B202C[crit][class]` with class = weapon record byte `+0x0D` (0 staves, 1 daggers, 2 swords, 5 ammo, 6 spears) or enemy record `+0x71`. Creature sounds: `Battle_PlayCreatureCue` `0x801E39BC` = `0x600 | obj+0xE0 << 1`. `MAGIC008.EMI` has no sound call (12 of 141 BMAGIC overlays do, all bank 1), so spell sounds go through the effect interpreter, probably `SE_PlayTracked` `0x8015E10C` (named; it hides its caller from the write trace). [`SOUND_CUES.md`](SOUND_CUES.md) "Battle: how a cue is chosen", `tools/se_watch.py`, `names/se_cues.toml`. |
| 2026-09-17 | **The two unnamed boot helpers are named: `Packet_Commit` and `SE_Play`.** Headless Ghidra decompiles of the boot EXE (`ghidra_run.py export --decompile`) plus the live tables in `slot00/02/03`. `0x8014E494` = **`Packet_Commit(layer, size)`**: the caller has built a primitive at the cursor `0x80145988`; the call `CatPrim`s it onto the tail of OT layer `layer` (8 tails at `0x801459CC`) and bumps the cursor by `size`, inside a per-frame `0x9000`-byte arena at `0x80018000 + db*0x9000` with a `0x34` guard — over budget the packet is *silently dropped*. `Main_FrameLoop` `0x8014AAC8` owns the flip (`db` = `0x80143D44`), `Packet_FrameReset` `0x8014AF98` re-arms cursor and tails, `Packet_FlushToOT` `0x8014B06C` `AddPrims` each chain into the 8-entry reverse OT, so layer 0 draws on top (text = 1, window frames = 6/7). `0x8015E908` = **`SE_Play(cue)`**, `cue = bank<<8 | id`, bit `0x8000` = keep the caller's L/R volume: seven bank handlers `SE_CueSetup_Bank0..6` (table `0x80182CA4`, the three words after it are `SoundSet_Layout` data, not banks 7..9) read a 31-entry × 4-byte cue table at `0x8014869C + bank*0x7C` {VAB override, pan|prog, tone|priority, chord|voice} and the bank's VAB header `0x80148A14[bank]`, then up to four `SsUtKeyOnV` on SPU voices 16..23; a lower-priority cue on a still-keyed voice (`SE_PollKeyStatus` `0x8015DA34`, per frame) is dropped. The cue tables are **zero in the EXE** — read from savestates: banks 3..6 identical in field and battle, banks 1 and 2 both swapped for battle, so a cue id is not a sound id across modes. `SoundSet_Layout` `0x801621F8` lays 3 sets (3/7/2 VABs) out from `0x8011A000`; `slot02/03` are set 0, `slot00` set 2. Also named: `SE_PlayVol` / `SE_PlayAtObject` / `SE_PlayNearSelf` (distance boxes into the curve `0x80182C00`), `Boot_Init` `0x8014E974`. 18 rows, all `emit = false`; `ghidra_run.py names` pushed 28. [`GFX_PACKETS.md`](GFX_PACKETS.md), [`SOUND_CUES.md`](SOUND_CUES.md). |
| 2026-09-17 | **The decoder's unchecked-index state table runs native; play-side proof of the copy-loop fix.** The user's walk through the same area read **384 → 152 interpreted instructions per frame** on the copy-loop build (all of it the kernel patch ranges plus one boot-EXE row). That row, `0x80164AC4`, was the file decoder's state dispatch: a real in-image pointer table at `0x80164AB0` whose index is a stored, pre-scaled byte offset with no `sltiu/beq` guard and no `sll`, so the bounded-table resolver had nothing to prove its extent with. `resolve_self_limited_jump_table` (second commit of psxrecomp [#382](https://github.com/RetroPortingToolKit/psxrecomp/pull/382)) takes the extent from the table's own layout: words from the base while each is a 4-aligned in-function code address outside every delay slot, ending where the lowest target begins. Same CPS-default safety argument as the stride resolver. One match in the boot EXE (five entries), every other shard byte-identical; the synthetic test gained a second function. Overlays recompiled, relprof rebuilt; the twenty-area headless probe shows the row gone and only the kernel patch ranges left (34.6 per frame over the warp window). Pin bumped to `integration/vector-stub-plus-367` `88f4582a`. #381 stays a separate PR by design (one-file BIOS-emitter guard vs a game-emitter analysis pass). [`computed-stride-jump.md`](computed-stride-jump.md). |
| 2026-09-17 | **The decompressor's copy loop runs native; the whole non-kernel residual of a play session is gone.** The first walk after the vector fix still read 384 interpreted instructions per frame because a pure field walk is ~118 (headless slot-5 probe: all of it the `0x27B4` exception prologue and the card stubs) and the rest was one loop: `func_80164CE4`, the file decompressor's literal copy, 32 unrolled `lbu/nop/sb` groups entered mid-body through `jr t7` with `t7 = base + n*12` (Duff's device; the base is planted in the decoder context by `0x80164A3C`). No table in memory, so the recompiler emitted a bare CPS tail-transfer, the trampoline found no dispatch key at the interior PC, and the dirty-RAM interpreter ran the rest of the copy, every iteration, every load — battle transitions, area changes, the card screen. Live RAM over the loop equals the disc image word for word, so the harvest's `bootexe` label ("written at run time") was wrong for it: an entry-point gap, not dirty text. Fix in the game emitter: `resolve_computed_stride_jump` walks the chain back from the jr (`addu R,B,S`; `S` from one `sll` or the two-`sll` sum), requires N ≥ 2 shape-identical groups after the delay slot, and the emitter then registers the interior labels and switches on the runtime target exactly as it does for a jump table, CPS default kept, so a wrong guess costs dead cases and never correctness. Exactly one match in the boot EXE (33 entries); everything else byte-identical. Overlays recompiled through `axis_b_loop.sh --skip-harvest --force` (27 audit-only shard failures, unchanged). **Same twenty-area headless warp on both builds:** copy-loop interp 684,626 → **0**, kernel rows unchanged, all-interp 42.8 → 32.9 per frame. Fork branch `feat/computed-stride-jump` `67c79e8f` off upstream master (+ `test_computed_stride_jump_codegen.py`, CTest); the checkout sits on `integration/vector-stub-plus-367` `85894111` (= #367 + #381 + this). Left: a real walk with battles (`tools/interp_rate.py`, expect ~120 per frame), the PR, the pin bump; fix the `bootexe` class description in `harvest_interp_pcs.py`; re-anchor slot 5. [`computed-stride-jump.md`](computed-stride-jump.md). |
| 2026-09-17 | **The A0/B0/C0 call vectors run native on OpenBIOS; the kernel residual is now the declared patch ranges only.** Plan step 5 turned out not to need a new mechanism: the generated dispatch trampoline already carries `psx_bios_try_native_call_stub`, which reads the four live words at `0xA0`/`0xB0`/`0xC0`, decodes the handler address and tail-transfers without the interpreter — but its guard knew only the retail stub (`lui $t0; addiu $t0,$t0; jr $t0; nop`). A headless relprof boot dumped the live OpenBIOS stub: `addiu $t0,$zero,0x2AA0; jr $t0; nop; nop` (ROM `0x6970`), so the guard failed closed and every kernel call paid an interpreter entry for two instructions. The caller is one site: the game's scheduler yield `func_8014B770` → Psy-Q `ChangeTh` thunk `0x8017F734` (B0:10h), ~230 per frame in the field, ~1,300 on the title screen, 90 M entries over all sessions. Fix: the emitted guard accepts both shapes, still decodes the target from live RAM on every call and still fails closed on any other word (rule 18); cycle steps charge three instructions for the OpenBIOS shape, four for retail. psxrecomp [#381](https://github.com/RetroPortingToolKit/psxrecomp/pull/381) (branch `feat/openbios-vector-stub-shape` `8b50cd09` off upstream master; + `test_bios_vector_stub_shapes.py`, registered with CTest), dispatch totals unchanged (3990 / 13012). **`kernel_patch_ab.py`, ranges ON, per frame:** OpenBIOS vectors 459.2 → **0.0**, all-interp 844.9 → **143.7**; retail 274.4 → 259.2 (noise, its stub already matched). Both images boot clean to 11k+ frames. **Wall clock unproven:** the headless OpenBIOS run went 37.4 → 27.4 s, but so did retail (34.3 → 27.3 s) across the same two days, so that is the machine, not the fix. The `kernel-patch-sites.md` explanation of the MMX6 B0 result is withdrawn in place. Left: a real walk read with `tools/interp_rate.py`, the pin bump once #381 merges. [`vector-stub-shapes.md`](vector-stub-shapes.md). |
| 2026-09-16 | **The "thin harvest" was a tool artifact; the headline metric is now interpreted instructions per frame.** The user's new-area / new-boss sessions banked "underwhelming" PC counts, so the harvest was audited end to end: it is correct. Every session since 2026-09-12 carries ids, area stamps and occupancy CRCs; the boss session added 11 BOSS-band PCs (each 1 entry, ≤ 26 instructions — the boss ran native) and the WORLD01/02 walk added 19. What was wrong was `pc_coverage.py duplicate_sessions`: union-find merged through zero-gain sessions (subsets of every session, since all share the kernel / boot-EXE core), so the boss session with 29 exclusive PCs was reported as a re-harvest of 2026-09-12, and the 3.3–18.8 % / 1.8–5 % coverage figures were built on that merge. Fixed (absorb only into a superset of itself, no transitivity; the report now says "subset sessions" and admits it cannot tell a re-harvest from a fresh run on covered content). The 2026-09-11 Log entry below is superseded. Coverage was also measuring the wrong thing after the seeding, so it is retired as the headline: `harvest_interp_pcs.py` now prints **interpreted instructions per frame** (and no longer prints a meaningless interp-vs-dispatches percentage), `area_poller.py` records it per harvest, and new [`tools/interp_rate.py`](../tools/interp_rate.py) tabulates it per session from the timeline. Long walks read **290–390 per frame = 0.05–0.07 % of the guest**, all of it kernel trampolines, the `0x27B4` prologue and the `0x80164D28..E90` boot-EXE stubs. Queued next: the A0/B0/C0 trampolines (*Next up 1b*). |
| 2026-09-15 | **Pins bumped: psxrecomp `baca0a8a` → `4a792379`, recomp-ui `26756c2` → `20e0540`.** Our [psxrecomp#346](https://github.com/RetroPortingToolKit/psxrecomp/pull/346) merged 2026-09-12 (`c5390e42`); the pin is upstream master `193a60b8` (62 commits on) plus one commit, because **plain master cannot regenerate this title**: upstream `3a174fab` calls a DLL-only reconciliation at the tail of `compile_overlays.main()` with `cache_dir`, which `--static` never binds, so every static run wrote its output and then died with `UnboundLocalError`, exit 1, aborting `axis_b_loop.sh` before the rebuild. Fixed and opened as [psxrecomp#367](https://github.com/RetroPortingToolKit/psxrecomp/pull/367), with `compile_overlays_static_tail` (fails on master, passes on the fix; `test_aot_overlay_pipeline.py` still 24/24). [recomp-ui#48](https://github.com/RetroPortingToolKit/recomp-ui/pull/48) had collided with master again — master's `cb7e54b` independently fixed the same two launcher bugs (duplicate ImGui IDs on shortcut rows, Backspace unbind); merged master in and resolved upstream-first, keeping only what master lacks (no-gamepad pad-column gating, Select-chord labels, the `None` unbind persistence). #48 now reads MERGEABLE/CLEAN. **Regenerated across the bump:** 7 base-EXE shards moved, overlay codegen hash `0x4fe894d2`, 772 overlay units; BIOS dispatch totals unchanged (OpenBIOS 3990, retail 13012) and still no dispatch key inside a declared range. **Trap:** `build-relprof` had drifted to `Release` with `PSX_DEBUG_TOOLS=OFF` (an unset build type defaults to Release in `runtime.cmake`, and cached options persist), so the first rebuilt exe booted with no debug server; reconfigured with every option explicit (HANDOFF → *Building against the pin*). **Verified** on the corrected tree: debug server listening, frames 9 → 10 520 in 30 s headless, `kernel_bless` mismatch 0 with 3 patch ranges / 26 skips, localization plugin registered, `.exe.debug` sidecar present. |
| 2026-09-13 | **EXP / zenny boost sliders built and proven — the first trusted static plugin.** `src/bof3_exp_boost.c` + `mods/preloaded/packages/bof3.exp-boost` (one feature, two integer sliders 1..50): one hook at `Battle_EnemyDefeated 0x801E542C` scales the dying enemy's yields through the current-object pointer `0x801EB458`, so totals, awards and level-ups follow. Headless proof on the user's one-attack-left anchor: 84/6 → 840/60, Teepo +864, wallet +60, same tick count as stock; sliders go down to 0, and the user verified in play that EXP 0 / zenny 10 gives a 0-EXP, no-level-up win with zenny still awarded. Traps paid for: hooks are emitted only at prologues (the BATL_END results phases are alias entries → nothing emitted, unit byte-identical); function-entry callbacks fire whether or not the feature is enabled; the `0x801D0C00` band is shared (check registry id `0x10`); `psx_mod_function_entry` gets `a0` garbage there — read the game's own pointer. [`EXP_BOOST.md`](EXP_BOOST.md) |
| 2026-09-13 | **Steal roll found and the first gameplay mod package shipped.** Pilfer/Steal roll inside their own BMAGIC overlays (MAGIC065 / MAGIC216 at `0x801EEC00`): `(Rand() & 0xFF) < rate[enemy drop class] × agility mod`, rate table `0,1,3,6,12,16,32,32`, mod 4..12 by AGI difference — word for word the community wiki table; the stolen item is enemy drop slot 1 and is cleared on success. Found by a write-traced replay loop (`tools/steal_hunt.py`) plus a Rand-only function filter that lists every roll with its caller; the game-mode function `0x801E42C0` was a false lead. `mods/preloaded/packages/bof3.steal-always` (format 6, two guarded `disc_user` patches turning the random byte into 0) proven 6/6 headless vs 0/22 stock; `CMakeLists.txt` now declares `PRELOADED_MODS_DIR`. Trap: `names/abilities.toml` / `magic.toml` names are shifted one record against the engine's ids (`0x41` is Pilfer, labelled Tsunami). [`STEAL.md`](STEAL.md) |
| 2026-09-12 | **Warp harness: all 200 areas visited headless from one savestate; the records proven and the last static gap found.** `tools/warp.py` pokes the pending-area cells and the mode request byte (`0x80143BB0 := 5`, exactly `Field_ChangeArea`'s writes) from a field anchor the user parked (slot 5): 200/200 areas in 508 s, nine anchor reloads, no failures. Traps paid for: the attract demo is a field state but its SCENA16 script wins every later transition (only the first poke lands); the active-mode byte `0x80143B90` (2 field, 4 box/menu, 5 battle, 8 world map) must never be written; a Circle in the walk step opens a box that never closes; every regenerate stales all `.pst` anchors. Results: sweep 3 on the record-seeded exe entered 92 interior pcs (46 unseeded); the loop compiled its harvest and sweep 4 entered 20 (14 unseeded), none of them from sweep 3 — the self-heal is real. `--attribute` (a dirty-PC snapshot per room) plus new [`tools/warp_gap.py`](../tools/warp_gap.py) classified all 19 residual pairs: **in-image code-pointer runs the descriptor walk does not reach** (per-entity handler tables), nothing else. Generic rule added to `extract_overlays.py` as a default discovery source (`pointer_roots`: aligned in-image words at a `jr $ra`-preceded or prologue target; 2,393 candidates, 19/19 covered, `--no-pointer-seeds` for A/B); compiled the same evening after one wrong turn (shape-only roots doubled the audit-failed shards 26 → 53; gating on the framework's `plausible_callable_target` CFG proof brought it to 27 and the exe shrank 288 → 258 MB). **Sweep 5 on that build: 200/200, 0 unseeded, 0 seeded-but-interpreted, harvest +0** — the AREA band is closed statically. **Axis B's stop condition is now the sweep's unseeded count on a fresh build**, not a coverage estimate. Left interpreting: kernel trampolines and a boot-EXE run `0x80164E48..90` during battle transitions. [`LOADER_RECORDS.md`](LOADER_RECORDS.md) → *Runtime proof*. |
| 2026-09-12 | **Address maps re-verified against the loader-records work, and taught the two things it exposed.** `regions.py check` passed unchanged — the ten-band map, the zero-run scan, the measured spans and the plugin constants `names/regions.toml` derives from were all untouched by that work, which is exactly what the check exists to prove. Two real gaps in `xref.py`, both closed: **(1)** the prose scan globbed `docs/*.md`, so the 1,515 lines in `docs/loader_records/` were invisible — 327 distinct addresses, and the scan is recursive now: addresses cited in docs went **671 → 969**. **(2)** the 1,319 rows of `names/*_records.toml` gave **989 distinct pcs** an identity the maps could not see, so `lookup` now answers e.g. `0x801F2C8C` = `area 0 handler[0]` in `AREA000.EMI#13`, `queue` stops offering those pcs as unnamed, and `stats` counts them in their own tier — an engine entry is an *identity, not a name*, the same tier as a seed root. The sidecar list is imported from `extract_overlays.py ENGINE_RECORD_FILES`, so a sixth sidecar added there needs no edit here. Two deliberate scan exclusions: the record sidecars are read as an identity source but not as prose (a generated row is not a document citing an address, and 1,319 of them would swamp the census), and the generated [`XREF.md`](XREF.md) is never scanned — it had been inflating its own `stats` by one. Nested docs now print as `loader_records/AREA.md` rather than a bare basename that could collide with a top-level doc. **Where the queue points now:** the loader selectors and tables, all cited heavily and none nameable — `0x80143F00` the area number (11 docs), `0x801802EC` the AREA boot table (9), `0x8014686C` the chapter byte (8), `0x80145020` the party combo (7). That makes `names/variables.toml` the clear next sidecar, with the loader tables as regions behind it; neither is seeded yet because their geometries live in prose cells, and guessing a stride would assert what the source states. Also: `names/*_records.toml` were missing from `NAME_MAP.md`'s sidecar index — added. [`ADDRESS_MAPS.md`](ADDRESS_MAPS.md) → *What the loader records changed*. |
| 2026-09-12 | **Every overlay family's entry points are in the disc, not in play — loader records found, seeded and compiled.** Asked what a play session discovers that static analysis cannot, the answer turned out to be *the occupant identity and the profile weight*, not the entry pcs: the engine never enters an overlay through its header run (which the overlay's own code consumes) but through records it keeps per family, all readable off the disc. Four Opus research passes (reports in [`loader_records/`](loader_records/)) found and proved them with 0 counter-examples: **AREA** — boot table `0x801802EC[area]` → a 0x44-byte descriptor at the end of the area's section with an init pc at `+0x40` and a handler array at `+0x3C` (200/200, 67/67, 671/671), plus GAME.EMI's 11-row hook table `0x801C8F54`; **SCENARIO** — `0x801C944C[chapter]` → a 5-slot vtable plus two sub-tables `0x801CDC4C`/`0x801CDC9C` (20/20, 89/89, 211 more); **BOSS** — boss id `u8[0x801462E6]` → `Boss_EncounterTable 0x801CDF18` → `Boss_FileTable 0x801CDFF8` → `Boss_EntryTable 0x800B2048` in the battle engine (55/55), and every BOSS file is a self-contained battle bundle carrying `BATTLE.EMI`'s engine; **PLCHAR** — keyed by the 19-row party combo table `0x801824AC`, entered via GAME.EMI `0x801CD8F0`/`0x801CD964` (19/19 each) into a `u32[2][3]` per-slot dispatch in each image's last six words. Negatives with evidence: no script opcode loads a scenario, no per-character table into the PLCHAR band, `ENEMY<n>.EMI` (`area + 9`) is VRAM data with no code. New [`tools/loader_records.py`](../tools/loader_records.py) writes `names/{area,scenario,boss,plchar}_records.toml` (1,319 rows, 1,267 distinct pcs, every one 4-aligned and inside its own section, joined by md5 because the capture set dedups siblings); `extract_overlays.py` gained `engine_entry_pcs` (`ENGINE_RECORD_FILES`, `--no-engine-seeds`) and joins those plus the 131 magic handler pcs into `static_dispatch_entry_pcs`. **914 pcs were in no seed set at all** (553 AREA, 232 SCENARIO, 100 PLCHAR, 29 BOSS) — about as many interior entries as every play session since 2026-08-31 combined (~1,080), and disjoint from the header seeds (intersection 0). Compiled: `STATIC_DISPATCH_ENTRY` 594 → 1,229, the audit-failure set is byte-for-byte the same 23 shards as before the seeds (all BOSS/WORLD data-walked-as-code drift from observed pcs), `build-relprof` relinked 08:44. Not yet proven at runtime — the first visit to an unplayed area, boss or party change on this exe is the test. Side findings: `0x80145020` is the PLCHAR residency/combo byte, `0x8014686C..` is the scenario state block (closes the unlabelled block-head words), `0x80182830` is the BGM table, a boss dialer needs only `Battle_StartBoss`'s five writes. [`LOADER_RECORDS.md`](LOADER_RECORDS.md). |
| 2026-09-12 | **RAM spans have a naming home: `names/regions.toml` + `tools/regions.py`.** The gap [`XREF.md`](XREF.md) exposed the same day — the repo's most-cited addresses were the overlay band bases and the two `.EMI` block dests, and no sidecar could hold a span — is closed by **16 regions: eleven overlay bands, the two message pools, the insert scratch array, the EXE image, the kernel area.** Every row is *derived* from a committed source and carries the citation, so nothing is asserted: the ten-band map in [`OVERLAY_EXTRACTION.md`](OVERLAY_EXTRACTION.md) (occupants / unique / unique bytes), the zero-run scan in [`OVERLAYS.md`](OVERLAYS.md) §1, the measured spans in [`band-overlap-attribution.md`](band-overlap-attribution.md) (which also supplies the eleventh band, LOGO.EXE's own `0x801CE000`, absent from the ten-band map because it is a PS-EXE not an `.EMI` section), `src/bof3_localize.c`'s own `AREA_BLOCK_LO`/`AREA_BLOCK_HI` and insert geometry, the live-text-block row of [`TEXT_ENGINE.md`](TEXT_ENGINE.md), `disc_probe.json`, and psx-spx for the kernel area. `check` re-derives and exits nonzero on drift (verified by tampering one `end`: caught, and `seed` repaired it while keeping a hand-added alias), so the file cannot rot away from its sources. **Two convention bugs the byte counts caught, which is why every bound is now asserted rather than read:** the zero-run table's dash is *exclusive* at the top (its own 188,415 for `0x80093801`–`0x800C1800` proves it), and the live text block's `0x80017628` is one-past-the-end, not the last byte — `0x80017628 - 0x80014000` is exactly the 13,864 bytes the row states, and it is the same convention `AREA_BLOCK_HI` uses. A row's `end` now travels with a `bound` field saying what it *is* (`measured occupant span` from capture data vs `zero-fill window`, which bounds where occupants may land and is **not** proof one reaches it); band `0x801F6C00` carries no `end` at all, because no committed source gives one. **Independent cross-check:** the derived spans put LOGO.EXE's straddle of the swap slot at 109,568 bytes = 107.0 KB, matching the 107 KB that `band-overlap-attribution.md` states in prose. Overlaps are reported as 13 containments by construction and **2 real straddles**, and `xref.py` lists every containing region narrowest-first — first-match-wins is the `pc_coverage.py` bug that document was written about. Containment is context, not a name: only an exact base counts as resolved, so prose coverage moves 662 → 676 of 2,162 and the `queue` top is now genuinely unnamed work — `0x8015E908` (a JAL root, known game code not library), `0x801CEEDC`, `0x80143F00`. **What still has no home:** the single RAM variable. `0x801490AC` is `MSG_STR_CUR` in `src/bof3_localize.c` and `0x801490A8` is `MSG_STR_BASE`, both cited in six documents, and the naming layer cannot say so. Also fixed: `name_map.py`'s TOML emitter wrote integer fields in decimal, which made a span sidecar unreadable (`base = 2147483648`) and silently defeated the first drift test — `base`/`end` now emit as `0x%08X` like `pc`. Both maps are written up in [`ADDRESS_MAPS.md`](ADDRESS_MAPS.md), which is the doc to read before quoting a region `end`. |
| 2026-09-12 | **The prose is joined to the naming layer: `tools/xref.py` + [`XREF.md`](XREF.md).** The structured side was already a graph (`symbols.toml` by PC, `names/functions.toml` and `names/data.toml` by section md5 + pc, `subsystem_map.py` joining them); the 10,700 lines in `docs/` were not joined to any of it, so an address read in an investigation doc could not be checked for a name without a grep. The tool runs offline over committed files only — `analysis/` is not required, which is why it works on a fresh checkout — and reads the memory layout from `disc_probe.json` rather than hardcoding it. **Census:** 668 distinct `0x80xxxxxx` addresses are cited in `docs/*.md` and 2,159 across docs + `symbols.toml` notes + `names/` evidence fields; 662 of those (30.7 %) resolve to a name, 28 more are unnamed JAL targets already in `seeds/ghidra_funcs.txt`. **The finding the `queue` view produced:** the most-discussed unresolved addresses are the overlay band bases — `0x801D0C00` (14 docs), `0x801EEC00` (13), `0x801F2C00` (12), `0x801CE400` and `0x80196800` (10 each), `0x800C1800` (9), `0x801F6C00` (8) — plus the two `.EMI` block dests `0x80010000` (12) and `0x80014000` (8). They are the repo's most-cited landmarks and none of them has a naming home: `symbols.toml` holds `[[func]]` entries only, and `names/overlays.toml` keys by md5 with no dest field, so band bases and RAM variables can only be named in prose. A `names/regions.toml` landmark sidecar would close it; not built, because the band list lives in gitignored `analysis/` and entering the bases by hand would be assertion, not evidence. **Limits stated in the tool and in the doc header:** an address alone cannot say code vs data (81.6 % of `.text` is overlay zero fill, and data islands share addresses with sibling code), one address can resolve to several overlay functions because bands share load addresses, and a non-exact hit is reported as the nearest named entry at or below with its delta — neither side records a span, so that is a neighbourhood, not containment. `shared` lists the 14 addresses that six or more documents discuss. Regenerate `XREF.md` when docs, `symbols.toml` or `names/` change. |
| 2026-09-11 | **Kernel patch sites FIXED and open upstream as [psxrecomp#346](https://github.com/RetroPortingToolKit/psxrecomp/pull/346).** Install slots became RANGES with a `len` and an explicit `resume` (`jalr` / `fallthrough` / `none`) compared against the ROM-baked word rather than against zero; the bless verifier compares a body in segments that skip them; and `dirty_ram_interp` hands back at a range end, which the plan did not have and without which nothing works (kernel page 0 is permanently dirty, so straight-line interpretation never leaves it). Both profiles declare their measured ranges and `SCPH1001.toml` pins `[program.image] sha256` to v2.2. **Per frame, `PSX_BIOS_HLE=0`, matched frames, one binary A/B'd via `PSX_KERNEL_PATCH_RANGES=0`:** mismatch 21 → 0 (OpenBIOS) and 74 → 0 (retail); kernel interp −47.6 % / −97.8 %; all interp −42.7 % / −93.9 %. The residual is exactly declared-words × entries on every range, so only the guest's own patched instructions interpret (rule 18). Card read traces byte-identical across the A/B on both images, all 32 entries. **Trap that cost a wedged boot:** a PC inside a declared range must never be a native dispatch key — six pre-existing jal-return continuations were (OpenBIOS `0x357C`, retail `0x4974`/`0x4980`/`0x4984`/`0x4988`/`0x6444`), and dispatching one re-enters the body whose hook sets `pc` back to itself, spinning the dispatch loop at frame 0; the emitter now drops them and refuses to emit if a range covers a function *entry*. **Second title:** Mega Man X6 gets −84.2 % kernel / −84.4 % total on OpenBIOS from a true baseline, mismatch 20 → 0 (and 35 → 0 on retail), which also falsified a harness assumption — its B0 vector goes to zero once the bless verdict is clean, so the A0/B0/C0 vectors are no longer discounted from the headline. Both measurement tools moved into `psxrecomp/tools/` and generalised off `--project-root`. [`kernel-patch-sites.md`](kernel-patch-sites.md) → *The fix and what it bought*. |
| 2026-09-11 | **The Axis B harvest is 3.3–18.8 % complete, not 3.8–31.5 %.** Picking the next target after the kernel fix turned up 11 of the 14 recorded session ids as provable *subsets* of `20260904T093558` — `area_poller.py watch` re-harvests a running session every 15 minutes and each pass was banked under its own id, so they sample one process rather than adding coverage. `pc_coverage.py --merge-duplicates` is the honest read: BOSS 3.3 %, field/map core 4.7 %, PLCHAR 9.0 %, BATTLE+ETC+SCENARIO 11.4 %, WORLD 15.6 %, BATTLE+BMAGIC+ETC 18.8 %, and `0x80117000` Research Plant never sampled; four bands stop being estimable entirely. Axis B has had no genuinely new content since 2026-09-04, which makes one long play session through unseen areas the highest-value action available. Always pass `--merge-duplicates`. |
| 2026-09-11 | **relprof exe refused by Windows; DWARF split into a sidecar.** After the 13:57 regenerate (`generated/` 2.1 GB) the RelWithDebInfo link produced a valid 1.146 GB PE that Windows would not load ("this app can't run on your PC" / WinError 193): mingw ld maps the ~860 MB of `.debug_*` sections into the image, SizeOfImage reached 1.93 GiB. `.bss` (935 MB) is the framework's debug rings and was already there. Proof: `objcopy --strip-debug` on a copy gives a 284 MB exe that boots headless. Fix: `CMakeLists.txt` gained `PSX_SPLIT_DEBUG` (default ON for Debug/RelWithDebInfo), a POST_BUILD `objcopy --only-keep-debug` + `--strip-debug --add-gnu-debuglink` that leaves `<exe>.debug` beside the exe; gdb and `addr2line -e <exe>` follow the debuglink (verified: an RVA resolves to `memory.c:212`). Cost ~8 s per link. The step runs after the framework's staging and before its mod-catalog check. **Also today:** the SCPH1001 dump staged for the BIOS A/B is **v2.0 (05/07/95 A, CRC 55847D8C)**; the framework's profile, seeds and runtime identity check (`main.cpp` `kScph1001Crc`) target **v2.2 (12/04/95 A, CRC 37157331)**, so the retail boot fail-fasts at `0xBFC0192C` (seeds land on the wrong code). Needs a v2.2 dump. Kernel-patch capture on OpenBIOS: [`tools/kernel_patch_diff.py`](../tools/kernel_patch_diff.py), results in `analysis/kernel_patch_diff.json` (three Psy-Q patch sites unbless the exception handler; 44.6 % of all interpreted instructions). |
| 2026-09-11 | **Kernel share of interpreted work root-caused: the BIOS exception handler, unblessed by the Psy-Q kernel patches on both BIOSes.** 44.6 % of all interpreted instructions across every session are kernel RAM, 1.22 of 1.48 billion in OpenBIOS's `exceptionHandler`; it is compiled but the kernel-bless byte check fails because `_patch_gte`/`_patch_card`/`_patch_pad` rewrite it at boot. New `tools/kernel_patch_diff.py` boots headless and diffs kernel RAM against the ROM copy: OpenBIOS has 3 patch sites (handler prologue shift + `mfc0 Cause`; a `jalr` stub into the card fast-track installed on first card access; the card handler replaced by a `jr` to boot-EXE 0x8017EAA0), retail SCPH-1001 has 4 — and its stub lands at **0xCC0**, not the profile's 0xCF0 slot, and the bless memcmp covers slot words anyway, so retail interprets the handler too. Retail needed its backend generated from the user's dump (`regen_bios.sh`, then a reconfigure) and three discovery-gap rounds of seeding to reach frame 341 — **which turned out to be a revision mismatch, not a seed gap** (other session, same day): the staged dump is SCPH-1001 v2.0 (CRC `55847D8C`) and the framework profile/seeds/disasm are v2.2 (CRC `37157331`); the empty `[program.image] sha256` let it generate silently. **Rerun on the v2.2 image the same afternoon (user staged it): clean boot, frame 9534 at t+30 s, 6 patch sites in 5 bodies, 75 mismatched bodies — and the card stub lands exactly in the profile's 0xCF0 slot yet the exception handler body still fails the bless memcmp, which is the proof that the install-slot hook and the bless verifier do not compose.** The other session's `PSX_SPLIT_DEBUG` post-link split landed in `CMakeLists.txt` in the same window (exe 289 MB + 945 MB `.debug`); the hand-stripped copy is redundant. **Side finding:** the 14:06 relprof link stopped loading ("this app can't run on your PC"): DWARF sections pushed SizeOfImage to 1.93 GiB; `objcopy --strip-debug` copy boots; a post-link debug split is the fix. Write-up and the five upstream asks: [`kernel-patch-sites.md`](kernel-patch-sites.md). |
| 2026-09-12 | **True furigana: readings above the kanji, built and on screen.** Four headless tests on a field state facing an NPC (file `slot02`, AREA014, saved by the user) reopened the rendering route: the quad blitter advances by 12 + P (a `<0d>…<0e>` run under `<0f><13>` is 6 px kana at 6 px pitch), the 14 px newline never tracks size, and a `gpr` write from a function-entry plugin moves the text on screen. The user's cut replaced the per-glyph layout table: a ruby row is its own row in the string, so x aligns by glyph count (half a text cell per 6 px glyph, `0x09` bytes as half-cell gaps that the renderer ignores and the stepper does not count — the font has no empty cell and `0xFF` draws junk when shrunk), and the plugin only sets y per row (offsets 8, 1, 29, 22 from the origin: two text/ruby pairs in the 42 px box). `src/bof3_localize.c` hooks the renderer `0x80150598` and both blitters (`0x8014F6BC`, `0x80151F4C` — the shrunk glyphs go through the latter, a1 = the string pointer) and applies the rule only to a page that starts with `<0f><13>`, the builder's signature. `tools/build_ruby_script.py --furigana` builds `jp_furigana` / `jp_furigana_all` (4,542 / 5,699 entries; authored breaks kept, third rows split to a new page, pages with their own span or preset kept verbatim behind a reset — the emphasis census, `tools/emphasis_census.py`, found 26 such pages with kanji, decision: keep the shout); both selftests 0 mismatches, which also fixed 87 pre-existing mismatches from the insert budget wrapping rows with re-flow off. Verified on the AREA014 scarecrow talk, four pages, from the table (`analysis/xlate_shots/furigana_area014_pages.png`); `jp_ruby` unchanged. **Traps:** the probe's Circle must land right after the load (the idle animation walks the party off the NPC while the harness settles); the plugin's pointer gate must accept the ring at `0x9F000000` where a redirected message lives; the Bash tool's heredocs eat one level of backslash (use the Edit tool for C strings). **Then, the same night: readings from the game's own 8 x 8 kana font.** 6 px point-sampled kana were illegible even at 4x; the atlas has no spare cells (1 empty of 882), but its single-byte page carries a complete 8 x 8 kana set (rows 168-215, `names/font_small.toml`). The plugin hooks the quad blitter's packet commit `0x8014E494` (sixth hook entry, filtered by return address) and re-points a reading glyph's `POLY_FT4` at the small cell: same page, same CLUT, 8 px square. Rows reordered ruby-first so the next-page arrow (which sits off the last row -- the user's stray glyph) lands under text; offsets -2 / 6 / 19 / 27 per the user's 2 px lift. Verified on AREA014 (`analysis/xlate_shots/furigana_area014_8px.png`). Three fixes from the user's play frame: the small font's rows start at 169 and an 8 px quad needs UV extent 8 (extent 7 loses the last texel row — 2 px off every reading's foot); the next-page arrow is a sprite through the hooked blitter, so both hooks now filter on the renderer's return address; and the arrow's y is cursor + 14 + P, so the page's last glyph leaves the cursor at row y − P. **Then retired the inline variants:** `jp_ruby`, `jp_ruby_all` and the first-occurrence furigana scope are out of `game.toml` and the plugin (user's call — furigana rows cost no width, so first-time-only saved nothing, and brackets lost to readings above the line); one shipped reading language, `jp_furigana`, every word (5,699 entries), and it is the builder's default. Left: the reading review pass, and the readings dropped after runtime inserts. [`FURIGANA.md`](FURIGANA.md). |
| 2026-09-11 | **Inserted-name readings verified in play (user).** A real item pickup and a master's skill line both drew with readings under *Japanese (Ruby)* on `build-relprof` — record 1 (skill name, filled by the area script through a pointer the static scan could not see) is rewritten in time as well, so the record-1 fallback in [`INSERT_RUBY.md`](INSERT_RUBY.md) (moving the write-back to the stepper's first step) is not needed. Closes the last open item of the insert work; the Ruby track's remaining work is the reading review pass. |
| 2026-09-11 | **Third box-pointer writer found; master talk now gets ruby (user-verified live).** Mygas's apprenticeship line (AREA061 slot 2, the coloured 攻撃力 / 防御力 page) drew plain JP although its hash was in every table. Live RAM showed the box base at `0x80010371`, inside the JP block, i.e. the plugin's redirect had been thrown away. An immediate-form `sw` scan of the boot EXE for the two globals found the writer the 2026-09-05 trace never saw: **`MsgBox_Replay` `0x801515F8`** re-derives `0x80010000 + u16[table + 2 * 0x801490A4]`, stores it into both globals and parks the stepper in state 1 (delay 8, or 16 from `MsgBox_ReplayIfShown` `0x80151554`, sub-state 5 of the `0x14` choice variant `c >> 4 = 1` — state 4 — which the master prompt uses; a second caller sits in the page-break state) with **no `MsgBox_Reset`**. Fix: the plugin's redirect is one shared function and a second hook on `MsgBox_DelayState` `0x80150F3C` (every replay lands in state 1) redirects again when base == cursor inside the JP block, once per distinct base; `game.toml` lists both addresses. Census: 81 of 7,014 messages use that choice variant (~30 areas), 68 a third variant not traced; the hook is variant-agnostic. Regenerate touched one base-EXE shard, the overlay layer and savestate codegen hash are unchanged. **Trap:** the first relink silently lacked the second registration (a scripted edit did not apply and nothing asserted it) — the plugin's boot line `registered at MsgBox_Reset + MsgBox_DelayState` is the check. Headless: Circle never registers on the choice box (state stays 4), so the proof is the user's live run on `build-relprof` from in-game slot 3 (file `slot02`, saved on the choice). Docs corrected: TEXT_ENGINE.md resolver, LOCALIZATION_APPLY.md "The second door", HANDOFF row 7. |
| 2026-09-10 | **Readings on inserted names — built and exercised live.** Item / skill names never sit in a message: the box carries `<07><nn>` and `MsgBox_Step` draws a 32-byte scratch record at `0x801490D4 + 0x20·nn` (the decompile's `0x801490D3` is the loop's pre-increment form — the character insert has the same `-1`). The design's two open checks were answered from the GAME.EMI disassembly instead of a live print: every record-0 writer (`Field_SearchSpot`'s item branch at `0x801B4094` via the new boot symbol `Item_NamePtr` `0x80166720`, a twin at `0x801B3870`, `Field_GiveZenny`, two more sprintf sites, the key-item memcpy, BATL_END) fills the record and *then* opens the message, and the copy starts at `+0` — 8 name bytes plus a NUL at `+8`. Built: `tools/build_ruby_script.py` emits `generated/bof3_insert_<code>.c` next to each Ruby table (109 names: 75 item + 35 ability, `--insert-review`), `src/bof3_localize.c` collects the `<07><nn>` indices while it walks the message and rewrites each record before the lookup (its first guest-RAM write; scratch only; idempotent), CMake defines `BOF3_INSERT_HAVE_<CODE>`, and the insert budget is an `Annotator` parameter (16 for the Ruby scopes, 8 stays for English). Live check over the debug port on savestate slot 0: pointed every area message at a copy of the pickup line, pre-filled record 0 with 薬草, pressed Circle — `insert hit #1 rec=0 at 801490D4 (4 -> 10 bytes)` and the box drew 薬草（やくそう） on its own row, then を手（て）に入（い）れた (`analysis/xlate_shots/insert_pickup_synthetic.png`). **Two traps:** the narration check measured inserts at the 16-cell budget and misfiled every insert-bearing page as verbatim narration (276 → 170 pages once it measures the shipped widths — the pickup line itself had no readings on the first run); and prefixing `C:\msys64\mingw64\bin` to PATH for the build makes `python` resolve to MSYS's interpreter (no SudachiPy), so a regenerate silently fails — generate with the Anaconda `python` first, then prepend the toolchain. Readings fixed on the way: 光合成, さかなのお頭, とっこう薬, 気つけ薬 (`names/readings.toml`). **Second kanji-table correction (user, on Rei's line):** cell `0x1354` was transcribed as 冒, so 探さないと read おかさないと; all 184 uses are 探 (探す / 人探し / 探し物 / 探険) — `names/kanji.toml` corrected, both tables regenerated. Then built the check that should have existed: `tools/font_sheet.py kanji` renders the kanji sheet cell by cell beside the table's kanji in a real font (`analysis/font/kanji_proof.png`); all 441 cells eyeballed and declared clean — **wrongly**: the user then caught 春 read いずみ on the next screen; cell `0x12F9` was 泉, the glyph is 春 (春のマクニール, 春の牧場, 春が来た — and `text_tables.py` had been folding 泉 as a *season* word next to 秋 all along, which should have been the tell). Fixed, `names/places.toml` captions re-extracted, tables regenerated. The proof page makes a slip findable; it does not make a fast pass reliable — check it cell by cell at full zoom. Second pass at full zoom over all 441 cells: `0x12F9` was the only remaining mismatch. Three slips so far, and each is a *translation* homonym — 春/泉 both "spring", 探険/冒険 both "explore/adventure", 代/賃 both "fee/charge" — so the prior table's first pass matched cells against the English corpus, not the glyph sheet (user's reading, fits all three). Also fixed: `tools/text_tables.py` was still loading the prior work's D:\BoFIII JSON for kanji, so `names/places.toml` kept 泉 after the TOML fix and the season fold broke; it now reads `names/kanji.toml` through `jptext` (captions re-extracted: 春のマクニール, 春の牧場; bonus 火星ダコ decodes). **Fourth slip, same evening:** `0x1234` was 奥, the glyph is 下 (下さい, 地下, 城下, 下手 — 311 uses); user caught 下 read おく in the MacNeil confrontation. This one was *on the proof page and noticed* ("1234 looks like 下") and waved through as 12 px noise — the page works, the reader has to trust it. Then `0x1292` 凶→邪 (邪魔, 邪道, 邪悪; 94 uses), spotted by the user on the proof page. Five slips, all EN homonyms. A relative pixel matcher (each cell against every table kanji in MS Gothic's 12 px strike) was tried and is useless — dense glyphs like 魔 win every comparison. Evening, user's second look at the proof page: `0x12E9` 究→験 and `0x1342` 飛→忍, and 験 forced `0x136B` 研→実 (実験プラント, 実は ×200, 木の実, 真実). **Eight** slips, all EN homonyms. A third automated attempt, `tools/kanji_subst_audit.py` (substitute 2,600 candidate kanji into each cell's script contexts and score the tokenization), also found nothing: its 80 suspects are proper nouns and game words, and every mid-list context reads correctly. Three tools tried, zero slips found by tooling, eight found by reading — the proofreading pass is a human job and the proof page is its aid. All corrected, tables regenerated, sidecars re-extracted, `build-relprof` relinked. [`INSERT_RUBY.md`](INSERT_RUBY.md). Left: a real pickup and a master's skill line (record 1) seen in play. |
| 2026-09-10 | **Framework pins bumped to the renamed org; `psxrecomp` back on plain upstream `master`.** Both upstreams moved to `RetroPortingToolKit` on 2026-09-09; `.gitmodules` follows (note `git submodule sync` rewrites each submodule's `origin` from it, so the fork stays reachable under a second remote). `psxrecomp` `2fa3472a` → `ed55299b` (68 commits): the 2026-09-06 blocker is retired because mstan's `merge/frameblend-localization` — sole home of the six `RecompLauncherCNetplayLaunch` netplay fields — is now merged into recomp-ui master (`13d7d69`), so upstream psxrecomp builds against a published recomp-ui for the first time. Our `fix/gpu-polyline-terminator` ([#313](https://github.com/RetroPortingToolKit/psxrecomp/pull/313)) also merged, leaving no fork-only work. `recomp-ui` `db12620` → `26756c2`: PR [#48](https://github.com/RetroPortingToolKit/recomp-ui/pull/48) refreshed by **merging** master in (not rebasing, to keep review history), 53 commits behind — netplay lobby/chat, frame blending, per-GUID pad binds, display aspect row, HiDPI. Three conflicts, resolved upstream-first: both capability defines kept in `recomp_launcher.h`; the bind grid takes master's pad-source/keyboard-source split over our KEY+GAMEPAD pairing, with `db12620`'s `!capture_assist` guard re-applied to both capture predicates; the capture handlers keep our Backspace-clears path. `ctest` identical (12/14) on the merged branch and on pristine master `8bf4738` — both failures reproduce upstream. **Trap: the bump forces a regenerate.** The new `dirty_ram_interp.c` calls `psx_overlay_static_can_dispatch`, which `compile_overlays.py` only began emitting after the old pin, so a stale `generated/` fails at link with an undefined reference — the first symptom, and easy to misread as a codegen bug. Base-EXE codegen was byte-identical (0 of 35 shards updated, dispatch unchanged); only the overlay layer moves, and `axis_b_loop.sh --skip-harvest` redoes it. Verified end to end: `build-relprof` links a 1.11 GB exe that boots to an OpenGL 3.3 context at 60 Hz with all three localization tables registered. **Also now actionable:** `setx PSX_STARVATION_TIMEOUT_US ""` puts the watchdog back (#321 is in this pin), and the freeze detector no longer fires during host pause loops (#339), retiring the `slow_frames`/`hard_freeze` false pairs. Framework license unchanged (PolyForm Noncommercial 1.0.0). |
| 2026-09-10 | **Kanji table in-tree, one cell corrected.** The unread-token audit (9 tokens, 77 uses, all out-of-vocabulary) exposed 先祖賃々: the prior decode table had `0x132C` as 賃, but 代 never decoded and all 54 uses read as 代 (時代, 代わり, 13代目, 身の代金). The table now lives at `names/kanji.toml` (441 codes incl. the six past 0x13AD, corrections as comments) read by the new `tools/jptext.py`; `page_rows`, `ruby_fit`, `build_ruby_script`, `verify_msgtable` no longer import `D:\BoFIII`. Ruby tables regenerated: 時代（じだい）, 先祖代々（だいだい）; unread 45 → 43. Then closed the rest: stretch stripping in the annotator (気（き）ィ, 設備（せつび）ーい), sidecar entries for 闘場 / 闘都 / 兄者, and an OOV sweep of the whole script (no other misread cell; it did catch the numeral handler reading 一回戦 as いち + かいせん, now two `next` rules on 一). Unread words 45 → **0**. **Later (user screenshot):** a skill-learned line ran 19 cells because runtime inserts (0x03/0x04/0x07/0x08) counted as zero width in the Ruby layout; now budgeted like the English encoder (`INSERT_WIDTH` 5/5/8/11), the line re-flows to its authored rows. **And** 何かないかな？ (examine furniture) had no reading because it is not area dialogue: it is slot 0 of the system message block, `BIN/ETC/AFLDKWA.EMI` dest `0x80014000` (8-byte header + the area-script offset table, 309 slots, mostly menu/shop/memcard text). An overlay scan shows `Msg_OpenSystem` → `MsgBox_Reset` (64 sites) is the only door from that block to the box, while menus read it via `Msg_SystemPtr` (1,879 sites) and never pass the hook, so the **whole block** is now a second source for both Ruby tables and the hook filters at runtime (+57 / +101 entries; selftest 7,014 messages, 0 mismatches). Trap: the plugin's pointer gate ended at 0x80014000, so system-block messages were skipped before the lookup; widened to 0x80017628 in `src/bof3_localize.c`. **Left for next session:** readings on *inserted* names (item / skill via `<07>`, a scratch record the stepper draws) — design in [`INSERT_RUBY.md`](INSERT_RUBY.md). |
| 2026-09-10 | **Ruby readings get an override sidecar and an audit list.** User noticed 私 read ワタクシ. Measured: SudachiDict `core` vs `full` over all 43,075 dialogue runs differ in 0 readings and 270 segmentations (full merges compounds), so `full` is now the every-word table's dictionary (武器屋（ぶきや）, 機械文明) and `core` stays for first-per-area. New: `names/readings.toml` (surface or verb dictionary-form key; seeded 私 = わたし, 言う = いう covering 言っ/言わ/言え, and a phrase rule `next = [...]` keyed on the following token: 何 = なに before を/が/も/か/particles/punctuation, なん otherwise — a ten-message pilot found 何を the only Sudachi miss in ~30 ambiguous occurrences; and `prev = [...]`, a suffix match on the text before the word: 方 = かた after 旅の / 若い, the person sense the lexicon reads as ほう after の), `--ambiguous FILE` (540 surfaces with more than one lexicon reading, by frequency, with candidates and what printed), whole-token reading placement for kana-interrupted compounds (最後の夜（さいごのよる）), and a trim fix for katakana on the page (方向キー was ほうこうき). Then (user request, after seeing つぎは split across rows) the re-flow moved from glyph units to **phrase units**: every token is a unit and particles / auxiliaries / suffixes attach to the word before; page splits 399 → 554 and 1,097 → 1,471. Selftest still 0 mismatches; both tables regenerated (5,835 / 6,042 entries). [`FURIGANA.md`](FURIGANA.md). |
| 2026-09-10 | **Japanese (Ruby) user-verified in real play.** Launcher Localization dropdown → *Japanese (Ruby)* on `build-relprof`, opening of the game replayed with readings on screen; user's verdict: works like a charm. First on-screen check of the Ruby table beyond the single headless AREA150 prompt. Readings still unproofread (FURIGANA open item 2). |
| 2026-09-09 | **`jp_ruby_all`: readings on every kanji word** (user request), the third table through the same plugin: `tools/build_ruby_script.py --scope every`, launcher label *Japanese (Ruby, every word)*. 6,063 messages change, 21,596 readings, 1,096 pages split for a fourth row (400 with first-per-area), 366 KB. The runtime budget is the plugin's 2 KiB per-message slot, not the 16 KiB window: longest message 702 B, so nothing hits the cap and no area needs re-evaluating. Verified headless on the AREA150 prompt, every word read. |
| 2026-09-09 | **Japanese (Ruby) is a selectable script, on screen.** The plugin became multi-table (`generated/bof3_xlate_<code>.c` globbed by CMake, `BOF3_XLATE_HAVE_<CODE>` on the plugin source only, the resolved language string picks the table) and `tools/build_ruby_script.py` produced `jp_ruby` from the JP disc: every glyph keeps its bytes, readings (SudachiPy, stem-placed `起（お）きる`, first occurrence per area) are inserted as kana glyphs between `0x28`/`0x29`, box pages re-flow to 16 cells and split over three rows with `0x02`, narration pages (wider or taller than the box, 101) pass through verbatim, and `--selftest` round-trips all 6,768 distinct messages byte for byte. 5,926 messages change, 11,070 readings, 314 KB. Verified headless with `PSX_LANG=jp_ruby` on the AREA150 prompt: `hit #1 (31 -> 34 bytes)`, 夜を待（ま）って‥砂漠に出ますか？ inside the frame. `game.toml` lists `{ code = "jp_ruby", label = "Japanese (Ruby)" }`. Open: the readings are not proofread. [`FURIGANA.md`](FURIGANA.md), [`LOCALIZATION_APPLY.md`](LOCALIZATION_APPLY.md). |
| 2026-09-09 | **English dialogue renders in the JP build: the apply path exists.** The framework's trusted-plugin hook (`[recompiler].mod_function_entry_funcs = ["0x8015042C"]`, one regenerated shard, 5 s) calls `src/bof3_localize.c` at `MsgBox_Reset` entry; the plugin walks the JP message at `0x801490AC` (control-aware: `0x14` is `14 a b c` + `c&0xF` NUL-terminated options, so `find(0)` truncates every choice), FNV-1a64-hashes it, looks it up in `generated/bof3_xlate_en.c` and repoints `0x801490A8/AC` at a copy in enhancement memory (`0x9F000000`, `psx_mod_alloc_guest_memory`); no game RAM is written and misses leave JP. The table is the **US disc slot for slot**: all 200 AREA files have the same 256-slot table and the same control-code set (0 mismatches), so `tools/build_script_xlate.py` re-encodes the US bytes (its font order: `.`=3E `,`=3C `'`=8E `"`=90 `?`=5C `!`=5D) into the JP glyph codes, **capitals** because the JP sheet has no lowercase and no free cells, re-wraps to the box (measured on screen: **192 px interior = 16 cells**, one more than the script's 15; a first cut at 19 came from misreading the window record's 12.4 value and overran the frame, caught by the user on the screenshot) and splits over-three-row pages with `0x02`: 6,721 entries, 470 KB, 561 US slots untranslated. **Verified headless on slot 4 (= AREA150, not MacNeil)**: `hit #1 ptr=800108C6 -> 9F000000`, the screen shows `DO YOU WANT TO WAIT / FOR NIGHT BEFORE / GOING INTO THE` then `DESERT?` + `YES / NO`, and the same presses give the same stepper states as the JP run (Cross resolves that prompt, Circle does not, in both). The table is generated, never committed (Capcom's script from the player's own US dump); CMake builds the plugin only when it exists. Traps: the config-hash change does **not** require an overlay recompile (the static C is content-identical; the loop's mtime check was wrong and is fixed); a target-wide include dir recompiles every overlay object; the harness's stdout is fully buffered. [`LOCALIZATION_APPLY.md`](LOCALIZATION_APPLY.md). |
| 2026-09-09 | **The ruby script variant is unblocked on the data side; what is left is the encoder and, separately, delivery.** Byte budget measured, since a re-authored block has to fit the RAM the game gives it: the area block loads at `0x80010000` with the system pool at `0x80014000`, a **16 KiB window**. Annotating first occurrence per area costs **+13.8% over all 200 blocks** (562,543 -> 640,304 bytes, median +13.3%), and the worst block goes 13,008 -> 14,737 = **89.9% of the window**. Nothing exceeds 16 KiB, nothing approaches the `u16` offset table's 65,535 limit. So the next session can write the encoder: glyphs from [`names/font.toml`](../names/font.toml), the control stream carried through untouched, `0x01`/`0x02` re-authored, the offset table rebuilt -- with a **round-trip test available today** (encode the unmodified script, diff byte for byte against the disc). **Three things stay open and none of them blocks that work.** (1) **Delivery is unbuilt** -- the `MsgBox_Reset` repoint is a design, no code substitutes message bytes for this title yet, and the framework's generic hook does not fit here (F-3/F-4); this gates a *playable* build and is shared with the English track. (2) The **15-glyph box width is an assumption**, the widest line the shipped script uses rather than a measured clip boundary; settle it at `Window_DrawFrame` `0x8015A58C` or on screen once delivery exists, and every overflow figure improves if it is really 16-18. (3) **Readings are costed, not proofread** (`言う` -> `ゆ`; mode C splits some compounds), so the reviewed reading sidecar comes before shipping. [`FURIGANA.md`](FURIGANA.md). |
| 2026-09-09 | **The single-byte half of the text encoding is read, off the disc — the encoder is no longer blocked.** `BIN/ETC/ENDKANJI.EMI` section 0 de-interleaves (even 2 KiB chunks = left half, odd = right) into the same 21-cell x 12 px atlas the box mapper walks, and the mapper's own arithmetic indexes it: **byte < 0x5B -> cell = byte** (`sltiu 0x5B` at `0x80152740`), **byte >= 0x5B -> cell = byte + 0x23** (`addiu 0x23` at `0x801529B8`), **`0x15 nn` -> cell = nn + 0x5B**. The two rules meet exactly where they must -- `0x5A` = `Z` is the last cell before the symbol block and `0x5B` = `あ` lands on row 6 column 0, which is where the sheet puts it. Result: `0x41`-`0x5A` = `A`-`Z` and `0x30`-`0x39` = digits (so `<48><50>` really is `HP`), but **`0x3E`/`0x3F`/`0x40` are `‥`/`？`/`！`, not ASCII `>?@`** -- which **settles the doc's open Latin/digit question** with a split answer. Also named: `0x28`/`0x29` `（）`, `0x2A`/`0x2B` `「」`, `0x3B`/`0x3C` `『』`, `0x2C`-`0x2F` `,` `ー` `.` `/`, `0x3A` `・`, `0x3D` `=`, `0xFD`/`0xFE` `。`/`、`, `0xFF` the word separator, the `0x15` symbol page (`～` alone is 387 of its 487 uses), and five kanji past `0x13AD` pinned by context (問 志 星 愛 管). **Coverage over the 200 area scripts: 36,064 of 204,356 glyph cells unmapped (17.65%) -> 0 (100.00%)**; the script now decodes end to end. The last nine codes explain the sheet: **six are kanji on the symbol page** (`0x15` 0x0E 護, 0x10 放, 0x18 早, 0x1D 消, 0x1E 新, 0x1F 業), outside the `0x12xx`/`0x13xx` space entirely, because the kanji sheet is **exactly full -- 21 x 21 = 441 slots for exactly 441 codes (`0x1200`..`0x13B8`), zero empty cells**. Not a frequency cut either: 8 kanji that never appear in area dialogue hold slots (battle/menu words) while 新 at 25 uses sits on the symbol page, so these read as late additions to a frozen sheet. The other three are the zenny mark, `＆`, and `0x13B5` 可 -- and six of the nine are the "eight missing codes" the prior decode work had proposed hunting in the non-dialogue files. `tools/font_sheet.py` (render / table / show) -> `names/font.toml`. For the ruby variant this closes the encoder gap and picks the bracket: `（）` is unused elsewhere, while `0x2A` and `0x3B` are the hanging-punctuation cases and would hang into the margin at line start. [`TEXT_ENGINE.md`](TEXT_ENGINE.md), [`FURIGANA.md`](FURIGANA.md). |
| 2026-09-09 | **Inline furigana costed against the whole script, and it fits — as a re-authored third variant, not as rendered ruby.** Decision (user): ship *Japanese (Ruby)* beside English and Japanese, as data — `漢字（かんじ）` inline — accepting that re-authored line and page breaks will re-cut some intended screens. Box line capacity measured from shipped content: **15 glyphs**, over 8,323 box lines (the 16-28 glyph lines are all narration or choice menus, where the option list joins the prompt); it is an observed maximum, not a measured clip boundary. Cost over all **3,793 distinct confirm pages** with real SudachiPy readings (15,610 words, **2.41 kana each**, 42 unreadable, ~4.4 glyphs per annotation): annotating every word and keeping the authored breaks puts **31.6%** of pages over three rows and pushes **320 of 2,100 two-row pages (15.2%)** to a fourth; re-flowing the page instead drops that to **9.0%** and **47 (2.2%)**; annotating only a word's **first occurrence per area** (5,488 repeats suppressed) reaches **4.3%** and **19 pages (0.9%)**. The finding that matters: **the third row is not spare capacity** — it is already used on 1,215 pages and 72% of those overflow. Overflow is spent as a **page break rather than a fourth row** (`0x02` is one byte, nothing counts pages), so four-row pages stay untested. Delivery is a third `[localization].languages` entry (config; the launcher dropdown already reads that list) riding the same `MsgBox_Reset` repoint the translation track chose. **The compiler is not in this path** -- none of the 406 compiled overlays loads at `0x80010000` (184 `AREA*.EMI` files contribute overlays, but their *code* sections at the band bases); the script block arrives by CD DMA and never goes through codegen, so a re-authored script answers to the box stepper, not a build step. What is left is an **encoder**, and its gap is narrower than the 14.35% of confirm-page glyphs missing from the character table suggests: it needs the inverse map only for what it *introduces* (kana, known, plus one bracket pair -- `0x3B`/`0x3C` is the lead), copying everything else through as bytes. The unknowns cost segmentation quality near punctuation, not correctness, and the row measurements are unaffected because an unmapped glyph still occupies one 12 px cell. Cheap close: the atlas ordinal is the atlas index, so codes below `0x5B` are the punctuation/digit/Latin rows of the font sheet already rendered in the prior decode work. `tools/ruby_fit.py` (width / cost / sample). [`FURIGANA.md`](FURIGANA.md). |
| 2026-09-09 | **The box has two glyph blitters, and one of them scales — font sizing is a shipped feature, not new work.** The renderer picks on bit 3 of `0x801490A0` (`0x801507EC`): the default path builds a Psy-Q **`SPRT`** (`w` at `+16` a literal `0x0C`, `h` at `+18` cropped by the pair table at `0x8017FEF8`, the appear wipe), which cannot scale because PSX rectangles sample texels 1:1; the effect path `0x80151F4C` builds a **`POLY_FT4`** whose vertex X is `cursor + 0x0C + P` (`0x80152C5C`) against a fixed 11-12 texel UV extent — true scaling from the same 12 px atlas. `P` is `0x801490C4`, the param of the `0x0F` preset table at `0x8017FF30` (26 entries of `{type, param, u16 duration}`), and the shared type-2/3 handler `0x80151BC0` is what *sets* bit 3, so asking for a size change is what switches blitters. Presets are +6/+11/+24 and -3/-6/-9 px over 30 f / 60 f / forever; **106 uses in the 200-area script**, every one following a `0x0D`..`0x0E` span — which retracts this doc's *enable/disable drawing* reading of that pair: it is the emphasis span the effect applies to (AREA000 msg 42 `<0d>えらいっ<40><0e><0f><0a>`, grow +11 forever). Shrink ships twice (AREA008, -3 forever). Also settled: **`0x16 <frames>` is a second page break** (timed auto-advance, 670 uses), and the rows-per-page census over all 200 blocks says **three rows is ordinary** — 1,215 confirm-advance pages across 122 areas — while **no confirm page ever prints four**; the 4-9 row pages are all narration ending at `0x16` or the terminator (AREA144/AREA055 interleave blank rows). For furigana this moves the blocker: the primitive and a smaller glyph already exist, what is missing is a sub-12 px **advance** (`addiu 0xC` at `0x801508A0`, shared by both paths, single immediate), per-row metrics rather than one global `P`, and a better source than point-sampling a 12 px cell. `tools/page_rows.py` (rows / codes / styles, disc or `--bin-root`, style table read from the EXE). [`TEXT_ENGINE.md`](TEXT_ENGINE.md). |
| 2026-09-08 | **The afternoon's "crashes" were two different things, and the frequent one is not a fault.** Five freeze events (each a `slow_frames` dump then a `hard_freeze` dump ~2 s later, ~110 MB apiece, **918 MB total**). The last two -- 15:27 frame 4872 and 13:49 frame 10749 -- symbolise, against the RelWithDebInfo build, to `main -> psx_scheduler_run -> ... -> sdl_vblank_present -> `**`savestate_menu_host_pause_loop`**` (main.cpp:6380) -> SDL_Delay -> NtWaitForSingleObject`; the 13:49 one is the same loop one frame deeper, in `gl_renderer_present_hold_last -> gl_swap_with_osd -> gl_draw_osd_image -> glTexImage2D -> nvoglv64`, i.e. the paused menu uploading its own OSD texture (the NVIDIA frames are a red herring). The loop's own comment says *"Freeze guest in vblank present while the save-state slot menu is open"*: the guest is stopped **on purpose**, so the classifier's `frame_delta == 0 -> wedge_kind 1` is guaranteed, not probabilistic. Corroborated by `psx_cycle_count` being **byte-identical across every tick of the heartbeat ring** (a wedged guest burns cycles; a paused one does not) and by `in_exception 0` / `sio_card_active 0` / a `last_store_pc` (`0x801751F4`) that also appears in a *clean* `atexit` report from the same session. Three loops hold the guest this way (`savestate_menu_host_pause_loop`, `rewind_host_pause_loop`, the netplay admit barrier) and all feed only `starvation_watchdog_heartbeat()`, never the wedge detector -- filed upstream as **[mstan/psxrecomp#338](https://github.com/mstan/psxrecomp/issues/338)**, sibling of #321, with two suggested fixes (an explicit `freeze_heartbeat_set_paused`, or requiring cycle progress as well as frame progress before `wedge_kind 1`). **The real crashes were the two `fatal` events at 13:45:41 and 13:46:27** -- a recurrence of the `0x2934` kernel-RAM dispatch fail-fast, twice in one session and from a *different* call site than 2026-09-01 (`ra 0x801B1638` vs `0x801A36BC`), which retires that doc's "single observation, not reproducible" header; `a1 = 0x00009088` is a **TCB pointer** matching the companion dump's `current_tcb` at a `syscall3_enter`, a new handle for the next look. None of it is related to the imported US save: the 13:49 event predates the card write and matches 15:27 exactly. Dumps distilled to `analysis/freeze_events_20260908.json` and the 918 MB deleted. [`crash-kernel-ram-2934.md`](crash-kernel-ram-2934.md). |
| 2026-09-08 | **The imported US save loads, and the two "failures" were both faithful: area numbering is identical between JP and US.** First live load of the converted save came up in an unrecognised area with a one-character party -- exactly the predicted failure mode -- and it was neither. Both discs listed and compared (`tools/disc_ls.py`, 890 entries each): **200 `AREA<n>.EMI` on each, no JP-only or US-only number, every one in the same `BIN/WORLD<nn>/` directory**, and per-area size aligned at shift 0 has **max delta 0.12 %** over all 200 (median 0.107 %) while every other alignment is strictly worse (0.32 % at -1, 0.49 % at +1, 1.17 % at +3). Area numbers are filenames on the disc, so `AREA141.EMI` (164,120 B JP vs 164,297 B US -- **+177 bytes of English text in the same room**) is the same place in both builds; the save's area `0x8d` loaded where it was saved, and the user recognised the room. The solo party is equally faithful: the block holds formation 0 *and* 1 = `[7, 255, 255]`, the slot summary agrees independently, and the sprite on screen is char_id 7 (adult Ryu, blue spiky hair / olive tunic / pack and sword) -- the rest of the roster is intact in the records at Lv 37/22/16/37/21/23, just not in the active formation. Stable once loaded. **One real finding left open:** the game came up with **US confirm/cancel**, so BoF3 persists its OPTION config inside the save block and the JP build honoured the US value -- faithful, but the option bytes are not located, and a byte-diff cannot find them because the US save differs from every JP save on hand almost everywhere by being 42 h further along. Experiment queued: change the button setting in OPTION, save to a fresh slot, diff against the parent block; then `save_import.py` grows `--jp-options`. [`SAVE_IMPORT.md`](SAVE_IMPORT.md). |
| 2026-09-08 | **US saves load onto a JP card: the two builds write the same save block.** Measured, not assumed, against `saves/Breath of Fire III (USA).1.mcr` (42 h 37 m, Lv 38): the US block's u16 at file `+0x270` is the byte-sum of the same 0x10B0-byte block with that u16 excluded -- the **JP rule at the JP offset**, matching exactly (`0xafc1`), and a sweep of every 2-byte offset in `0..0x120` x every length in `0x1000..0x1200` finds no other offset that works. `save_tool.py verify` then joins the US save against the **JP `.EMI` tables** in `names/`: 43 of 46 checks pass, including all four inventory categories' ids, all eight ability lists' types, and 7/8 ATK + 8/8 DEF equations resolving to named JP items -- so **item, ability and equipment ids are shared between regions**. The three failures are not region drift: the title frame is English (cosmetic; the load screen reads the slot summary), `names/` holds only 16 key items against the save's 29 (every id <= 15 resolves in story order -- our table is short, not the save), and roster 5's ATK is +5 over base+weapon with **no accessory equipped**, which also retracts the tool's accessory excuse for roster 4 -- an unmodelled term in `save_tool`'s ATK equation (Master bonuses the likely candidate), invisible until a save this late existed. Conversion is directory-frame-only: filename `BASLUS-00422BOF3NN` -> `BISLPS-00990BOF3NN` (`names/data.toml` `SaveFile_NameTemplate`) with the frame and header XORs recomputed; the 8 KiB block is copied verbatim, so its checksum stays valid. `tools/save_import.py` (list/import, `--slot/--into/--as/--force`, refuses an unverifiable source, backs up the destination); the US save is now on `saves/card2.mcd`. **Still unproven: it has not been loaded by the JP game.** The residual risk is the area number (US save sits at area `0x8d`) and the flag array -- a mismatch loads into the wrong place rather than failing loudly. [`SAVE_IMPORT.md`](SAVE_IMPORT.md). |
| 2026-09-08 | **Retraction: the 23 "real-code gaps" in the data-island memo were a classifier artifact; confirmed gaps are 0.** All 29 pairs (the row had grown from 23) were audited against the capture bytes. Three causes, none a coverage loss: **24** were zero fill or pointer words scored as code because `_looks_like_code` credited `w == 0` as a nop hit, so a zero-filled window scored 100 %; **2** were record tables whose words decode as accidental control flow (SHOP `0x801E5710`, 3-byte records whose first word is a `jal` landing in RAM; BATE `0x801D8CD0`, packed halfwords reading as a column of `beq`); **1** was a misattributed memo line — SCENA13 `0x801F7568` holds an ordinary function (`addiu sp,-0x20`, `jr ra` at +136, zero undecodable words in 4,700) while SCENA17 at the same address holds the **staff roll** and refuses at +12, and the line reporting 51 unsupported is SCENA17's. Fixes in [`tools/data_islands.py`](../tools/data_islands.py): zero words excluded from the ratio and a **frame** (`addiu sp,sp,-imm` or `jr ra`) required, so the code row goes 29 → 2; a **walk tiebreaker** (`walk_outcome()`) drops candidates whose walk reaches a return with nothing refused when candidates outnumber memo lines — deliberately *not* a match on the memo's unsupported count, which comes from the compiler's own notion of unsupported and is not reproducible here; and a downstream-piece check, which shows the 2 survivors (MAGIC002 `0x801EEC10`, COMMU00 `0x801EEC9C`) are a data pad in front of a function compiled at its own address. Pairs 4,074 → 4,073 (one fabricated row), mismatch flag clears. The join's "N lines for N candidates" is the **healthy** case, not a fault: candidates are only pieceless occupants, so N-for-N means every occupant that lacked a piece there also failed. [`DATA_ISLANDS.md`](DATA_ISLANDS.md), [`HANDOFF.md`](HANDOFF.md) both corrected in place. |
| 2026-09-08 | **EMI section-type census: the two types the community doc does not name, and the limit of the type id.** Over the JP disc's 6,344 sections — type 0 misc 2,623, type 6 VH 1,020, type 7 VB 1,020, **type 8 904**, type 3 image 593, type 10 SEQ 119, **type 1 65**. **Type 8** is an audio companion record (4–96 B, median 28): **901 of 904 are flanked by a type 6 before and type 7 after, all three sharing `+0x04`**, the other 3 follow a type 10 — and its `+0x04` only ever holds 0..6, so **the TOC's `+0x04` is type-dependent and is a bank id, not a RAM pointer** for these. `BIN/BENEMY/*.EMI` is the trap: all 200 files are exactly `(6, 8, 7)` with bank 6 and no RAM-destined section at all — enemy *sound*, not enemy stats. **Type 1** is 65 large sections (124–332 KB) all destined for `0x80033800` in `BPLCHAR`/`PLCHAR`/`ETC`, with a type-0 in the same file targeting the same address in 44 of 65 — alternate character payloads. Practical conclusion: all 406 compiled overlay sections are **type 0**, so the type id definitively rules assets *out* (art is type 3 and never compiled) but has no power to separate code from data, because every game-logic table — the level/growth table, the ability table, every data island — sits in type 0 beside the code that reads it. [`EMI_TYPES.md`](EMI_TYPES.md). Reporting type 8 / type 1 upstream to the data-doc maintainer is the user's action. |
| 2026-09-08 | **The registry id is now the residency signal, and the loader cell is the load hook — both title-side, no framework change.** `tools/resident.py` reads one u32 per band base (the header registry id, written with the section's first sector by `CD_getsector`) and validates it against the 405 known ids, so the memory-card staging buffer at `0x800C1800` or an unused band never reads as an overlay; with the loader's file-id cell `0x80146464`, `File_LoadDone` state `0x80146490` and the area number `0x80143F00` it prints e.g. `BATTLE(0x011) | GAME(0x01E) | PLP012(0x021) | BATTLE(0x010) | BATL_END(0x142) | AREA056(0x06C) | SCENA03(0x108)  load ENEMY056.EMI  area 56` -- the full resident set where the script-block md5 named only the AREA. `area_poller.py watch` prints it on change, logs a `resident` timeline row, and `harvest_interp_pcs.py` stamps the id set on newly-entered PCs (`resident` key, beside `areas`). `tools/load_watch.py` arms the write trace on `0x80146464` = every `File_LoadRequest` with file id, caller and args: one battle round = BATL_SE, MAGIC070 (Ryu, Rejuvenate), BATL_RET, MAGIC100 (Nina, Typhoon), BATL_RET, COMN_SE. Skipped on purpose: replacing the runtime's `occ_crc` with an id (a framework change for no new information) and the native hook / on-screen readout (IDEAS I7, I8 -- enhancement phase). |
| 2026-09-08 | **Spell→overlay link VERIFIED live.** New pre-fight save (file slot 1, Ryu + Nina), `callstack_diff capture --hold up --press circle x6 --watch 0x80146464 --watch 0x801EEC00`: `File_LoadRequest` wrote file id `0x156` (MAGIC070) at f+336 and `0x170` (MAGIC100) at f+724, both with `ra` inside `Magic_LoadForAbility` `0x800AB120`; the band header word `0x801EEC00` then became `0x16E` and `0x186` -- exactly those files' registry ids read off the disc -- written by `CD_getsector` from `CdReadyCallback` (the section lands from the CD buffer header-first). Nina's Rejuvenate and Typhoon. `Magic_LoadForAbility` verified, the two overlay rows and five ability rows verified (`magic_map.py VERIFIED_FILE_IDS` keeps them across regeneration). [`OVERLAY_HEADERS.md`](OVERLAY_HEADERS.md). |
| 2026-09-08 | **Spell→overlay link closed, read off the disc: the loader takes disc-file ids, and the battle engine maps ability id → file id in two tables.** Boot `File_LoadRequest(file_id)` `0x801629CC` resolves through `File_LBA` = `u32[0x80182DBC + id*4]`, a table of all 887 disc files' LBAs in directory order (`tools/file_ids.py`, 887/887 exact); `File_LoadDone` `0x801636F0` is the spin. Every `jal 0x801629CC` immediate is therefore a filename: the game-mode dispatcher loads `0x262` = GAME.EMI, `0x261` = FIRST.EMI; BATTLE/BATL_END load `u16[0x80143F00] + 0x2AB` = AREA<n>.EMI, so **`0x80143F00` is the current area number**. The engine's `Magic_LoadForAbility` `0x800AB120`: `row = u8[0x800B3450 + ability]`, `file = u16[0x800B3538 + row*8]`, handler pc at `+4`; `Magic_LoadForItem` `0x800AB1FC` reaches the same rows via four u8 sub-tables (`0x800B39F0`). **File id ≠ registry id** (MAGIC001 = file `0x125`, registry `0x144`), which is why the morning's `0x144..0x1BF` searches were empty. Three-way consistent without a trace: files are numbered after ability ids (1→MAGIC001, 3→003, 4→004, 45→045, 133–136→133–136), 222/222 handler pcs lie inside their file's code section, every file id resolves. 222 of 227 abilities load a file (5 engine-side), 147/151 rows used by abilities, 122 distinct sections. `tools/magic_map.py` → `names/magic.toml`, and the 122 BMAGIC rows of `names/overlays.toml` now carry `Battle FX: …` aliases at `evidence` (was filename hypothesis); 3 boot funcs in `symbols.toml`, 2 engine funcs + 3 tables in `names/functions.toml` / `names/data.toml`. Trap: the extractor dedups identical sections, so 20 MAGIC files compile under a sibling's name — join by md5. Next: one live cast to promote to `verified`. [`OVERLAY_HEADERS.md`](OVERLAY_HEADERS.md) "The loader". |
| 2026-09-08 | **Header entry tables seed the extractor; registry ids are in the sidecar; A/B flat on the saved scenes, and the residual is named.** `extract_overlays.py` reads the overlay header (`registry_id`, `header_entry_pcs`) and unions the 2,064 exported entry pointers into `dispatch_entry_pcs`, declared in the framework's `static_dispatch_entry_pcs` (STATIC_DISPATCH_ENTRY); 1,587 of them were known to neither seed set (SCENARIO 429, WORLD 622, BMAGIC 329, BOSS 97) and 544 were newly classified by the compiler. `names/overlays.toml` rows carry `id = "0x144"` from the catalog (`load_overlay_ids()`); catalog `observed_entries` and `pc_coverage.py` now count harvested PCs only, so header seeds do not inflate coverage. Audit: 19 benign shard fails (was 17). **A/B (`tools/interp_bench.py`, 12 slots × 15 s, interpreted insns per frame): 1.00x on every slot.** `tools/interp_bucket.py` says why: on the hottest slots ~60 % of the residual is **one unharvested jump-table case label** `0x801F92F4` in SCENA12 (id `0x111`, empty header table) and ~40 % is the BIOS exception handler; nothing header-shaped is left on played content. The seeds pay in unplayed areas, which no savestate holds yet — the next new-area session is the measurement. [`OVERLAY_HEADERS.md`](OVERLAY_HEADERS.md) "Seeding the extractor". |
| 2026-09-08 | **Overlay headers: a global registry id, unique across all 405 compiled EMI overlays.** Every overlay opens with `+0x00` u32 **registry id** then a variable-length table of entry pointers into its own image (0..59 entries; 32 of the 124 BMAGIC images have none and start code at `+0x04`). The id is **unique with zero duplicates across 405 overlays** in the dense range `0x010..0x1BF` (432 slots, 27 unused), assigned in blocks that broadly follow the directory — `SCENARIO` `0x105..0x11D`, `BOSS` `0x11E..0x140`, `BMAGIC` `0x144..0x1BF` are exactly contiguous — and it is **not derivable from the filename** (`MAGIC001` = `0x144` but `MAGIC008` = `0x14F`, out of run). `LOGO/LOGO.EXE` is the exception: first word `0x6E795356` (`"VSyn"`), because it is a PS-EXE, not an EMI section. This explains the `0x801EEC00` band confusion: `0x801EEC10` is *inside the pointer table* for most MAGIC images, *already code* for the 20 with short tables, and for MAGIC002 (3-entry table) it is the 8-byte island before code at `+0x18`. **Open — mapping a spell to its overlay:** the registry id is not an ability id; ruled out on the day were the ability table (`0x801CB230`, no field in `0x144..0x1BF`; its `+0x06` is the known `0x4000 + n` text ref), any dense LUT (no run of ≥32 consecutive u16 in `0x105..0x1BF` in `GAME.EMI`/`BATTLE.EMI`/`BATL_END.EMI`), and sprintf'd paths (no `%`-format naming `EMI`/`BIN`/`MAGIC` in any compiled overlay). Next: the boot EXE, then the runtime anchor. Method note worth reusing: the finding came from a **cross-image differential** — same offset across 124 siblings sharing one load address turned 8 unreadable bytes into a mapped format in one query. [`OVERLAY_HEADERS.md`](OVERLAY_HEADERS.md). |
| 2026-09-07 | **Data islands: the compile's "data walked as code" rejections explained, named, and folded into the loop.** After the evening Axis B run (registered entries up in every played band, 17 audit failures unchanged), every (PC, occupant) pair from the afternoon's 41 self-heal/residual rows was checked against the rebuilt fragment table: 2,041 have a piece, 307 are past the occupant's image end, 11 are memoized rejections, 0 unexplained. The 11 read by hand are all data at an address where a sibling has a function: BOSS021's handler pointer table (×3), MAGIC parameter/offset tables, SCENA17's **staff roll**, BATL_END's **results-screen printf formats** (`%7d`, `*%2d`), COMMU00's halfword table, START's **memory-card file-name template** `BISLPS-00990BOF3%02d`. New [`tools/data_islands.py`](../tools/data_islands.py) joins memo lines to occupants (image spans entry + no piece there; matched the memo line-for-line except one entry), classifies the bytes (code / strings / jp text / pointer / halfword / word / zero / mixed) and prints a name from the new [`names/data.toml`](../names/data.toml) sidecar (4 entries, `name_map.py check` validates them). Over the whole memo (3,583 pairs): ~2,300 struct-like, ~400 ASCII, ~390 JP text, ~300 pointer tables, and **23 real-code rejections** (prologue then an unsupported opcode downstream: START `0x801D0E54`, BATE, SHOP, MAGIC002/123, COMMU00, AREA035/084/127/139) — the only kind that is an actual gap, now listed by the tool. `axis_b_loop.sh` runs it as phase 5a' after the compile. [`DATA_ISLANDS.md`](DATA_ISLANDS.md). |
| 2026-09-07 | **Residency problem re-read: the "attribution gap" class was stale, and the biggest interpreted sink was hiding in "outside compiled code".** User challenged the afternoon poller summary (9 of 17 new PCs "attribution gaps", 10 "ambiguous", 51/61 "outside compiled code"). Checked each of the 9 against the 12:24 captures and the 12:27 fragment table: **8 of 9 were never demanded for any occupant** (new interior points that are a function start in a sibling, so a sibling's whole function "spans" them); they self-heal through the normal loop. Only `0x801D7D88` (1 entry, 15 insns) was demanded for all 18 and still has no fragment for anyone — the one real residual. The hottest shared-band row `0x801CE404` (213 M insns, PLCHAR) now has a fragment for all 19 occupants; its last sighting predates the 12:29 rebuild, so it is unproven until the next session. Owner breakdown of the whole observed set: **kernel RAM `<0x10000` = 25 PCs, 56.7 % of all interpreted instructions** (exception handler `0x27AC`, gates `0xA0`/`0xB0`, IntRP walk — the BIOS kernel copy is install-at-runtime and interpreted by rule, a framework-level fact); boot-EXE dirty text `0x80164E84..` = 100 PCs, 2.5 %; overlay bands ≈ 40 %. **Tooling fixed:** `harvest_interp_pcs.py` `occ_split` now splits `attrib_new` (undemanded, self-heals) from `attrib_residual` (demanded, still no resident piece; PCs listed), adds `kernel` / `bootexe` owner classes and skips unenriched rows; `pc_coverage.py` gains owner strata (`(kernel RAM -- owner: BIOS)`, `(boot EXE dirty text -- owner: SLPS_009.90)`, extent read from `game.toml`), keeps them out of the global estimate and out of *go play these next*; `area_poller.py` prints and stores the new keys. Verified: report runs, 38 boot-EXE + 6 kernel entered rows leave the pool; offline classifier over the accumulated file gives 9 new / 27 residual (residual inflated by pre-rebuild `occ_*` values — see HANDOFF §1b caveat). Evening follow-up on the first enriched summary (17 undemanded / 1 residual / 6 kernel / 37 boot-EXE / 12 "outside"): the 12 were all INSIDE compiled bands with no piece from any occupant (7 in the single-occupant battle engine, `0x80093A74` / `0x80093F00` hottest) — first-sighting interior entries, same work as seedable; relabelled `interior_new`, `none` now means no band and no owner. Agreed path: re-evaluate after the next play session; then framework work to stamp the resident *image* checksum at entry and key harvest/extract on (image, PC). |
| 2026-09-06 | **The headless savestate wedge does not reproduce; `tools/scene.py` makes a savestate a reusable offline asset.** The 2026-09-05 wedge (load acks `last_ok: 1`, then VSync and `static_hits` freeze in a BIOS `B0` loop) was recorded against `build-enrich` (Debug) and blocked the plan's biggest multiplier: every scene-specific question needed another live play session. Re-tested on `build-relprof`: **11 of 12 slots boot, load and resume headless** (300-460 emulated fps, 8-9 ms restores). The twelfth — slot 8, one of the two slots in the original report — is not wedged but *refused at the header*: a pre-v7 file (`version=5`, `codegen_hash=A4319B6F`, written 2026-09-01) that the v7 integrity gate rejects, reported as a bare `last_ok: 0` with no reason over TCP. The other reported slot (4) loads and runs. `build-enrich` and `build-dbg` are both retired, so the Debug-only failure cannot be re-tested — it is un-repro, not fixed. New [`tools/scene.py`](../tools/scene.py): `preflight` reads every `.pst` header offline and names a stale slot before a 13 s boot is spent on it (the header has carried `codegen_hash` / `abi_tag` / `codegen_ver` since v7 — SAVESTATES.md said it carried no build stamp); `check` boots+loads+resumes every slot as a regression test; `run --slot N` lands a headless instance on a scene, injects an optional press sequence, and hands the live debug port to any existing tool (`{port}` / `PSX_SCENE_PORT`). Every load is followed by `verify_running()` — VSync must advance across three windows, or the wedge is reported *as* a wedge with registers + `irq_state` captured while still frozen, rather than a `last_ok: 1` silently poisoning the next measurement. Two things found in passing: the runtime's `screenshot` fails with `cannot open file` on paths over ~260 chars (MAX_PATH), and killing the process instead of sending `quit` loses the runtime's own savestate diagnostics to stdio buffering. |
| 2026-09-05 | **The save/load and intro screens are BIOS-interpreter-bound, not overlay-bound — the fragment fix is neutral for them; dbg's 30/45fps is an -O0 artifact.** User reported build-dbg: Capcom intro 45fps, save screen ~30fps. Reached the LOAD-GAME card-selection screen headless (`slot00`->Start->card check->NEW/LOAD menu (`slot06`)->Down->Circle->"どのカードからロードしますか", parked `slot11`; needs `--disc`, and the New/Continue path only surfaces via the settled menu state — inputs during the card-check transition are dropped). **In relprof (optimized) it runs 418fps (floor min 354.9 / p1 392.3 / median 416.7), the card-check screen 470fps (floor 393.7)** — 6x the 60fps cap. `per_pc`: **99.2% of interp is BIOS/kernel (<64K), overlays ~0** (15 insns), so the overlay fragment fix cannot affect these screens (old==new). The dbg 30/45fps is the same BIOS code run through build-dbg's instrumented -O0 dirty-RAM interpreter (~14x slower than relprof); it is not compiled, so neither the fix nor the binary bloat (fragments never execute here, so not in the i-cache working set) touches it. **Intro:** also a definitional no-op (single-occupant LOGO band, byte-identical). Caveat: measured the card-selection screen; the deeper save-slot list (character previews) may draw via GAME/SCENARIO overlays — not yet reached headless. Harness: `scratchpad/{ab_frames_disc,floor_bench}.py`, scenes confirmed offline with `pst_tool.py vram`. |
| 2026-09-05 | **Floor (not mean) check on the memory-card screen.** Requested follow-up. Frame-anchored floor over 100ms windows (new build, 12s): **min 393.7, p1 433.8, p10 461.2, median 470.8, max 487.4 fps** — the 474 mean hides no low floor; the worst window is >6x the 60fps cap, one recurring ~26ms single-frame hitch (present in old slot3 too = periodic BIOS/harness, not build-specific). Composition DID shift: `per_pc` shows old ran 15.5% of interp (1.9M insns/window) in the `0x801EEC00` band, which the fix moved fully native (new 0% there); but 84-99% of the screen is BIOS/kernel card-polling code neither build changes, so fps/floor equal (mean 473.7 old / 474.1 new). Old floor not directly measurable (old+disc+savestate path flaky) but old does MORE interp (12.3M vs 10.4M/window) so its floor is <= new's, no regression. slot5 floor blocked by the unrelated intermittent BIOS fail-fast at `0x00002934` (crash-kernel-ram-2934); its mean regression (359->325 fps, <1% variance) is smooth. `frame_timeseries dc`=0 outside verify mode, not an overlay signal. Harness: `scratchpad/floor_bench.py`. |
| 2026-09-05 | **A/B follow-up: Capcom logo and memory-card screen show no fragment-fix effect.** Requested by the user. **Logo:** cannot be driven in the headless harness (Capcom `LOGO/LOGO.EXE` is a CD-XA FMV — boot races to the title even with `--disc` + `PSX_BIOS_HLE_KEEP_INTRO=1`; `LOGO.EXE` never executes). Analytically it is a **no-op for the fix**: the `0x801CE000` band has a single occupant (LOGO.EXE), and the per-variant keying only changes multi-occupant bands, so old and new emit identical LOGO code. The logo was already made native 2026-09-01. **Memory-card screen** ("メモリーカードのチェック中です", reached from `slot00`+Start, parked in `slot06`): frame-anchored A/B old vs new, 2 reps, ~474 fps both, interp/frame 470 vs 467 — **within noise, no change**. The screen is light (470 interp insns/frame) and CPU-idle at 474 fps headless, so neither the fragment coverage nor the binary-size throughput cost is visible; its real-hardware "near the 60 fps floor" feel is the frame cap / GPU-pacing, not CPU/dispatch. Net: the +coverage / -throughput trade from the earlier A/B applies to **CPU-bound interpreter-heavy in-game scenes**, not to these two front-end screens. Old baseline was rebuilt (old dispatcher on the current runtime, so the delta isolates the fragment fix). |
| 2026-09-05 | **A/B perf: the fragment fix moves real work to native, at a throughput cost from binary bloat.** Frame-anchored headless A/B (deterministic savestates, identical 1800-frame windows, old `build-relprof` 08:17 exe vs the rebuilt one; both codegen hash `ea3d3259`, savestates load fine on relprof — the wedge was the Debug tree only). Interpreted instructions per frame fell on every scene: battle menu (slot3) 493→482 (-2%), battle (slot4) 579→491 (-15%), interp-heavy field (slot5) **1369→681 (-50%)**, three repeats each <1% variance. The PLCHAR band collapsed (slot4 369k→5k interp insns) and the WORLD-area band went 1.65M→0 (slot5) — the fragments the fix generated are being hit, native dispatch count per frame flat (native runs got longer, no new dispatch overhead). BUT uncapped headless throughput dropped ~9-10% everywhere (slot5 359→325 fps): the 950 MB exe (was 396 MB; +38k dispatch entries, +20k scattered fragment fns) has worse i-cache/TLB locality than the interpreter work it saved. All scenes stay >280 fps headless vs the 60 fps play cap, so this is spent headroom, not dropped frames, unless a scene sits near the CPU floor. The 1.6 GB `generated/` is the root cost; the per-occupant fragment attribution follow-up (HANDOFF → Enrichment) would cut both. Harness: `scratchpad/ab_frames.py`, deltas per-frame off `dirty_ram_stats`/`dispatch_stats`. |
| 2026-09-06 | **"The Capcom intro never captures" — the poller was never at fault; overlay bands overlap.** `LOGO.EXE`'s band `0x801CE000` spans 118 KB and covers **PLCHAR (`0x801CE400`) entirely plus 107 KB of the `0x801D0C00` swap slot** — legitimate, since the logo is gone before a battle or shop loads. `pc_coverage.py` matched the **first** containing span in dict order and the captures file listed the logo band **last**, so all **1,233 observed PCs inside its span** went to earlier bands (1,174 → `0x801D0C00`, 59 → `0x801CE400`) and it could never win one. Order came from file iteration, so a re-extract could silently reshuffle hundreds of PCs between strata. Fixed: **narrowest containing band wins** (deterministic, most specific). The residual limit is real — with 0.9% exclusive address space, no arithmetic on an address can credit that band, so the report now prints `SHADOWED (1% exclusive) -- not playable-to-fix` and drops such bands from *go play these next*, which had been recommending a session that cannot move the number. Correct resolution is residency (`occ_crc`, now live on relprof; `area_poller watch`), not addresses. **`0x80117000` WORLD01 remains a genuine never-sampled stratum** (4.5 KB, 100% exclusive). Also: the intro is a weak harvest target regardless — `LOGO.EXE` runs ~native since 2026-09-01 and the observed set records interpreted PCs only. [`band-overlap-attribution.md`](band-overlap-attribution.md). |
| 2026-09-06 | **`build-dbg` retired; `build-relprof` is the single debug-tools tree.** Checked before deleting rather than assumed: every `PSX_*` cache option is identical between the two trees (15 keys, zero differences), the runtime contains **0 `assert()` calls** so `-O0`'s lack of `NDEBUG` buys nothing, and `build-dbg` was still on framework rev `alpha-126` against the pin's `alpha-206`. Its only unique property was `-O0` for source-level debugging of framework C — kept available by reconfiguring on demand. Against that it ran ~14x slow (below real time on the intro and card screens), lacked the enrichment, and was the tree HANDOFF told you to play, which is how a whole class of session data was being lost. Preserved first: the 13 loose screenshots to `analysis/session_shots/` (two are cited by `BRINGUP.md`, now repointed); savestates and memcards live in `saves/` and were never inside it; `input.ini`/`keybinds.ini` were byte-identical to relprof's modulo line endings. Tool defaults repointed — `run_dbg.cmd` (would have failed outright), `fmv_bench.py --exe`, and the docstrings in `playsession.py` / `enrich_pcs.py` / `verify_msgtable.py` / `harvest_logo_handlers.py`. `run_dbg.cmd dbg` still works if a tree is reconfigured. Repo 14.3 GB → 12.8 GB. |
| 2026-09-06 | **Play-session yield: two corrections, both of which were costing data silently.** (1) **The Axis B loop was pointed at the wrong build.** HANDOFF said to play `build-dbg`; that tree predates the dirty-PC enrichment (`occ_crc=0` occurrences in its exe vs 3 in `build-relprof`), so a session there produces bare PCs and the seedable / attribution / outside split cannot be computed at all — `harvest_interp_pcs.py` reported *nothing* rather than complaining. The harvest is port-based, so no rebuild was ever needed: `build-relprof` is enriched, has the same debug tooling, and holds 60 fps. Fixed — the tool now prints a loud WARNING when no row carries `occ_crc`, `axis_b_loop.sh` defaults to `build-relprof` and gained `--build DIR`, and HANDOFF step 1 is corrected. (2) **Coverage was overstated by duplicate sessions.** `pc_coverage.py` had been flagging 7 session ids that provably sample one process; counted as independent they inflate Q2 and the estimator reads near-saturation. After `--merge-duplicates` (10 session ids → 3, all 2,358 PCs preserved, verified identical) the picture changes materially: global estimate 1,199 → **3,798**, `0x801D0C00` 83.9% → **17.9%**, WORLD 91.3% → **41.3%**, field/map core 39.2% → **6.2%**. Axis B is far further from its stop condition than the old numbers implied. Priorities now read: WORLD01 and the Capcom intro band **never sampled**, then field/map core, BOSS and the BATTLE+ETC band. |
| 2026-09-06 | **Pin bumped to `2fa3472a`; recomp-ui #48 refreshed.** The morning's blocker was re-checked against the day's upstream movement and is unchanged: our #324/#325 are in `upstream/master` (now `8d56cf65`, 26 commits past the old pin), but psxrecomp master reads 20 `RecompLauncherCNetplayLaunch` fields of which **six exist only on mstan's unmerged `merge/frameblend-localization` (`bba6266`, committed again today)** — 37 ahead of / 5 behind master, header edits spread over ~13 commits tangled with lobby chat and frame blending, so no clean cherry-pick. Confirmed that refreshing our own #48 onto master does **not** unblock it: the fields are on neither our branch nor master (0/5 on `origin/master`, `feat/additional-ui-functionality` and `db12620`; 5/5 on `bba6266`). Pinned instead to `2fa3472a` = old pin `17f49ad3` **fast-forwarded** by our two commits (no merge commit needed — the branch already contained the pin), mirrored as `pin/bof3-fragments-17f49ad3` on the fork so the SHA outlives the merged PR branch. **Verified, not assumed:** `runtime/src/main.cpp` — the file that broke the `155e269b` attempt — recompiles and `psx-runtime` links in 17.4 s. This also closes the pin/build mismatch: `build-relprof` was already built from this code while the gitlink said otherwise. Separately, #48 was 5 commits behind master (SBI picker, #50); merged master in (not rebased, to keep PR review history), clean auto-merge, and `ctest` is an identical 9/11 on the merged branch and on plain master — both failures reproduce upstream, so no regression. |
| 2026-09-06 | **Battle icon-strip damage confirmed fixed — item closed.** Player retest on the `17f49ad3` pin (which carries the GP0 polyline terminator fix, [#313](https://github.com/mstan/psxrecomp/pull/313)): the enlarged command icon keeps its full 24×24 texture across several heal spells and item uses in several areas — the effect class that originally produced the damage — with no recurrence. Confirms the 2026-09-03 diagnosis that the icon strip and the herb-effect terrain wipe were the same shaded-polyline de-phasing. The savestate-load fallback theory is superseded and retired unqualified. [`battle-icon-strip-rows.md`](battle-icon-strip-rows.md). |
| 2026-09-06 | **All three PRs merged upstream; the pin bump was attempted, blocked by upstream, and reverted.** [#321](https://github.com/mstan/psxrecomp/pull/321), [#324](https://github.com/mstan/psxrecomp/pull/324) and [#325](https://github.com/mstan/psxrecomp/pull/325) merged 05:36-05:42Z, and `git log upstream/master..17f49ad3` is empty. But `psxrecomp` `155e269b` will not compile against any published `recomp-ui`: the netplay series (`8f266efe`, `5e1b7d33`) reads `guest_memcard` / `is_spectator` / `spectator_wire_slot` / `host_spectates` / `slot_port[_valid]` off `RecompLauncherCNetplayLaunch`, fields that exist only on mstan's unmerged WIP branch `recomp-ui@merge/frameblend-localization` (`bba6266`, still moving today) — not on `recomp-ui` master, not on our `db12620`. `8f266efe` is an ancestor of all three of our merge commits, so no upstream commit carries our work *and* builds. Pin reverted to `17f49ad3`; retry when the recomp-ui half merges. **Second finding, unrelated:** the relink that exposed this also failed with a duplicate `ov_frag_...801F6C90` across two overlay objects although one source defines it — **stale objects from the pre-#325 unit numbering**, not a codegen bug; #325 renumbers every `overlays_static_NNNN.c` and the old `.obj`s outlived their sources with newer mtimes. Cleared them; 779 units relinked in ~15 s (ccache). **Third:** the now-current `build-relprof` benches at **213.8 / 226.8 present/s** on the FMV vs 99.6 on 2026-09-05 — the first measurement of the fragment fix on the play tree. Interp rose 38.6 k -> ~450 k insns/s in ~66 k blocks/s (~6.8 insns/block), consistent with fragment-boundary churn; unconfirmed. |
| 2026-09-05 | **Fork branch housekeeping: today's work is all PR'd, the dead prototype is retired.** The two branches pushed today without PRs now have them upstream — [psxrecomp#324](https://github.com/mstan/psxrecomp/pull/324) (`feat/dirty-pc-enrichment`) and [#325](https://github.com/mstan/psxrecomp/pull/325) (`fix/static-fragments-per-variant`, stacked on #324; GitHub cannot take a cross-fork base branch, so both target `master` and #325's body says to merge #324 first). Audit of the rest: `fix/present-skip-unpaced-vsync-cadence` and `fix/gpu-polyline-terminator` (#313) are already in upstream master; #321 (starvation watchdog) is open. The one unpushed branch, `fix/vblank-cadence-pacing`, held only `d725af45` — the walk-HLE prototype whose own commit message records that it was refuted (the IntRP walk at `0x2914` runs only during FMV startup; steady-state IRQs take the `0x27F8` fast path) — on top of three commits already upstream. Its reusable half is now transcribed into [`vblank-pacing-bug.md`](vblank-pacing-bug.md) → *Reusable mechanics*: the guest-callback-from-native-hook contract, and the count-on-match-before-the-enable-check pattern that let a disabled prototype disprove its own premise. Branch deleted; commit archived on the fork as `archive/vblank-intrp-hle-prototype`. **recomp-ui #42 (scanlines card) was closed unmerged on purpose** — its card was folded into #48 `feat/additional-ui-functionality`, which is the live PR awaiting merge and the current pin's lineage. |
| 2026-09-05 | **Static fragment pass fixed: per (entry, image), not per address (psxrecomp fork `fix/static-fragments-per-variant` `2fa3472a`).** Diagnosis path: `occ_resolve.py` on the title-screen probe showed every SCENARIO-band piece from a non-resident section; offline, only ONE compiled variant spanned `0x801F6C90` (a SCENA19 fragment) although all 20 SCENA captures listed it; `compile_overlays.py` on SCENA16+SCENA19 alone built 55 fragments, on the full set SCENA16 got 0 — in every multi-capture band only the LAST capture carried fragments (BOSS055 35/35, PLP678 19/19, SCENA19 20/20) because `existing_entries`/`static_entry_sources` were keyed by address. Fix keys demands `(entry, image crc)`, runs fragment compiles on the `--jobs` pool with the image table sent once per worker, memoizes deterministic rejections (audit, and the recompiler throwing on a walk off the image end — the one `exit 3`) as skips, and groups fragments one unit per image. Full run: 20,250 fragments, 3,570 memoized, 238 s cold, served keys 99,778 → 138,148, nothing lost, `failed=17` unchanged. Title probe (headless, Start/Start/Circle): SCENARIO-band interpreted entries 12 → 2, the two left unharvested. `generated/` 1.6 GB / 779 units, build-dbg exe 699 MB; the per-fragment cost is ~43 ms. Enrichment branch committed `6e760748`; both pushed to the fork. Harvest/poller now report the gap split. |
| 2026-09-05 | **Tier-1/2 runtime enrichment built and verified (psxrecomp fork branch `feat/dirty-pc-enrichment`, uncommitted).** Each interpreted PC now carries, stamped on external entries only: `occ_crc` + `occ_ok` (the compiled static piece whose code ranges span the PC — the runtime validates **per function/fragment**, so this is a function-level CRC, joined back to section + symbol by new [`tools/occ_resolve.py`](../tools/occ_resolve.py) from `generated/overlays_static.c` — and whether it validated at the time) and `ext_ra` (the caller's `$ra`; `ext_ra == pc` marks a `jr ra` return into interpreted code). The first draft returned the loader's process-global last hash, which is never set on the static path (all zeros on a 60 s boot); replaced with a per-PC scan of the static-match cache, then extended with the validity flag when the first real result showed why: **at the title screen the resident SCENARIO-band section is SCENA16, but every compiled piece spanning the interpreted PCs there was compiled from another section's bytes** (`0x801F6C90` ← SCENA19 fragment, 517 entries; `0x801F7348/70` ← SCENA04; …) — the band is compiled yet fully interpreted because the resident section has no pieces at those PCs, an attribution gap in the observed→extract step, not an interior gap a seed can fix. `tools/harvest_interp_pcs.py` merges all three fields. Built in a new `build-enrich` tree (Debug, mirrors build-dbg; ccache made it a 5-minute build) so the live play session on build-dbg was untouched; headless boot + injected Start/Circle past the title was the test bed because **headless TCP savestate loads wedge on this build** (VSync counter frozen after `load` of slots 4 and 8, `last_ok: 1`) — noted under Known issues. Reply buffer 32 → 64 KiB. `docs/TCP_COMMANDS.md` (submodule) documents the fields. |
| 2026-09-05 | **Ghidra re-seeded with the Psy-Q names.** New `ghidra_run.py names` (+ `tools/ghidra/apply_names.py`) writes symbols.toml onto existing programs — the boot program and each overlay program's `boot_*` map — instead of waiting for a re-import; `--overwrite` makes symbols.toml win over hand names (it replaced the stale `Save_FlagsChecksum` and the `bios_*` thunk names). Boot program: 527 named of 1029. The four overlay programs imported before the 08:40 boot-mapping fix (GAME, SHOP#0, SHOP#8, START#8) were re-imported `--overwrite` so their decompiles stop truncating at boot-EXE calls. Proof: `Window_DrawFrame` decompiles as `GetGraphType(); SetDrawMode(); SetTile(); SetSemiTrans()`. |
| 2026-09-05 | **Psy-Q library names: 500 boot-EXE functions named in one pass (NAME_MAP route 2 closed).** New [`tools/psyq_sigs.py`](../tools/psyq_sigs.py) matches the whole-object-file signatures of lab313ru/psx_psyq_signatures (cloned beside the repo, not vendored) against `disc/SLPS_009.90`: an object is a hit only when its entire code matches under the relocation wildcards, so every global label inside names a function with no per-function false positives. Version table: SDK **3.70** wins outright (287 objects / 574 labels vs 237 for 4.00, 165 for 3.6x; the EXE is stamped Jul 16 1997); libcd's object set matches better from the 4.6 folder so `apply` merges every version and lets the largest object win per span. Rules paid for: 16-byte stubs (COMB_3, DMYNOT1, SMP_12, VM_VIB) match inside real objects or at six unrelated places — accepted only when wildcard-free **and** unique in the EXE, which keeps the LIBAPI BIOS thunks (`rand`, `printf`, `open`/`lseek`/`read`/`write`/`close`, `firstfile`/`nextfile`, `_card_*`, `Exec`, `FlushCache` …) and drops the junk. Result: 246 placements, 526 labels, **500 new `confirmed` rows** (`psx_symbols.h` 527 funcs): libsnd 121, libgpu 101, libgte 63, libcd 53, libspu 45, libetc 29, libapi 18 + thunks, libcard 7. Of GHIDRA.md's six open boot helpers `0x8017AF98` = `GetTPage` and `0x8017CC50` = `SetDrawMode`; `Rand` and `BootEntry` kept their names (Psy-Q agrees: `rand`, `__SN_ENTRY_POINT`). Duplicated names got `_2`/`_3` suffixes (three `memclr` copies in the INTR objects, libcard/libpad wrappers vs their BIOS thunks). The Ghidra programs see the names on their next `import` (seed_overlay reads symbols.toml). |
| 2026-09-05 | **Dialogue anchors landed: the box-string writers are named.** `capture --watch 0x801490A4-0x801490B0` on `slot04` (MacNeil village NPC, Circle ×2) caught the whole open in one frame: GAME.EMI **`Script_ShowMessage`** `0x801A27A8` (u16 id at obj+8; bit 0x8000 runs a script callback first, bit 0x2000 selects the system pool) → boot **`Msg_OpenScript(idx)`** `0x8015034C` = `0x80010000 + u16[0x80010000 + 2*idx]` → `0x801490A8`/`AC`, then **`MsgBox_Reset`** (eats a leading `0x0C` speaker byte, `Window_Alloc(0,0)`), index to `0x801490A4`; its twin **`Msg_OpenSystem`** / **`Msg_SystemPtr`** reads the `0x80014000` pool with the header word chosen by id bits 14–15. Both have zero boot-EXE callers — every message open is an overlay calling one of them. The two-page line is one string split by `0x02` (state 3), no second open; `MsgBox_Step` advanced one glyph per 6 frames. Message 3 decoded off the disc (`u16[3] = 0x245`) matches the aligned corpus row (`pairs.json` block index = section index + 1). Also read from the stepper: codes `0x0A` sound, `0x0F` style table `0x8017FF30`, `0x10` flag toggle, `0x14` choice menu, and every byte `>= 0x12` except `0x14` is a glyph — so `0x3E/0x3F/0x40` and ASCII `HP` are atlas indices, not controls. `Window_Task` → **`Window_DrawFrame`** `0x8015A58C` draws the box from window record `0x80148644` (+4/+6 pos, +0x10/+0x12 size; text origin `(x>>4)+10, (y>>4)+6`) — IDEAS I3's open question. **Hook consequence:** the string pointer is never a dispatch argument, so the framework's a0..a3 apply hook cannot see BoF3 dialogue; its in-place message patch fits but needs a BoF3 encoding profile upstream, or a repoint at `MsgBox_Reset`. 13 boot functions `confirmed` in `symbols.toml` (`psx_symbols.h` 27 funcs), `Script_ShowMessage` at `evidence` in `names/functions.toml`. [`TEXT_ENGINE.md`](TEXT_ENGINE.md) "The resolver". |
| 2026-09-05 | **World-map place names are painted plates, now read out.** The user's check (AREA007's map label is マクニール村 in kanji; no such string anywhere on disc or in RAM) led to the texture: each world map's 256 KB page (`dest 0x0E001000`, 32×32-halfword tiles, 1024 8-bit texels wide, CLUTs from `0x8002BE00`) carries a strip of pre-painted name plates (14-row body, widths 44/60/76 texels, wrapping at texel 256). Proof: the AREA033 section matches `slot07`'s VRAM byte for byte, and the JP/US disc diff of every area's images is exactly the nine world maps + three fishing spots + the Pompom dock. `tools/plates.py` decodes the pages and finds the plates by their rim; `names/plates.toml` holds 85 plates / 42 names transcribed from the contact sheet, 39 with wiki English. Pixel budget for English: 3 / 4 / 5 glyph cells at the game font. The "blank plate + rendered text" idea is written up with its constraints. [`TEXT_TABLES.md`](TEXT_TABLES.md). |
| 2026-09-05 | **Name tables read straight from the `.EMI` (IDEAS I4 done).** `tools/text_tables.py extract` parses the fixed-stride record tables off the disc through the `.cue`: GAME.EMI holds **five item tables** (consumables 92 × 14 B at `0x801C995C`, key items 16 × 12 B, weapons 83 × 20 B, armour 68 × 18 B, accessories 52 × 16 B — the four inventory categories are four tables, which settles the category question) and the ability table (227 × 16 B at `0x801CB230`); MTEST.EMI the 200-entry debug place list; COMMU02 / START the roster and the new-game templates. Written to `names/items.toml`, `abilities.toml`, `places.toml`, `characters.toml` with the wiki glossary's English (spells/items ~95 %). **The MTEST index is the AREA number** (200 entries = AREA000..199, names agree), so `places.toml` joins each debug entry to its area script: the on-entry kanji banner (`caption`, 11 areas — グラウス山 user-confirmed on screen), the developers' message-0 label (`dev_label`, 81), and English through the wiki (caption → dev label → kana → a wāpuro-romaji fold of the wiki's romaji column) = 45 of 200, all checked by eye. The world-map scripts hold region names and spot descriptions, not a spot-name list; most interiors never display a name. `save_tool.py dump` now prints `薬草(Healing Herb) x28`; `verify` gained four cross-checks and all pass on every save: every id is a record, every learned ability sits in the list its table type (`b1 & 3`) selects, **weapon `power` = ATK bonus, armour `power` = DEF bonus** (Garr's Titan Belt +10 is an accessory effect code, reported unverified). Encoding: `0xFB` = ヴ, digits/capitals are raw ASCII in name fields, `0xFF` separates words. Also fixed a `save_tool.py` bug (a comment had eaten the `level` field; `dump` raised). Masters are a message block, not a table. [`TEXT_TABLES.md`](TEXT_TABLES.md). |
| 2026-09-05 | **Starvation-watchdog fix filed upstream (track F).** The 2026-09-02 false-trip patch went to [mstan/psxrecomp#321](https://github.com/mstan/psxrecomp/pull/321) from fork branch `fix/starvation-watchdog-wrap` (`430c93b8` off the pin): heartbeat read before the clock through a pure `starvation_watchdog_stale()` helper, `_Atomic` heartbeat, and exit-origin labels (`starvation_watchdog`, `console_close`, `console_ctrl_*`, `signal_sig*`) so these exits stop reading `atexit`/`unknown`; new ctest `starvation_watchdog_test`. Verified on `build-dbg`: 10 min with `area_poller.py` at 1 s and the watchdog **enabled** at 4 s — no trip (pre-fix ~2 min); forced 1 ms trip labels the report correctly. Gitlink unchanged at `17f49ad3` (bump when merged, then drop the `setx PSX_STARVATION_TIMEOUT_US=0` workaround). [`starvation-watchdog-false-trip.md`](starvation-watchdog-false-trip.md). |
| 2026-09-05 | **`Battle_Init` to evidence (track B).** Body vs the `battlebegin.json` +563 write set: all 33 offsets explained — the effective-stat block `C+0x90..+0xAC` is snapshotted to `C+0xB0..+0xCC`, engine `Formation_ApplyStatMods` (`0x800A79AC`, keyed on the formation id `0x80144F54`) adjusts the scratch, and it is copied back; low-HP status `0x2000` (< max/4) set here; command/flag bytes zeroed; battle globals reset. `0x800A8AD4` = normal/boss split. [`BATTLE_RAM.md`](BATTLE_RAM.md) actor table. |
| 2026-09-05 | **Capcom intro sampled twice (track D); relprof verified on the pin.** Two fresh boots harvested during the FMV (`--session d_boot1/2 --area LOGO/LOGO.EXE`): 0 new entries and **0 interpreted PCs in the logo band** — it is fully native, and `pc_coverage.py` reports it NEVER SAMPLED only because the estimator has no rows there (8 sessions total now, est. 46 % overall). `build-relprof` was already current against `17f49ad3`; `fmv_bench.py`: 99.6 present/s, 38.6 k interp insns/s during the FMV (baseline ~97.6 uncapped), `build-dbg` 50.1/s (baseline 49–52). No pacing regression from the pin. |
| 2026-09-05 | **Skill and item commands captured.** `--hold up` / `--hold down` with three Circles per member: skill = command 4 with the id in `C+0x11A` (`SkillMenu_Open` / `SkillMenu_Confirm` / `Skill_TargetSetup`, default target from the skill table `0x801CB230`), item = 5 (`ItemMenu_Open` / `ItemMenu_Confirm`, count reserved at confirm by `ItemMenu_Reserve`); **`C+0x118` is the target actor** (`Attack_TargetSetup` too); `Actor_SkillItemDone` clears the byte and deducts the AP cost. Rei's steal, the vitamin heal (Nu save, Ryu 1 → 10) and the enemy specials all land through **`Effect_ApplyResult` `0x8009A160`** (engine; seeded into Ghidra from the gap's only prologue): a `jalr` effect table `0x800B165C` indexed per skill (`0x800B1438`) or per item (`0x800B164C`) fills a result record `0x8014639C` (+4 HP delta, +6 AP delta) that it applies with heal cap, ±9999 clamp, kill + survive check. 10 names (9 evidence). [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-05 | **The command menu, Run and the enemy AI live in the battle engine band.** The first venn was blind because the default `fn_filter` (`0x801D0C00-0x801F0000`) excludes `BATTLE.EMI#15` at `0x80093800`; recapturing the five commands with `--lo 0x80093800 --hi 0x801D0C00` and a 100-frame window (the engine band stamps ~1 700 entries/frame, so anything longer wraps) found them. Each actor carries a command byte `C+0x119` (1 Attack, 2 Defend, 0 Watch with `C+0x124 \|= 1`, 4 skill, 5 item) written by `Cmd_ConfirmAttack` `0x80093E14` / `Cmd_ConfirmWatch` `0x8009521C` (engine) or the Defend table target; Auto sets `0x801462E4 \|= 0x10` and `AutoBattle_FillCommands` writes 1 to all three, refilled every round by `Battle_RoundStart`; `Battle_CommitRound` builds the order, consumes items and calls the engine's **`EnemyAI_ChooseActions`** (8-row scripts at `0x800E407C + (rec+0x60)*0x88`, condition opcodes + action types, once-only bits at obj `+0xE1`). **Run**: `Escape_Roll` `0x80098278` — 3rd attempt or preemptive = certain, else `Escape_Chance(mean party AGI − mean enemy AGI)` from `0x800B1430` `{32..52}/64` (+8 second try, +8 status `0x2000`, cap 64) vs `Rand & 63`; success → `Escape_Begin`, fade, mode 5/3; failure → `Escape_Failed` rebuilds the order without the party. 19 more names (13 evidence). Still open: Defend confirm body (Ghidra gap), Defend flag `0x80`, skill/item captures, AI row semantics. [`BATTLE_RAM.md`](BATTLE_RAM.md) "Commands, Auto, Run and the enemy AI". |
| 2026-09-05 | **Battle commands, turn order, kills, drops and the results tally (track C).** Five command captures from `slot03` (Attack, Defend, Watch, Auto, Run ×3 — every Run succeeded), one Nu round, and a victory capture paged through the results with `--press-gap 150`. The five-way venn has **no command-specific compiled code** (0 auto-only, 0 defend-only; watch-only = two Examine state entries; run-only = the escape exit), so the decision points are in shared or still-interpreted code. What the captures + Ghidra did give: `Battle_BuildTurnOrder` `0x801DAAB4` (party **AGI `+0x24`** + command bonus × level-class pct, enemy **AGI `+0x28`** + random byte; list at `0x80146308`, cursor `0x8014631E` consumed by `Battle_BeginAction`), `Battle_EnemyDefeated` `0x801E542C` (enemy **`+0x04` zenny, `+0x06` EXP, `+0x08` = level**), `Battle_RollDrops` `0x801E525C` (drop slots `+0x18/+0x1A`, `+0x1C/+0x1E`; `Rand & 0xFF <= {0,0,1,3,7,31,127,255}[class]`; list `0x80146330`/`0x80146350`/count `0x80146323`), and BATL_END's `BattleResult_Setup` (`Char_LevelUp` caller, zenny ×1.5 condition, tick step = max(1, total/30)) / `_ExpTick` / `_ZennyTick` / `_AwardDrops` (the `Inventory_Add` caller — body only, no drop landed at 1.6 %). Banner pool `0x801EB460` + slide states named. Enemy skill damage is applied by engine `0x8009A3E4`, not `Battle_ApplyDamage`. 30 names (17 evidence), BATTLE_EMI3 (663 fn) / BATL_END (109) / BATTLE_EMI15 re-imported. Interpreted PCs harvested (6 sessions, est. 50.8 % coverage; BATL_END runs almost entirely interpreted). Traps: ring wrap keeps the newest 262 144 entries; `slot03` is the first member's menu. [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-05 | **Damage formula read end to end; a Ghidra blind spot fixed.** `Battle_BaseDamage` = max(0, ATK − DEF) (enemy attackers: DEF = (party mean DEF + target DEF)/2, plus `enemy+0x08 × (2..3)/10`), + `Rand&1`, then `Battle_ScaleDamage`: ×205/256, × an 8-entry 0.85..1.20 table, × elemental affinity (`0x8009FA78`, unread) and × a type table under weapon flags. The "defence steps" are **to-hit rolls** (`Battle_HitCheck_PartyTarget` vs target evade % `rec+0x37`, `_EnemyTarget` vs attacker hit % `rec+0x38`). ATK/DEF are persistent `+0x20`/`+0x22` (enemy `+0x24`/`+0x26`), copied per action to `0x801EC278` (actor) / `0x801EC258` (target) by `Battle_BeginAction` / `Battle_ResolveAction_*`. One `slot03` round reproduces all four hits exactly (31 / 13 / 6 / 1). Rei = roster 4. **Tooling:** every overlay decompile had been silently cut at the first boot-EXE call because `Rand` is a BIOS thunk (`jr` to `0xA0`) Ghidra flagged no-return; `seed_overlay.py` now maps the boot EXE into each overlay program and makes the 55 thunks returning ([`GHIDRA.md`](GHIDRA.md) Traps). 8 names written (5 evidence, 3 hypothesis). [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-05 | **`psxrecomp` re-pinned to plain upstream `master` `17f49ad3`.** Upstream merged everything the fork carried (#296 parallel compile, #307 fast-forward pad, #313 polyline terminator, #318 fast-forward toggle, #319/#320 keymap explicit unbind); `3b8b98fa` → `17f49ad3` is fast-forward with no fork-only commits left behind. `recomp-ui` unchanged at `db12620` (#42 + #48 still open). Rebuild pending. |
| 2026-08-29 | Repo inventoried; `docs/` and `CLAUDE.md` established. Localization intent recorded. |
| 2026-08-29 | Build toolchain installed (MSYS2 MinGW-w64) and proven by a full 169-target emitter build. Toolchain blocker cleared; README gained a *Development environment* section. |
| 2026-08-29 | Dump moved into gitignored `isos/`; `[game].disc` switched from an absolute machine-local path to repo-relative `isos/Breath of Fire III (Japan).cue`. Portable for any checkout. |
| 2026-08-29 | **First boot.** Emitters built (67/67); generate clean under `strict`; `psx-runtime` linked (232/232); headless boot reaches game code and runs ~26k frames. Logged in [`BRINGUP.md`](BRINGUP.md) with framework bugs F-1 / F-2. |
| 2026-08-30 | **The game loads.** Boot 001's wait-loop diagnosis retracted — the pinned PCs are `DrawOTag`/`VSync`. Root cause of the blindness: `PSX_DEBUG_TOOLS=OFF` in Release. Built `build-dbg/`, added `tools/disasm_exe.py`, captured title screen + prologue over the debug server. [`BRINGUP.md`](BRINGUP.md) → Boot 002. |
| 2026-08-30 | **Savestates verified working** on the LLE backend — save *and* load, round-trip confirmed against the VSync counter at `0x8018603C` (advanced 17,460 -> 20,312, rewound to ~17,828 on load). Files: `saves/openbios/state_8014AA0C_slotNN.pst` + `.thumb`. Driven by `tools/playsession.py state save|load N`. |
| 2026-08-30 | **Script load path measured.** `wtrace` on the text buffer shows the script arriving by CD-ROM DMA (channel 3) kicked from PC `0x80177B78`. `rtrace_*` is MMIO-only so it cannot see the *readers* — finding text-draw PCs is an open tooling gap. [`LOCALIZATION.md`](LOCALIZATION.md) 4.2b. |
| 2026-08-30 | **Localization assessed.** Capture pipeline armed via `PSX_XLATE_CAPTURE=1` (it is *not* always-on, contrary to framework docs — F-3); 39.6M calls scanned, 0 real strings, 3 false positives that decode as MIPS instructions (F-4). English scaffold generated; US build shown **not** address-compatible with JP (0x3000 shift, 4.6% seed overlap). [`LOCALIZATION.md`](LOCALIZATION.md). |
| 2026-08-30 | **Text engine identified - the translation blocker is closed.** Ghidra 12.1.3 + GhidraMCP 6.0.0 + PyGhidra stood up; boot EXE imported as Raw Binary at `0x80093000` (1025 functions). The three `0x80010004` leads resolve to `0x80150598` (box renderer), `0x8015096C` (stepper) and `0x8015AD34` (immediate `draw(x,y,str)`), sharing one control-code vocabulary and the glyph path `0x8014F6BC` / `0x80151F4C` (21-glyph, 12 px atlas). Message lookup is `0x80010000 + W + u16[base + 2*index]` where `W = *(u32 *)0x80010004`. [`TEXT_ENGINE.md`](TEXT_ENGINE.md). |
| 2026-08-30 | **Regional builds compared (JP/US/EN/FR/DE).** No release has runtime language support - one EXE per disc, no language strings, a separate SKU per language (SLES-01304/01319/01320). No Western EXE is address-compatible (EU vs US ~21% word-identical over the real code region; earlier whole-file similarity figures were zero-fill artifacts). Cross-language `.EMI` diffing isolates all divergence to the `0x80010000` section (93.4% of bytes, and the only section that changes size), **independently confirming** the Ghidra finding and showing translations are not bound by the JP byte budget. [`regional-builds.md`](regional-builds.md). |
| 2026-08-30 | **Message-table formula confirmed live — the last static-only claim is now evidenced.** Read directly off a running `build-dbg` session: the name-entry screen's on-screen string located at `0x80014A86`, and the formula walked from `0x80014000 + W` decoding 17/24 item descriptions; then the mine-area script at `0x80010000` decoding 16/16 slots of the モーグ/ギリー prologue. Correction: the `u16` table base is **per block** — the area script has no `W` header and its table starts at +0, so `*(u32 *)0x80010004` is not universal. Name-entry, config and item text live in the `0x80014000` pool, *not* the `.EMI` area script, so replacing only that section leaves menus Japanese. `tools/verify_msgtable.py` added. [`TEXT_ENGINE.md`](TEXT_ENGINE.md). |
| 2026-08-30 | **Savestate load found to wedge/crash the runtime.** TCP `savestate load` blocked the emu thread past 30 s, tripped the starvation watchdog into two ~100 MB freeze dumps, and killed the listener; the in-game **X** load path crashes the process. **Enter/Start loads work.** Recorded under Blockers → open, non-blocking. |
| 2026-08-30 | ~~**Reproducible crash isolated: unregistered overlays.**~~ **RETRACTED same day — the diagnosis was wrong.** See the retraction entry below. |
| 2026-08-30 | **Overlay crash diagnosis retracted.** The `interp_unsupported` record (`pc 0x801D0AA8`, `insn 0x00000000`, *instruction guard*) was read as a crash signature. It is not. A clean headless boot that never left the title screen, took no input and exited via `tcp_quit` produces the **identical** record, and `dirty_ram_unsupported` reports **`aborts: 0`** — the dirty-RAM interpreter probes that address once at boot, the guard rejects it, nothing aborts. Corroborating static evidence: the boot EXE contains **zero** JAL, J, lui/ori pairs or data words referencing the `0x801CE401`-`0x801D0C00` gap, and a seed (`0x801D0C04`) sits immediately past its end — so the gap is a zero-filled data/BSS region between code, not an overlay landing zone. `overlay_loader registered=0` is likewise expected: `[runtime] overlay_cache` defaults **off** and is a performance feature; overlay code runs via the dirty-RAM interpreter without it. **No crash artifact exists on disk** — both surviving run reports ended `atexit` via `tcp_quit` / `sdl_window_close`. |
| 2026-08-30 | **Freeze-dump disk cost noted.** Twelve automatic freeze dumps written in one afternoon, ~1.2 GB total in `build-dbg/` (gitignored, but they fill a disk fast). Each stall writes two ~100 MB JSON dumps. Worth capping or pruning between sessions. |
| 2026-08-30 | **The crashes are root-caused: the starvation watchdog, by design.** `starvation_ring.c` `exit(2)`s after 4 s without an emu-thread heartbeat (measured gap 4.027 s), which is why every report reads `reason: atexit` / `exit_origin: "unknown"` — `exit(2)` is untagged. Ruled out along the way: the save itself (`slot04` written complete, process lived 58 s more), SIO (~1,957 events/s is normal pad polling), a hard fault (no Windows Error Reporting record at all), and `psx_fatal_halt` (`fatal: None`). Override with `PSX_STARVATION_TIMEOUT_US=0`. Also found: two ~80 MB freeze dumps are written at *every* boot from a frame-328 watchdog false positive, ~160 MB per launch. |
| 2026-08-30 | **~90% of execution is interpreted — the seed list is the bottleneck.** Live measurement: 264.0 M interpreted instructions vs 28.0 M native dispatches (`dirty_ram_stats` / `dispatch_stats`, `miss_total: 0`, `aborts: 0`). `seeds/ghidra_funcs.txt` has 523 entries against 1,025 Ghidra functions, and all eight hot interpreted PCs sampled are missing from it; only 15 seeds are above `0x80190000` where the hot code sits. All are static-EXE addresses, so this is missed heuristics, not overlays. Unifies the day's symptoms: slow interpretation → boot freeze dumps + 4 s starvation `exit(2)`, and non-dispatchable PCs → `savestate_resume_pc_ok()` false → save refusals. |
| 2026-08-30 | **Save refusals explained.** `savestate_poll` defers 2 s then fails with `SAVE FAILED ... no safe resume PC` unless `psx_is_dispatchable(pc)` holds. Reproduced twice at one spot. The guard is correct behaviour — it refuses to write "a structurally valid but poisoned state" — so the fix is seed coverage, not the savestate code. |
| 2026-08-30 | **Submodule reset to the pin; regenerated and rebuilt.** `psxrecomp` had floated to `a91884a4`; reset to `f24b7e5d` (clean, one upstream commit, nothing local). Emitters already matched the pin. `build-dbg` rebuilt 199/199 after a reconfigure — note the generated shard count is captured at configure time, so a generate that changes the shard count needs `cmake -S . -B build-dbg ...` again or the link fails on undefined `func_*`. |
| 2026-08-30 | **Seed-list hypothesis tested and DISPROVEN.** Extending `seeds/ghidra_funcs.txt` from 523 to 868 (all `verified`/`high`/`medium` analyser functions it lacked) produced a **byte-identical generate** — same 35 shards, same 1467 dispatch entries — verified by running `psxrecomp-game` out-of-band against both seed files. The recompiler's own discovery already exceeds `functions.tsv`. Reverted to 523. `tools/export_seeds.py` kept for the record. |
| 2026-08-30 | **Interior-entry seeding works mechanically but is wrong here.** Adding 8 observed interior PCs raised dispatch 1467 → 1475 and made them dispatchable, staying clean under `strict`. Reverted anyway: those addresses are **zero in the EXE image**, so the emitted bodies are NOPs aliased into a zero-fill parent (`psx_alias_body_801D0C04`). Seeding code that is not in the image is fabrication, and it invites stale-registration bugs. The real answer is overlay capture. |
| 2026-08-30 | **Tools added:** `tools/harvest_interp_pcs.py` (reads `dirty_ram_stats`/`dispatch_stats` from a live run, reports the interpreted/native ratio and the proven interpreted entry PCs, writes `analysis/observed_interp_pcs.json`); `tools/export_seeds.py` (analyser → seeds merge, null result above). |
| 2026-08-30 | **Savestates survive a rebuild.** A slot written by the 09:26 build loaded correctly under the 19:45 rebuild (pin reset + regenerate + relink) and resumed at the expected spot. The open question in [`SAVESTATES.md`](SAVESTATES.md) is closed. Saving also works again on the rebuilt tree. |
| 2026-08-30 | **Save-failure mechanism pinned exactly.** Not the dispatchability of the resume PC as first thought, but `psx_irq_resume_context_snapshot_safe()` = `g_cosim_dirty_pump_site == 0`: an interrupt taken inside the dirty-RAM interpreter can never be snapshotted. Live sampling showed a steady 9.0-9.1% native / 91% interpreted mix, so the 2 s defer window routinely expires with no safe poll. Same root cause as the interpretation problem — overlay coverage. Second live profile captured to `analysis/observed_interp_pcs.json` (575 tracked PCs, 84.4% interpreted cumulative). |
| 2026-08-30 | **The zero-fill map: 81.6% of the text segment is runtime-loaded.** 1,187,899 of 1,456,128 bytes are zero in the image, in 11 runs >=2 KB from `0x80093801` to `0x801F6C00`; real static code is only ~268 KB. Confirms overlays dominate this title. Also quantified the contamination this causes: **18 of 523 seeds (3.4%) point at zero bytes** (all `low` confidence, plus the single `data` row), and **211 of 8,694 distinct dispatch addresses are zero-fill** — i.e. the runtime has 211 registered native entries whose bodies were compiled from nothing. Dirty-RAM invalidation masks them today; it is the OV-1 stale-registration risk. |
| 2026-08-30 | **Save failure is about the interrupt path, not the aggregate ratio.** Sampled live: native share *rose* from ~9% to ~18.6% while saves went from intermittent to consistently failing — so the overall mix does not predict it. What predicts it is where the interrupt lands: entries are at the BIOS vector `0x800000A0` and flow into game code at `0x801A1538`/`0x801A1720`/`0x801A19A8`, all of which are **zero-fill overlay addresses**. Every such interrupt sets `g_cosim_dirty_pump_site`, so no poll in the 2 s window is snapshot-safe. Refutes the "new areas are heavier on the interpreter" reading: overlay code is interpreted in *every* area, including ones where saves worked. |
| 2026-08-30 | **Play-session profile harvested; seeding formally ruled out.** 568 tracked PCs over a real session: **93.6% of interpreted instructions are overlay code** (zero in image), 4.2% BIOS/kernel, and just 2.2% static EXE — the last with **`entries = 0`**, meaning the interpreter never *enters* static code and there is no missing static entry point to seed. Overlay PCs touched span `0x801970B4`-`0x801DE098`. Written up in [`OVERLAYS.md`](OVERLAYS.md). |
| 2026-08-31 | **The static overlay path had no residency signal.** `overlay_page_gen` only advances for pages armed via `overlay_watch_set_range`, whose only callers were in the inert DLL loader — so the CRC gate was consulted once per variant per process and cached negatives were permanent. Armed the watch on the cold path (`psxrecomp` `aa6fa2c9`, branch `fix/static-overlay-residency-signal`, off the pin `f24b7e5d`; parent gitlink NOT bumped). Verified live: 99.93% gen fastpath, gate re-fires only on real load events, no false invalidation. Also **measured** §8's previously-inferred cost mechanism — the transition stall is the address-miss fall-through at ~264,000 lookups/sec, 49x the field baseline. [`OVERLAY_EXTRACTION.md`](OVERLAY_EXTRACTION.md) §10. |
| 2026-08-31 | **O(1) overlay dispatch (step 2 of the upstream fix).** Replaced the sparse `switch` in `generate_overlay_dispatch` with a compile-time open-addressed hash table (`psxrecomp` `69d783f5`). Headless A/B, identical captures/savestate/protocol: throughput 106.5 -> 113.2 emulated fps (+6.3%), p1 93.9 -> 105.2; at the transition 63.9 -> 131.3 fps while absorbing twice the address-miss rate. Behaviour verified identical (same address sets, hashes agree across all 524,288 word-aligned addresses, zero false hits). Also established that the on-disk build was **three-band**, not two, and that headless savestate load works. [`OVERLAY_EXTRACTION.md`](OVERLAY_EXTRACTION.md) §11. |
| 2026-08-31 | **All ten overlay bands reinstated as the configuration.** Step 3 of the upstream dispatch fix — a resident-occupant memo (`psxrecomp` `70153175`) — cut chk/hit 1.479 -> 1.069 and wasted gate calls 4.5x on the all-bands build, worth **+33% throughput** at boot (99.0 -> 131.4 emulated fps). With steps 1-2 this overturns §8: all-bands now beats three-band on both workloads measured. Also recorded the workload trap — the memo measures neutral-to-negative on a hit-heavy savestate run and +33% on a variant-heavy boot run, so dispatch changes must be measured on both. [`OVERLAY_EXTRACTION.md`](OVERLAY_EXTRACTION.md) §12. |
| 2026-09-05 | **Save format decoded.** `capture --watch 0x800C1800-0x800C2B00` during a save: the card module builds the title frame + three icon frames (`Card_BuildTitleFrame`, `Card_CopyIconFrame0..2`, file `0x000..0x1FF`), then SHOP.EMI **`Save_BuildImage` `0x801D69F0`** copies **one contiguous `0x10B0`-byte game block `0x801448D4..0x80145984`** (character records, zenny, party, inventory, key items, abilities, a summary block) byte-by-byte to file `+0x200`, summing into a u16 checksum at `+0x270`; boot `0x8015BFC4` copies one progress-flag bit into the summary (it is `Flag_Test`, not a hash — corrected 2026-09-05 by the save tool; it was named `Save_FlagsChecksum` here for a few hours). Slot list reads file `0xE80..0xEFF`. SHOP.EMI#0 promoted to `evidence` as the shop/inn/save game-mode. Sidecar: 23 evidence / 23 hypothesis, 13 boot symbols. [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-05 | **Save path found: BIOS file API, 0x1300-byte image staged at `0x800C1800`.** Two dead ends paid for (a party-block `ramdiff` sees nothing because a same-state save rewrites identical bytes; a boot-EXE fn filter wraps the ring in ~160 frames, and the libcard range is flooded by the `TestEvent` wait loop). A fn capture on the file-API wrappers alone caught the whole sequence: `firstfile`/`nextfile`, per-slot `open`/`lseek 0xE80`/`read 0x80 → 0x800C2680`/`close`, then `open(mode 2)`/`lseek 0`/**`write(fd, 0x800C1800, 0x1300)`**/`close`. The caller is a **memory-card manager module** (5 KB section shipped identically in SHOP.EMI and START.EMI, resident at `0x801EEC00`; promoted to `evidence`): seven `Card_*` functions named with evidence. Tracer: forest builder made linear (bounded indexed open stack; a boot-EXE capture used to hang for minutes), drain progress line, both swap slots always identified. [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-05 | **Equipment slots and the equip routine.** Same-weapon re-equip showed only `Char_RecalcStats` (+8 Pwr from the weapon, +1/+5 Def from armour, max HP halved by the `+0x1A` step); `ramdiff --width 1` on a real swap found record **`+0x0E` = equipped weapon id** (28→3) with the weapon leaving inventory category 1; `capture --watch` on the swap back named **`Menu_EquipConfirm`** (START.EMI `0x801D6D94`, evidence): for each of **six equipment slots `+0x0E..+0x13`** whose pending choice differs, boot `Inventory_Remove` (`0x80166F30`, new symbol) then `Inventory_Add`, store the id, then `Char_RecalcStats`. Slot→category table at START `0x801EC3E8` = [1, 2, 2, 2, 3, 3]. START.EMI (field menu) promoted to `evidence` and imported into Ghidra (623 fn). [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-05 | **Shop purchase → inventory arrays found.** `capture --watch` on zenny during four buys: SHOP.EMI subtracts zenny inline (`0x801D1E9C`, price × qty) and `jal`s boot **`Inventory_Add(category, id, count)` `0x80165AA4`**, whose pointer tables (`0x801C9934`/`0x801C9948` in GAME.EMI data) give the inventory: 4 categories × 128 ids at `0x80145048..` and counts at `0x80145248..` (cap 99), plus an id-only list at `0x80145448`. `Zenny_Sub` `0x80166FCC` named. SHOP.EMI (both sections) imported into Ghidra. Two tool fixes paid for: seeded functions are now created in descending address order (a lower start was swallowing a higher one), and write attribution only uses starts of overlays **resident at capture time** (a BATTLE.EMI start had been credited for a SHOP.EMI store in the shared band). [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-05 | **Full-battle context trace: the party actor object mapped.** `capture --watch 0x80145E8C-0x80145FCC` over a whole battle (3600 frames, 22 114 writes, 43 writers, ring not wrapped) attributed every field of the 0x140-byte party actor `C` to its writers across field, transition and battle: boot sprite code (`Actor_Task` +0, `Actor_UpdateScreenPos` +0x2E/+0x30/+0x32/+0x60 every frame, `Actor_AnimTick` +0x4A/+0x58/+0x5A), GAME.EMI field motion (+0x9, +0xC..+0x14, +0x34/+0x38 world x/z, +0x3E depth), `Encounter_PlaceParty` (+355, positions from the table at `0x80143F2C`), `Battle_InitPartyContexts` (+409), `Battle_Init` + **`Battle_InitMemberActor`** (+563), `Battle_ApplyDamage`, `Battle_WriteBackMember` (+1749) then **`Battle_ReloadPartyRecords`** (+1769, persistent → working `memcpy` for every slot, the reverse of the write-back). Three new evidence names, three boot `guessed` symbols, full offset table in [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-05 | **Battle start: the working record is a copy of the persistent one.** `capture --watch 0x80145F0C` across a battle transition caught three writers: GAME.EMI `0x801C253C` once at +481, game-mode `0x801D1490` once at +635, then `0x801E9B74` every ~2 frames. The first is **`Battle_InitPartyContexts(formation)` `0x801C23F8`** (evidence): per slot it builds a context `C = 0x80145E8C + slot*0x140` (present flag, slot, roster byte at `C+0x13C`) and `memcpy`s `0xA8` bytes of the persistent record into `C+0x74 = 0x80145F00 + slot*0x140` — so every persistent offset applies to the working record, and `0x1F800044` / `0x8014624C` are pointers to `C`. Party composition `0x80144F56 + slot + formation*3`, size `0x80146250`, char-id→roster table `0x80182488`. Hypotheses: `Battle_Init` `0x801D1228` (the first game-mode write, 1428 B setup), `Battle_MemberStatusTick`, `Battle_InitPartyMember`. [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-05 | **Level-up decoded end to end; persistent character records proven.** `capture --watch 0x80144B40-0x80144C10` on Teepo's L3→4 (user) caught the whole burst at one frame: six base-stat writes (+4,+4,+2,+2,+1,+4 at rec `+0x3C..+0x46`) and the level byte (`+6`) from `GAME.EMI` code, then boot `Char_RecalcStats` (`0x80165434`, whose body compares against `&0x80144964 + n*0xA4`, n<8 — the record base and stride are now code-proven), `Stat_AddClamped` (`0x80165EE4`, 0..999) from the equipment pass, and `AbilityList_Add` (`0x80165BCC`) storing skill `0x5E` at `+0x71`. `GAME.EMI` (`0x80196800`, 227 KB) imported into Ghidra (582 fn, all decompiled): **`Char_LevelUp(roster)` `0x801AEDD4`** walks a per-roster **level table at `0x801CC068 + roster*0x318 + level*8`** (u16 EXP-to-next, growth bytes hp/ap/pwr|def/agl|int, two skill ids) plus signed Master modifiers at rec `+0x85..+0x8A`. Decoding the table from the disc reproduces Teepo's observed growth byte-for-byte and gives cumulative thresholds 10/30/60/105/173/275 for Ryu — the community chart (10/25/62/155) is wrong for this disc; the 105 seen on screen is right. Also caught: end-of-battle `Battle_WriteBackMember`/`Battle_WriteBackParty` (working HP/AP/status → record), and `0x80145FC8` is the roster index, not an actor state. [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-05 | **Save-file verifier (remote plan track E).** `tools/save_tool.py` reads raw 128 KiB card images (`.mcd`/`.mcr`): `list`, `dump SLOT`, `verify`, `diff A B`. On `saves/card1.mcd` the u16 byte-sum verifies on all three saves, and every load-screen field cross-checks against the block it came from (name bytes, party ids with the Peco rule, level, play time, EXP, flag bit; the SJIS title's hours/minutes/level too). Independent oracle: Mednafen booted from the same card and its load screen showed リュウ Lv 1 at 00:28 / 05:13 / 05:10 with one portrait for file 1 and Teepo / Rei / Ryu for files 2 and 3 — exactly the tool's output (`analysis/mednafen_loadscreen_card1.png`). `diff 2 3` on the user's "identical except time" pair: only the checksum and the play time differ (two runs). Three RAM-map corrections fell out: **`0x8015BFC4` is `Flag_Test`** (bit `0x92` → summary byte, not a hash); **play time is `0x80144FBC` as h/m/s/frame bytes** (the `0x8014686C..` words copied to the block head are something else); and the ability list is **four 10-slot lists at record `+0x5C/+0x66/+0x70/+0x7A`** chosen by ability type (`AbilityList_ForType` `0x80167514`), not one list at `+0x71`. Bonus: the record names give the **whole roster order** — 0 リュウ, 1 ニーナ, 2 ガーランド, 3 ティーポ, 4 レイ, 5 モモ, 6 ペコロス, 7 パピー (char id 10) — the intro's baby dragon (user), proven live from `slot01` the same afternoon: party id `0x0A`, roster byte 7 throughout, `Battle_WriteBackMember` wrote HP 11→7 into record 7 at the intro-boss end (`papi_intro.json`). Char-id→roster table `0x80182488`: 7→0, 8→1, 9→0 (save 1's lone boy Ryu), 10→7, 14→4. Mednafen trap: the title cursor defaults to LOAD GAME and the name-entry screen cannot be cancelled — restart. [`BATTLE_RAM.md`](BATTLE_RAM.md), [`remote-plan-2026-09-05.md`](remote-plan-2026-09-05.md). |
| 2026-09-05 | **Name tables located in the `.EMI` (staged as IDEAS I4).** Searching the overlays for one known name in the in-game encoding (薬草 = `13 1a 12 8b`) found fixed-stride record tables instead of loose strings: **items** at GAME.EMI `0x801C995C` (14-byte `name[8] flags type 0x40 price`; ids 0 なし 1 あおりんご 2 パン 3 薬草 match the saves and the shop's −20/buy), **abilities** at `0x801CB230` (16-byte, the table `AbilityList_ForType` reads — its address was mis-read as `0x801DB231` earlier today and is corrected; Teepo's 0x5B/0x5E/0x67 = パダーマ/レイギル/ドメガ, Momo's 0x46/0x4B = アプリフ/リバル), the **new-game character templates** in START.EMI at `0x801EB4A4` (8 × 0xA4, byte-identical to the saves' untouched records — the record format readable statically), **character names** in COMMU02.EMI (roster order 0..6 again), **place names** in MTEST.EMI and the **masters** list in FIRST.EMI. Nothing built yet; plan and acceptance test in [`IDEAS.md`](IDEAS.md) I4. Also today: パピー (record 7) proven to be the intro's baby dragon (see the track-E row). |
| 2026-09-04 | **Zenny, the lifetime-earned total, and the results overlay.** `ramdiff` (+40 read off the results screen, two rounds) → `capture --watch 0x80144F4C --watch 0x8014502C`: a 1000-frame window saw 0 writes, 1200 saw 80 (+1 ×40 into each) — the results screen's start drifts, so `--window-frames` needs headroom; `capture` now compares watched bytes at arm vs end and warns when a cell changed with no traced write. Writer is boot `Zenny_Add(amount, skip_total)` `0x80166FFC` (zenny `0x80144F4C` cap 9,999,999; lifetime total `0x8014502C` unless skip), called from `0x801EF824`/`0x801EF848` in **`BATL_END.EMI`, resident at `0x801EEC00` during the tally** → overlay promoted to `evidence`, imported into Ghidra (94 fn). EXP row confirmed by a second battle (user): `0x8014496C` = Ryu, `0x80144B58` = Teepo (roster 3) → `BattleResult_AddExp` back to `evidence`. `symbols.toml`: `Rand`, `Zenny_Add`. [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-04 | **EXP and the persistent character records.** A kill anchor + `ramdiff` (+12 read off the results screen) + `capture --watch` found the EXP tick: +1 every 2 frames ×12 into `0x8014496C` (roster 0) and `0x80144B58` (roster 3) → **persistent character records `0x8014496C + roster*0xA4`** (EXP u32, cap 9,999,999; status-screen boot code reads `0x80144964..7C`). The store PC `0x801DD644` lay in a gap no static root or trace covered, and the writer had been misattributed to a boot-EXE start sharing the address; fixed the rule (overlay-band stores attribute to overlay starts only), added `ghidra_run.py import --start` to seed such gaps, decompiled it: `BattleResult_AddExp(amount)` `0x801DD564` + `BattleResult_MemberEligible` / `CharId_ToRosterIndex` (working record `+0x-7` = character id). "Next level" is threshold − EXP (65→77 with 40→28 ⇒ threshold 105). Import now also seeds every traced entry PC (449 starts vs 239; 495 Ghidra functions in the game-mode overlay). Two false positives paid for: `0x800E7CF0` was a CD-ROM DMA buffer (`dma_ch 3`), and long `ramdiff` windows net out later hits (heal +5 then −4) so party HP `0x80145F14` only matched on some rounds. [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-04 | **First battle data anchors landed: enemy HP, the damage path, the HUD.** User ran `ramdiff` on an Attack anchor (damage read off the screen afterwards) → two cells per enemy; `capture --watch` on both showed the same-frame order (working HP `0x801EB74C` written by `0x801DBB40`, then HUD copy `0x801484CC` by `0x801EA204`); the Ghidra bodies gave the record layouts: **enemy working records `0x801EB634 + n*0x118`** (game-mode overlay), **party working records `0x80145F0C + m*0x140`** (boot data, HP `+8`, max `+0x10`), **HUD gauge records `0x801484B8 + n*0x24`** (gauge = hp*55/max). Named with evidence: `Battle_ApplyDamage(attacker,target)`, `Battle_CalcDamage(attacker,target,flags)` (weapon-vs-family doubling, status floors, variance `{50,50,50,50,60,60,60,70}%` via boot `Rand` `0x8017ED4C` mod 8 from the table at `0x801D0C7C`, defend/back-row cuts, 9999 cap), `Battle_BaseDamage`, `HUD_GaugeUpdate`; hypotheses for the two defence steps and `HUD_GaugesTick`; `Rand` into `symbols.toml`. Game-mode overlay header = count word + 24-pointer entry vector (`0x801D0C04` is slot 0). `ramdiff` keeps both snapshots and `ramfilter --intersect FILE=n,n,n` crosses rounds. [`BATTLE_RAM.md`](BATTLE_RAM.md). |
| 2026-09-04 | **Ghidra second route stood up headless; tracer grew data anchors.** `tools/ghidra_run.py` (+ `tools/ghidra/`) drives AnalyzeHeadless through `pyghidra.ghidra_launch` (in-process `pyghidra.start()` recurses on this machine): boot EXE exported (1025 fn, 52 GTE, 77 jump tables), `BATTLE.EMI#3` @ `0x801D0C00` (302 fn) and `#15` @ `0x80093800` (347 fn) imported as their own programs, seeded from the recompiler's roots. Reads: game-mode entry is `base+4` (`0x801D0C04` from boot `0x8014EB20`; WORLD `0x801F2C04` from `0x801532A0`); the battle context pointer lives in scratchpad `0x1F800044` (105 game-mode + 48 engine functions); the three traced roots are table dispatchers on that context; `BattleMenu_TargetCursor(x,y)` and `Battle_FrameTask` promoted to `evidence`; 4 of 12 traced PCs are interior stamps of larger Ghidra functions; `0x801EF188` was in MAGIC069, not BATTLE (row fixed). Tracer: `capture --watch` (runtime write trace, writers attributed by store PC), `writes`, `ramdiff` (find a RAM cell by its delta, no RAM map needed), `name` (single-row upsert); `names/functions.toml` `[[function]]` key now read by both tools (12 rows were invisible to `name_map.py`). [`GHIDRA.md`](GHIDRA.md). |
| 2026-09-04 | **I1 tracer live-tested.** `tools/callstack_diff.py` captured Attack (Circle ×6) vs Defend (`--hold right`, Circle ×3) from a battle-menu anchor (file slot 10): 13 Attack-only / 4 Defend-only / 185 common functions; the Attack-only cluster is the highlighted-icon draw, the per-member pattern (`0x801DB4EC` actor 0/1/2 per frame) is common. Tool fixes paid for: KSEG0 filter form (physical caught 0 entries), `ra`-based nesting (ring `depth` is junk on direct stamps, no exits), sequential presses + `--hold` (the command menu is hold-direction), common-prefix unusable across runs. Then a 900-frame three-way run (Attack / Defend / Watch) with the new `venn` subcommand: 22 attack-only, 11 defend-only, 24 attack+watch (= target selection, correcting the first reading), 0 watch-only; six `hypothesis` names written to `names/functions.toml` for the Battle game-mode overlay (`Battle_FrameTask`, `Attack_Action`, `Defend_Action`, …); `capture` now resolves the resident overlay per band by RAM-prefix match. Two Watch-trigger captures (file slot 11, different enemies) intersect to 12 Examine-only functions (question mark, observed skill, learn check) → six more `Examine_*` hypotheses; Watch's code runs only when an enemy move happens while armed. Details: [`IDEAS.md`](IDEAS.md) I1. |
| 2026-09-04 | **I2 live-tested → HIGH.** The world map is an area (AREA016) whose script block holds the region/node labels and the guide paragraphs (slots 5–16); a guide open is the WORLD-band caller `0x801F2E6C` → boot-EXE message resolver `0x8015034C(idx)` writing `0x801490A8` (observed idx 7 = Mt. Glaus), and entering it loaded AREA023. The aligned corpus already has slot-for-slot English. Write trace on `0x801490A0–C0` is the instrument; `fn_filter` on the immediate draw caught nothing. `tools/callstack_diff.py` (I1) written by subagent, live-tested read-only, battle differential still owed. Details: [`IDEAS.md`](IDEAS.md). |
| 2026-09-04 | **Ideas intake opened** — [`IDEAS.md`](IDEAS.md) catalogs three proposals with feasibility: I1 combat call-stack mapping (HIGH: the runtime's `fn_entry`/`fn_exit` rings already record `ra`/args/depth/`v0`; tool not written), I2 programmatic area labels from map text via the same ring + `decode_text.py` (MEDIUM: which pool holds the labels is the one unknown), I3 1.5x dialogue box + furigana (readings sidecar HIGH from the `D:\BoFIII` Sudachi work; box geometry MEDIUM pending the frame-draw function; runtime rendering LOW — needs an upstream hook, enhancement-phase). Premise corrected: line/page breaks are authored `0x01`/`0x0B`, not auto-wrapped. |
| 2026-09-01 | **Framework synced; both submodules bumped; verified booting.** Merged upstream `mstan/master` into fork branch `fix/static-overlay-residency-signal` (`psxrecomp` `70153175`→`ecc0de16`, clean, 0 conflicts — upstream never touched our two files) and pushed it to `origin`. Bumped `recomp-ui` `8c30e004`→`4eda654` (required: merged psxrecomp uses the multi-disc launcher ABI). Upstream PR **held as draft** — fork is now a living integration branch. Full rebuild (emitters→generate→overlays→psx-runtime) verified: headless boot clean, overlays native at **~99.6% steady-state hit rate**, `gen_fastpath` ~96%, misses frozen post-load, `aborts` 0 — CD-ROM/DMA merge did not perturb the residency signal (`aa6fa2c9`). Two gotchas paid: regenerate `overlay_codegen_hash.h` (target `psxrecomp_codegen_hash`) BEFORE compiling overlays or the stale-recompiler guard fires; recomp-ui must move in lockstep with a psxrecomp master sync. Non-blocking: pre-merge `.pst` savestates load `last_ok: 0` (merge reworked `savestate.c`). [`HANDOFF.md`](HANDOFF.md) → "Shipping state". |
| 2026-09-01 | **Capcom-logo lag root-caused and FIXED.** The opening logo runs from `LOGO/LOGO.EXE` — a standalone 120 KB PS-EXE at `0x801CE000` (not an `.EMI` overlay), which the extraction pipeline never captured, so it ran 100% interpreted (~19 present-fps; `0x801CEEDC` = 91 M insns / 84.5% of interp). Headless is the trivial repro (Capcom is the first screen). Identified by decomposing the phase (interp 5M/s, native ≈0, `frame` counter for fps since the BIOS VSync counter freezes during the intro), reading live bytes (matched no `.EMI` occupant), and a 20-byte disc signature search (one hit → `LOGO/LOGO.EXE`, PS-EXE header t_addr=0x801CE000/t_size=0x1D800/entry=0x801CE724). Fixed by compiling it as a static overlay: new [`tools/extract_logo_overlay.py`](../tools/extract_logo_overlay.py) → `static-emi-v1` capture, merged into `overlay_captures_all.json` (+ `axis_b_loop.sh` phase 3a re-merge). **Result: `0x801CEEDC` native, Capcom interp 20× down (5.0M→0.25M/s), present fps 19→steady ~30, no regression.** ~30 is now pacing-limited (idle CPU; `CAPCOM30.STR` FMV/CD streaming), not CPU-limited. Pass-2 harvested the 10 jump-table interior entries (`analysis/logo_observed.json`). Compares to MMX4/5/6 which absorb their logo EXE automatically via the runtime-capture path (`overlay_cache=true`) we keep off. |
| 2026-09-01 | **Capcom FMV slowdown root-caused and fixed in the framework.** Host gdb stack sampling on a clean boot found ~40% of emu-thread time building `SpuGlobalState` snapshots just to read one SPUCNT bit on every device-service gate. Fix: cheap `spu_ctrl_read()` gate, identical semantics — upstream [mstan/psxrecomp#292](https://github.com/mstan/psxrecomp/pull/292). Measured headless clean boot: build-dbg 28.5→~52 vblank/s; RelWithDebInfo 97.6/s uncapped (~1.6× realtime). **User-verified windowed on `build-relprof`: Capcom logo, world map and memcard all hold 60 fps with clean audio.** Two wrong theses on the way (present-rate pacing; the interpreted BIOS IRQ handler) are recorded in [`vblank-pacing-bug.md`](vblank-pacing-bug.md). New tool `tools/fmv_bench.py`. |
| 2026-09-01 | **All psxrecomp PRs merged upstream; pin returned to `mstan/master`.** #289 (overlay dispatch trio), #290 (scanlines), #292 (SPU gate) merged; `psxrecomp` gitlink `ecc0de16` → **`1bf70960`** (upstream master, no fork commits). `recomp-ui` gitlink `4eda654` → `fda07fe` (fork `feat/present-scanlines`, pending [mstan/recomp-ui#42](https://github.com/mstan/recomp-ui/pull/42)). Full regenerate (emitters → generate → codegen hash → all overlay bands + LOGO → `build-dbg` and `build-relprof`). Benign audit-failure count 6 → 7 (`0x801EEC00` +1), expected shape. Smoke-tested headless on `build-relprof`: FMV 206 vblank/s uncapped (`fmv_bench.py`), overlay dispatch 98.3% hits with `crc_misses` frozen post-load, ~300 emu fps at the title. Docs cleaned for staleness: STATUS/HANDOFF/INVENTORY rewritten, vblank doc restructured around the fix, README index refreshed. |
| 2026-09-02 | **Gamepad input dead in-game: root-caused, fixed in `game.toml`.** The launcher recognised an Xbox Series X pad and wrote its GUID to `settings.toml`, but recomp-ui's `apply_default_pad_mode_for_source` sets every gamepad seat to **Analog** (poll id `0x73`), overriding the title's `default_mode = "digital"`. SLPS-00990 predates the DualShock and its pad reader ignores a `0x73` pad. Verified by A/B on the world-map savestate (`slot07`, headless `build-dbg`): with `p1_mode = "digital"` an injected Start opens the camp menu; with `"analog"` the screen never changes. Fix: `[controller] lock_mode = true` (the framework's digital-only-title key, as used for X4 / Tomba 2) — the launcher hides the selector and the runtime clamps a stale `settings.toml` mode. Keyboard was unaffected because keyboard seats are always digital. |
| 2026-09-02 | **Two battle rendering reports triaged.** (1) Enlarged command icon missing its top quarter: renderer is faithful (CPU mirror == GL FBO); the strip texture at VRAM (256..297, 480..503) has rows 480..485 zeroed, blocks 0-1 of `FIRST.EMI` section 6 lost exactly their first 384 bytes once at boot, Beetle savestate VRAM has all rows, so this is a runtime defect; the stage (CD read vs upload) awaits a watched relaunch. [`battle-icon-strip-rows.md`](battle-icon-strip-rows.md). (2) Rei drawn behind the house door during attacks: list order honoured; Beetle also phases attackers behind the door, so game behaviour. [`battle-depth-order.md`](battle-depth-order.md). |
| 2026-09-02 | **Build-dbg "hard crash" root-caused to a false starvation-watchdog trip.** No Windows crash record; `build-dbg/psx_last_run_report.json` (reason `atexit`, exit_origin `unknown`) has `starvation_watchdog_check → exit(2)` on its native stack, while `starvation_dump.jsonl` shows the heartbeat only 402 µs stale and the heartbeat ring shows 60 fps to the last sample. Cause: cross-thread read race in the watchdog (IO-thread heartbeat newer than the pre-read clock → unsigned wrap). Watchdog disabled machine-wide for debug testing via persisted `PSX_STARVATION_TIMEOUT_US=0`; upstream patch (read order + exit-origin label) to follow. Known-issues entry updated. |
| 2026-09-02 | **Second build-dbg exit of the day was a Windows Terminal crash, and it cost a 74-minute session's overlay harvest.** `WindowsTerminal.exe` faulted at 16:31:11 (`Windows.UI.Xaml.dll`, `0xc000027b`, first such event in 14 days); the game — a console-subsystem exe attached to that console — wrote `psx_last_run_report.json` four seconds later at frame 264564 with `reason: atexit` / `exit_origin: unknown` and no starvation dump (the persisted env var held). The runtime's `CTRL_CLOSE_EVENT` handler calls `exit(0)` without setting an origin — third unlabelled exit site, added to [`starvation-watchdog-false-trip.md`](starvation-watchdog-false-trip.md). The interp-PC rings live only in the process, so that session's PCs are gone (area timeline survived). **Mitigation landed:** `area_poller.py watch` now calls `harvest_interp_pcs.harvest()` every 15 min and on Ctrl-C (`--harvest-every`), with an atomic temp+replace write of the observed file; `harvest_interp_pcs.py` refactored around that function (CLI unchanged). Unit-tested against a fake server. Launch tip: run the game under `conhost.exe …` so a Terminal crash cannot reach it. |
| 2026-09-02 | **Fast-forward gets a controller host shortcut.** Turbo was keyboard-only (`[KeyMap] Turbo`, Tab). psxrecomp `feat/fast-forward-pad` (`2ae78109`, off upstream `22fbbfca`): third assist binding `Fast-forward`, default chord Select+L1 (`1528`), same `hotkey_pad_binding_down()` matcher as Rewind / Save states, threaded through every settings hop and persisted as `[hotkeys] fast_forward_pad`; guard test extended, README documents the chord. recomp-ui `feat/fast-forward-pad` (`6c7cd32`, off upstream `d8187a4`): the Controller page's Host Shortcuts grid drew only two actions, now every one the runtime advertises. Both cherry-picked onto local integration branches `bof3/int-fast-forward` (psxrecomp `adf54eaa`, recomp-ui `b4c8f51`) and the gitlinks moved there; syntax-checked against the real build flags, then both `build-relprof` and `build-dbg` rebuilt clean (ninja rc 0, 2026-09-02 20:15) with the new strings in the exe; not yet play-tested. Runtime PR opened: [mstan/psxrecomp#307](https://github.com/mstan/psxrecomp/pull/307); launcher PR [mstan/recomp-ui#47](https://github.com/mstan/recomp-ui/pull/47) conflicted on arrival: upstream #46 (`42c2870`) had already replaced the two-row grid with a three-column table over every binding, so #47 is redundant — close it; the local cherry-pick stays only until the pin returns to upstream master. |
| 2026-09-02 | **recomp-ui pin moved to #42 + current upstream master.** New integration branch `bof3/int-scanlines-master` (`a736d57`) = `fda07fe` with `origin/master` `da80dc7` merged; one conflict in `recomp_launcher.h` where #42 and #46 both appended struct fields, resolved upstream-first (`virtual_stylus` before `scanlines`). Brings the NDS stylus work and the rewritten three-column Host Shortcuts grid, so Fast-forward shows on the Controller page without #47. Both `build-relprof` and `build-dbg` rebuilt clean (ninja rc 0, 2026-09-02 ~20:45); not yet play-tested. |
| 2026-09-03 | **Mednafen oracle wired up.** Stock Mednafen 1.32.1 (Beetle's parent) in `./mednafen/` (gitignored) loads our disc and, since memcards are the same raw 128 KiB image on both sides, boots straight from `saves/card1.mcd` copied to its name; savestates do not cross. No scripting surface exists (`-remote` is a bare flag), so `tools/mednafen_ctl.py` injects scancodes from its own cfg bindings, guarded by a foreground-window check. Verified end to end: Start/Down/Circle reached the load screen listing all three saves, savestate slot round-trip, snapshot, clean quit. Traps: `MEDNAFEN_HOME`, Circle confirms on the JP release. [`MEDNAFEN.md`](MEDNAFEN.md). |
| 2026-09-03 | **Ground plane vanishing after the herb effect: root-caused and fixed in the framework.** Player report with savestates (`slot04` reproduces ~35 frames after load). GP0 ring showed the terrain quads still drawn but the 8bpp texture page at VRAM (448,256) zeroed; `.pst` diff proved the file intact, so the wipe was emulated execution; a frame-exact trace found a stray 341×341 FILL that was really a colour word of a gouraud polyline: our GP0 parser tested **every** streamed word for the `0x50005000` terminator and a Psy-Q junk-top-byte colour `0x52545454` matched, de-phasing the stream. Both oracles (Beetle `INCMD_PLINE`, DuckStation `HandleRenderPolyLineCommand`) skip the first two vertices and test only the first word of each vertex unit. Fixed in `psxrecomp` fork branch `fix/gpu-polyline-terminator` `402cada6` (off `master` `d08d84a3`, savestate-size neutral); `build-relprof` rebuilt and verified (page intact over 6000+ frames, terrain renders). Upstream PR [mstan/psxrecomp#313](https://github.com/mstan/psxrecomp/pull/313) opened by the user; `build-dbg` rebuilt and verified the same day. New `tools/pst_tool.py` parses savestates offline. [`gpu-polyline-terminator.md`](gpu-polyline-terminator.md). |
| 2026-09-02 | **`tools/run_dbg.cmd` added** after a hand-typed `conhost.exe …` launch of build-dbg died at startup (report: `frame 0`, no guest code run, `reason: atexit`) with the error lost when the console closed. The exe launches fine from PowerShell and from `conhost.exe cmd /c …` here, so the cause is unknown; the script keeps stderr in `build-dbg/stderr.log` and holds the window open on failure so the next occurrence is readable. Verified: game up in 10 s via the script; bogus `--game` produces a readable log. |
